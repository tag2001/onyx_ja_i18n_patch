From 135405e4e2db94d70cca7d654a3e2c69f1a7f67a Mon Sep 17 00:00:00 2001
From: Hajime Taguchi <hajime.taguchi.bp@jp.nttdata.com>
Date: Wed, 10 Jun 2026 17:32:21 +0900
Subject: [PATCH] feat(web): add Japanese UI localization draft

---
 docs/onyx_ja_i18n_patch_draft.md              | 214 ++++++
 web/src/app/admin/actions/mcp/page.tsx        |   2 +-
 web/src/app/admin/actions/open-api/page.tsx   |   2 +-
 web/src/app/app/settings/layout.tsx           |  15 +-
 web/src/app/auth/create-account/page.tsx      |  34 +-
 web/src/app/auth/error/AuthErrorContent.tsx   |  44 +-
 web/src/app/auth/forgot-password/page.tsx     |  52 +-
 web/src/app/auth/login/EmailPasswordForm.tsx  | 382 ++++++-----
 web/src/app/auth/login/LoginPage.tsx          |  21 +-
 web/src/app/auth/login/LoginText.tsx          |   9 +-
 web/src/app/auth/login/SignInButton.tsx       |  65 +-
 web/src/app/auth/login/page.tsx               |  10 +-
 .../auth/signup/ReferralSourceSelector.tsx    |  30 +-
 web/src/app/auth/signup/page.tsx              |  34 +-
 web/src/app/auth/verify-email/Verify.tsx      |  28 +-
 .../RequestNewVerificationEmail.tsx           |  10 +-
 .../app/auth/waiting-on-verification/page.tsx |  33 +-
 .../app/craft/v1/apps/oauth/callback/page.tsx |  44 +-
 web/src/app/federated/oauth/callback/page.tsx |  35 +-
 web/src/app/global-error.tsx                  |  50 +-
 web/src/app/layout.tsx                        |  51 +-
 web/src/app/mcp/oauth/callback/page.tsx       |  40 +-
 web/src/app/oauth-config/callback/page.tsx    |  22 +-
 web/src/components/LanguageSwitcher.tsx       |  34 +
 web/src/components/auth/AuthFlowContainer.tsx |  11 +-
 web/src/components/errorPages/ErrorPage.tsx   |  23 +-
 .../components/oauth/OAuthCallbackPage.tsx    |  48 +-
 web/src/lib/admin-routes.ts                   | 124 ++--
 web/src/lib/i18n.ts                           | 630 ++++++++++++++++++
 web/src/providers/LocaleProvider.tsx          |  67 ++
 web/src/sections/AppHealthBanner.tsx          |  15 +-
 web/src/sections/LicenseExpiryBanner.tsx      |  36 +-
 web/src/sections/actions/ActionCardHeader.tsx |  11 +-
 web/src/sections/actions/Actions.tsx          |  98 ++-
 web/src/sections/actions/MCPActionCard.tsx    |  80 +--
 web/src/sections/actions/MCPPageContent.tsx   | 136 +++-
 .../sections/actions/OpenApiActionCard.tsx    |  41 +-
 .../sections/actions/OpenApiPageContent.tsx   | 103 ++-
 .../sections/actions/PerUserAuthConfig.tsx    |  47 +-
 web/src/sections/actions/ToolsList.tsx        |  48 +-
 web/src/sections/actions/ToolsSection.tsx     |  17 +-
 .../actions/modals/AddMCPServerModal.tsx      | 127 +++-
 .../actions/modals/AddOpenAPIActionModal.tsx  | 159 +++--
 .../actions/modals/DisconnectEntityModal.tsx  |  28 +-
 .../actions/modals/MCPAuthenticationModal.tsx | 260 ++++++--
 .../modals/OpenAPIAuthenticationModal.tsx     | 146 +++-
 web/src/sections/sidebar/AccountPopover.tsx   |  21 +-
 web/src/sections/sidebar/AdminSidebar.tsx     |  24 +-
 web/src/sections/sidebar/AppSidebar.tsx       |  52 +-
 web/src/sections/sidebar/ChatButton.tsx       |  37 +-
 .../sidebar/ChatSearchCommandMenu.tsx         |  76 +--
 web/src/sections/sidebar/sidebarUtils.ts      |   4 +-
 52 files changed, 2732 insertions(+), 998 deletions(-)
 create mode 100644 docs/onyx_ja_i18n_patch_draft.md
 create mode 100644 web/src/components/LanguageSwitcher.tsx
 create mode 100644 web/src/lib/i18n.ts
 create mode 100644 web/src/providers/LocaleProvider.tsx

diff --git a/docs/onyx_ja_i18n_patch_draft.md b/docs/onyx_ja_i18n_patch_draft.md
new file mode 100644
index 0000000000..257f379a85
--- /dev/null
+++ b/docs/onyx_ja_i18n_patch_draft.md
@@ -0,0 +1,214 @@
+# Onyx 日本語化パッチドラフト
+
+作成日: 2026-06-10
+
+対象リポジトリ: `onyx-dot-app/onyx`
+
+ローカル作業ディレクトリ: `C:\Users\bttaguchihj\Documents\Codex\2026-06-10\Onyx`
+
+対象コミット: `b70008df6b538c40e2a44414d751e73494a0ab3e`
+
+対象 Web パッケージ: `web@1.0.0-dev`
+
+主要フロントエンドバージョン: Next.js `16.2.6`, React `19.2.4`
+
+## 目的
+
+Onyx の主要 UI を日本語で利用できるようにするためのパッチドラフトです。
+
+今回のドラフトでは、以前の作業で発生したエラーを踏まえ、単純な文字列置換だけではなく、ビルド確認とブラウザ確認を行いながら修正範囲を再整理しています。
+
+## 今回の到達点
+
+- 日本語 locale の基盤を追加
+- 認証系 UI を日本語化
+- 共通エラー画面、グローバルエラー、OAuth コールバック画面を日本語化
+- サイドバー、アカウントメニュー、チャット検索周辺を日本語化
+- MCP / OpenAPI アクション管理画面を日本語化
+- MCP / OpenAPI の追加モーダル、認証設定モーダルを日本語化
+- 管理画面のルート名、サイドバー見出し、主要ボタンを日本語化
+- `npm run build` と `git diff --check` が成功することを確認
+
+## バージョン更新時の効率的な確認観点
+
+Onyx は UI コンポーネントや管理画面の構成が更新で変わる可能性があります。そのため、次回以降は「ファイル名だけ」ではなく「画面要素の種類」で確認すると効率的です。
+
+### 最優先で確認する要素
+
+- 画面タイトル: ページヘッダー、モーダルタイトル、空状態カードのタイトル
+- 主要説明文: ページヘッダー説明、モーダル説明、フォーム補足文
+- 主要操作ボタン: 追加、保存、接続、認証、キャンセル、削除、切断
+- フォームラベル: 入力欄名、任意/必須表示、placeholder、helper text
+- 空状態: データ未登録時の説明、追加導線
+- エラー表示: global error、backend unavailable、OAuth callback error、validation error
+- サイドバー: セクション見出し、ナビゲーション項目、検索 placeholder、アカウントメニュー
+- 認証フロー: login、signup、forgot/reset password、verify email、waiting verification
+- OAuth / MCP / OpenAPI callback: loading、success、failure、redirect 関連文言
+- toast / notification: 成功、失敗、削除、接続、保存、更新
+
+### バージョン更新で変わりやすい要素
+
+- 管理画面のページ構成とルート名
+- MCP / OpenAPI のカード表示、認証方式、フォーム項目
+- エージェント、コネクタ、モデル設定などの管理画面本文
+- Opal UI コンポーネント由来の共通ラベル
+- API から返る description、tool name、connector name などのデータ由来テキスト
+
+これらはコード検索だけでは取りこぼしやすいため、実 backend または mock backend でログイン済み画面を開き、DOM の可視テキストを確認するのが有効です。
+
+### 変更可能性が低く、ファイル単位で追いやすい要素
+
+以下は固定メッセージ中心のため、バージョン更新後も同じファイルを見れば確認しやすい箇所です。
+
+- `web/src/components/errorPages/ErrorPage.tsx`
+- `web/src/app/global-error.tsx`
+- `web/src/components/auth/AuthFlowContainer.tsx`
+- `web/src/components/oauth/OAuthCallbackPage.tsx`
+- `web/src/app/oauth-config/callback/page.tsx`
+- `web/src/app/mcp/oauth/callback/page.tsx`
+- `web/src/app/federated/oauth/callback/page.tsx`
+- `web/src/app/craft/v1/apps/oauth/callback/page.tsx`
+- `web/src/sections/AppHealthBanner.tsx`
+- `web/src/sections/LicenseExpiryBanner.tsx`
+
+## 現在の主な変更内容
+
+### 日本語化基盤
+
+- `web/src/lib/i18n.ts`
+- `web/src/providers/LocaleProvider.tsx`
+- `web/src/components/LanguageSwitcher.tsx`
+- `web/src/app/layout.tsx`
+
+locale を保持し、`ja` / `en` の表示切替ができる基盤を追加しています。Cookie と localStorage を使い、画面遷移後も locale を維持します。
+
+### 認証系 UI
+
+- `web/src/app/auth/login/**`
+- `web/src/app/auth/signup/**`
+- `web/src/app/auth/create-account/page.tsx`
+- `web/src/app/auth/forgot-password/page.tsx`
+- `web/src/app/auth/reset-password/page.tsx`
+- `web/src/app/auth/verify-email/Verify.tsx`
+- `web/src/app/auth/waiting-on-verification/**`
+- `web/src/app/auth/error/AuthErrorContent.tsx`
+
+ログイン、サインアップ、メール確認、パスワードリセット、認証エラー周辺の表示文言を日本語化しています。
+
+### 共通エラーと OAuth コールバック
+
+- `web/src/components/errorPages/ErrorPage.tsx`
+- `web/src/components/auth/AuthFlowContainer.tsx`
+- `web/src/app/global-error.tsx`
+- `web/src/components/oauth/OAuthCallbackPage.tsx`
+- `web/src/app/oauth-config/callback/page.tsx`
+- `web/src/app/mcp/oauth/callback/page.tsx`
+- `web/src/app/federated/oauth/callback/page.tsx`
+- `web/src/app/craft/v1/apps/oauth/callback/page.tsx`
+
+backend 未起動時の fallback、予期しないエラー、OAuth 成功/失敗/リダイレクト中の文言を日本語化しています。
+
+### サイドバーと管理メニュー
+
+- `web/src/lib/admin-routes.ts`
+- `web/src/sections/sidebar/AdminSidebar.tsx`
+- `web/src/sections/sidebar/AppSidebar.tsx`
+- `web/src/sections/sidebar/AccountPopover.tsx`
+- `web/src/sections/sidebar/ChatButton.tsx`
+- `web/src/sections/sidebar/ChatSearchCommandMenu.tsx`
+- `web/src/sections/sidebar/sidebarUtils.ts`
+
+管理画面ルート名、サイドバーセクション、検索、チャット操作、アカウントメニュー周辺の表示を日本語化しています。
+
+### MCP / OpenAPI アクション管理
+
+- `web/src/app/admin/actions/mcp/page.tsx`
+- `web/src/app/admin/actions/open-api/page.tsx`
+- `web/src/sections/actions/MCPPageContent.tsx`
+- `web/src/sections/actions/OpenApiPageContent.tsx`
+- `web/src/sections/actions/MCPActionCard.tsx`
+- `web/src/sections/actions/OpenApiActionCard.tsx`
+- `web/src/sections/actions/ActionCardHeader.tsx`
+- `web/src/sections/actions/PerUserAuthConfig.tsx`
+- `web/src/sections/actions/modals/AddMCPServerModal.tsx`
+- `web/src/sections/actions/modals/AddOpenAPIActionModal.tsx`
+- `web/src/sections/actions/modals/MCPAuthenticationModal.tsx`
+- `web/src/sections/actions/modals/OpenAPIAuthenticationModal.tsx`
+- `web/src/sections/actions/modals/DisconnectEntityModal.tsx`
+
+MCP / OpenAPI の一覧、カード、追加モーダル、認証設定モーダル、切断確認モーダルを日本語化しています。
+
+## ブラウザ確認済み範囲
+
+mock backend を用いて管理者ログイン済み相当の状態を作り、以下を確認しました。
+
+- `/admin/actions/mcp`
+- MCP 追加モーダル
+- MCP 認証モーダル
+- `/admin/actions/open-api`
+- OpenAPI 追加モーダル
+- OpenAPI 認証モーダル
+- backend 未起動時の `/auth/login`, `/auth/signup`, `/admin/actions` fallback 表示
+
+mock データ由来の `Mock MCP Server`, `Weather API`, `Weather lookup action` などは翻訳対象外として扱っています。
+
+## 確認コマンド
+
+`web` ディレクトリで以下を実行済みです。
+
+```powershell
+npm run build
+```
+
+結果: 成功
+
+リポジトリルートで以下を実行済みです。
+
+```powershell
+git diff --check
+```
+
+結果: 成功
+
+## 残作業
+
+- 実 backend / DB / 管理者アカウントでの最終ブラウザ確認
+- Agents、Connectors、Language Models、Chat Preferences など、MCP/OpenAPI 以外の管理画面本文の追加日本語化
+- チャット実行画面、ファイル添付、検索結果、フィードバック導線の可視英語確認
+- API 由来データと UI 固定文言の切り分け
+- パッチファイルの書き出し
+
+## 実 backend 付き確認で見るべき画面
+
+優先度順に確認します。
+
+1. `/auth/login`, `/auth/signup`, `/auth/forgot-password`
+2. `/app`
+3. `/app/settings`
+4. `/admin/actions/mcp`
+5. `/admin/actions/open-api`
+6. `/admin/agents`
+7. `/admin/add-connector`
+8. `/admin/indexing/status`
+9. `/admin/configuration/language-models`
+10. `/admin/configuration/chat-preferences`
+
+各画面では、画面タイトル、説明文、主要ボタン、フォームラベル、空状態、エラー表示、モーダル、toast を確認します。
+
+## セキュリティ確認メモ
+
+今回の UI 翻訳修正では、以下を確認済みです。
+
+- OAuth callback の redirect は外部 URL へ直接飛ばないよう制御されている
+- markdown 表示は既存の sanitize 経路を利用している
+- 外部リンクは既存の link/button コンポーネントの `noopener noreferrer` 経路に乗る
+- 翻訳文字列に `dangerouslySetInnerHTML` は追加していない
+
+残る確認対象は、実 backend 付きでの認証済み画面、フォーム送信、toast、OAuth 接続フローです。
+
+## 注意点
+
+- PowerShell の表示では日本語が文字化けして見えることがあります。ビルドが通っており、ブラウザ表示で日本語が確認できる場合は、ファイル破損ではなくコンソール表示の問題である可能性が高いです。
+- 技術用語の `OAuth`, `OpenAPI`, `MCP`, `Client ID`, `Dynamic Client Registration`, `SCIM` などは、原則として英語表記を残しています。
+- API から返るツール名、サーバー名、説明文、コネクタ名はデータ由来のため、このパッチの固定 UI 翻訳対象外です。
+- バージョン更新後は、ファイル差分だけでなく、上記の画面要素単位で再確認してください。
diff --git a/web/src/app/admin/actions/mcp/page.tsx b/web/src/app/admin/actions/mcp/page.tsx
index b5445493ca..817bd3a011 100644
--- a/web/src/app/admin/actions/mcp/page.tsx
+++ b/web/src/app/admin/actions/mcp/page.tsx
@@ -12,7 +12,7 @@ export default function Main() {
       <SettingsLayouts.Header
         icon={route.icon}
         title={route.title}
-        description="Connect MCP (Model Context Protocol) servers to add custom actions and tools for your agents."
+        description="MCP (Model Context Protocol) サーバーを接続して、エージェントにカスタムアクションやツールを追加します。"
         divider
       />
       <SettingsLayouts.Body>
diff --git a/web/src/app/admin/actions/open-api/page.tsx b/web/src/app/admin/actions/open-api/page.tsx
index 504bf94c1d..7bbf222634 100644
--- a/web/src/app/admin/actions/open-api/page.tsx
+++ b/web/src/app/admin/actions/open-api/page.tsx
@@ -12,7 +12,7 @@ export default function Main() {
       <SettingsLayouts.Header
         icon={route.icon}
         title={route.title}
-        description="Connect OpenAPI servers to add custom actions and tools for your agents."
+        description="OpenAPI サーバーを接続して、エージェントにカスタムアクションやツールを追加します。"
         divider
       />
       <SettingsLayouts.Body>
diff --git a/web/src/app/app/settings/layout.tsx b/web/src/app/app/settings/layout.tsx
index 1932772ea3..e29462ef3b 100644
--- a/web/src/app/app/settings/layout.tsx
+++ b/web/src/app/app/settings/layout.tsx
@@ -7,6 +7,8 @@ import { SvgSliders } from "@opal/icons";
 import { useUser } from "@/providers/UserProvider";
 import { useAuthType } from "@/lib/hooks";
 import { Section } from "@/layouts/general-layouts";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 interface LayoutProps {
   children: React.ReactNode;
@@ -16,6 +18,7 @@ export default function Layout({ children }: LayoutProps) {
   const pathname = usePathname();
   const { user } = useUser();
   const authType = useAuthType();
+  const { locale } = useLocale();
 
   const showPasswordSection = Boolean(user?.password_configured);
   const showTokensSection = authType !== null;
@@ -23,7 +26,7 @@ export default function Layout({ children }: LayoutProps) {
 
   return (
     <SettingsLayouts.Root width="lg">
-      <SettingsLayouts.Header icon={SvgSliders} title="Settings" divider />
+      <SettingsLayouts.Header icon={SvgSliders} title={t(locale, "sidebar.settings")} divider />
 
       <SettingsLayouts.Body>
         <Section
@@ -32,7 +35,6 @@ export default function Layout({ children }: LayoutProps) {
           alignItems="start"
           gap={1.5}
         >
-          {/* Left: Tab Navigation */}
           <div
             data-testid="settings-left-tab-navigation"
             className="flex flex-col px-2 min-w-50"
@@ -41,31 +43,30 @@ export default function Layout({ children }: LayoutProps) {
               href="/app/settings/general"
               selected={pathname === "/app/settings/general"}
             >
-              General
+              {locale === "ja" ? "一般" : "General"}
             </SidebarTab>
             <SidebarTab
               href="/app/settings/chat-preferences"
               selected={pathname === "/app/settings/chat-preferences"}
             >
-              Chat Preferences
+              {locale === "ja" ? "チャット設定" : "Chat Preferences"}
             </SidebarTab>
             {showAccountsAccessTab && (
               <SidebarTab
                 href="/app/settings/accounts-access"
                 selected={pathname === "/app/settings/accounts-access"}
               >
-                Accounts & Access
+                {locale === "ja" ? "アカウントとアクセス" : "Accounts & Access"}
               </SidebarTab>
             )}
             <SidebarTab
               href="/app/settings/connectors"
               selected={pathname === "/app/settings/connectors"}
             >
-              Connectors
+              {locale === "ja" ? "コネクタ" : "Connectors"}
             </SidebarTab>
           </div>
 
-          {/* Right: Tab Content */}
           {children}
         </Section>
       </SettingsLayouts.Body>
diff --git a/web/src/app/auth/create-account/page.tsx b/web/src/app/auth/create-account/page.tsx
index 57e8453c00..28b9bd3a6e 100644
--- a/web/src/app/auth/create-account/page.tsx
+++ b/web/src/app/auth/create-account/page.tsx
@@ -1,25 +1,37 @@
 "use client";
 
-import AuthFlowContainer from "@/components/auth/AuthFlowContainer";
-import { REGISTRATION_URL } from "@/lib/constants";
-import { Button } from "@opal/components";
 import Link from "next/link";
+import { Button } from "@opal/components";
 import { SvgImport } from "@opal/icons";
 
+import AuthFlowContainer from "@/components/auth/AuthFlowContainer";
+import { REGISTRATION_URL } from "@/lib/constants";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
+
 export default function Page() {
+  const { locale } = useLocale();
+
   return (
     <AuthFlowContainer>
       <div className="flex flex-col space-y-6">
         <h2 className="text-2xl font-bold text-text-900 text-center">
-          Account Not Found
+          {t(locale, "auth.accountNotFound")}
         </h2>
         <p className="text-text-700 max-w-md text-center">
-          We couldn&apos;t find your account in our records. To access Onyx, you
-          need to either:
+          {t(locale, "auth.accountNotFoundBody")}
         </p>
         <ul className="list-disc text-left text-text-600 w-full pl-6 mx-auto">
-          <li>Be invited to an existing Onyx team</li>
-          <li>Create a new Onyx team</li>
+          <li>
+            {locale === "ja"
+              ? "既存の Onyx チームに招待される"
+              : "Be invited to an existing Onyx team"}
+          </li>
+          <li>
+            {locale === "ja"
+              ? "新しい Onyx チームを作成する"
+              : "Create a new Onyx team"}
+          </li>
         </ul>
         <div className="flex justify-center">
           <Button
@@ -27,16 +39,16 @@ export default function Page() {
             width="full"
             icon={SvgImport}
           >
-            Create New Organization
+            {t(locale, "auth.createNewOrganization")}
           </Button>
         </div>
         <p className="text-sm text-text-500 text-center">
-          Have an account with a different email?{" "}
+          {t(locale, "auth.accountNotFoundDifferentEmail")}{" "}
           <Link
             href="/auth/login"
             className="text-action-link-05 hover:underline"
           >
-            Sign in
+            {t(locale, "auth.signIn")}
           </Link>
         </p>
       </div>
diff --git a/web/src/app/auth/error/AuthErrorContent.tsx b/web/src/app/auth/error/AuthErrorContent.tsx
index 578c4c711f..ce5adfbd96 100644
--- a/web/src/app/auth/error/AuthErrorContent.tsx
+++ b/web/src/app/auth/error/AuthErrorContent.tsx
@@ -5,21 +5,18 @@ import Text from "@/refresh-components/texts/Text";
 import { Button } from "@opal/components";
 
 import { NEXT_PUBLIC_CLOUD_ENABLED } from "@/lib/constants";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
-// Maps raw IdP/OAuth error codes to user-friendly messages.
-// If the message is a known code, we replace it; otherwise show it as-is.
 const ERROR_CODE_MESSAGES: Record<string, string> = {
-  access_denied: "Access was denied by your identity provider.",
-  login_required: "You need to log in with your identity provider first.",
-  consent_required:
-    "Your identity provider requires consent before continuing.",
-  interaction_required:
-    "Additional interaction with your identity provider is required.",
-  invalid_scope: "The requested permissions are not available.",
-  server_error:
-    "Your identity provider encountered an error. Please try again.",
+  access_denied: "認証プロバイダーによってアクセスが拒否されました。",
+  login_required: "続行するには、認証プロバイダーでログインしてください。",
+  consent_required: "続行する前に、認証プロバイダーで同意が必要です。",
+  interaction_required: "認証プロバイダーで追加の操作が必要です。",
+  invalid_scope: "要求された権限は利用できません。",
+  server_error: "認証プロバイダーでエラーが発生しました。もう一度お試しください。",
   temporarily_unavailable:
-    "Your identity provider is temporarily unavailable. Please try again later.",
+    "認証プロバイダーが一時的に利用できません。しばらくしてから再試行してください。",
 };
 
 function resolveMessage(raw: string | null): string | null {
@@ -32,17 +29,19 @@ interface AuthErrorContentProps {
 }
 
 function AuthErrorContent({ message: rawMessage }: AuthErrorContentProps) {
+  const { locale } = useLocale();
   const message = resolveMessage(rawMessage);
+
   return (
     <AuthFlowContainer>
       <div className="flex flex-col items-center gap-4">
         <Text headingH2 text05>
-          Authentication Error
+          {t(locale, "auth.authenticationError")}
         </Text>
         <Text mainContentBody text03>
-          There was a problem with your login attempt.
+          {t(locale, "auth.loginAttemptProblem")}
         </Text>
-        {/* TODO: Error card component */}
+
         <div className="w-full rounded-12 border border-status-error-05 bg-status-error-00 p-4">
           {message ? (
             <Text mainContentBody className="text-status-error-05">
@@ -51,36 +50,35 @@ function AuthErrorContent({ message: rawMessage }: AuthErrorContentProps) {
           ) : (
             <div className="flex flex-col gap-2 px-4">
               <Text mainContentEmphasis className="text-status-error-05">
-                Possible Issues:
+                {t(locale, "auth.possibleIssues")}
               </Text>
               <Text as="li" mainContentBody className="text-status-error-05">
-                Incorrect or expired login credentials
+                {t(locale, "auth.incorrectOrExpiredLoginCredentials")}
               </Text>
               <Text as="li" mainContentBody className="text-status-error-05">
-                Temporary authentication system disruption
+                {t(locale, "auth.temporaryAuthenticationSystemDisruption")}
               </Text>
               <Text as="li" mainContentBody className="text-status-error-05">
-                Account access restrictions or permissions
+                {t(locale, "auth.accountAccessRestrictionsOrPermissions")}
               </Text>
             </div>
           )}
         </div>
 
         <Button href="/auth/login" width="full">
-          Return to Login Page
+          {t(locale, "auth.returnToLoginPage")}
         </Button>
 
         <Text mainContentBody text04>
           {NEXT_PUBLIC_CLOUD_ENABLED ? (
             <>
-              If you continue to experience problems, please reach out to the
-              Onyx team at{" "}
+              {t(locale, "auth.contactSupport")}{" "}
               <a href="mailto:support@onyx.app" className="text-action-link-05">
                 support@onyx.app
               </a>
             </>
           ) : (
-            "If you continue to experience problems, please reach out to your system administrator for assistance."
+            t(locale, "auth.contactAdministrator")
           )}
         </Text>
       </div>
diff --git a/web/src/app/auth/forgot-password/page.tsx b/web/src/app/auth/forgot-password/page.tsx
index 17d4bdb205..905a68a956 100644
--- a/web/src/app/auth/forgot-password/page.tsx
+++ b/web/src/app/auth/forgot-password/page.tsx
@@ -1,23 +1,25 @@
 "use client";
-import React, { useState } from "react";
-import { forgotPassword } from "./utils";
+
+import { useState } from "react";
+import { Form, Formik } from "formik";
+import * as Yup from "yup";
+import { redirect } from "next/navigation";
+
 import AuthFlowContainer from "@/components/auth/AuthFlowContainer";
 import Title from "@/components/ui/title";
-import { Text } from "@opal/components";
+import { Text, Button, Spacer } from "@opal/components";
 import { markdown } from "@opal/utils";
-import { Spacer } from "@opal/components";
-import Link from "next/link";
-import { Button } from "@opal/components";
-import { Form, Formik } from "formik";
-import * as Yup from "yup";
 import { TextFormField } from "@/components/Field";
 import { toast } from "@/hooks/useToast";
 import { Spinner } from "@/components/Spinner";
-import { redirect } from "next/navigation";
 import { NEXT_PUBLIC_FORGOT_PASSWORD_ENABLED } from "@/lib/constants";
+import { forgotPassword } from "./utils";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 const ForgotPasswordPage: React.FC = () => {
   const [isWorking, setIsWorking] = useState(false);
+  const { locale } = useLocale();
 
   if (!NEXT_PUBLIC_FORGOT_PASSWORD_ENABLED) {
     redirect("/auth/login");
@@ -27,7 +29,9 @@ const ForgotPasswordPage: React.FC = () => {
     <AuthFlowContainer>
       <div className="flex flex-col w-full justify-center">
         <div className="flex">
-          <Title className="mb-2 mx-auto font-bold">Forgot Password</Title>
+          <Title className="mb-2 mx-auto font-bold">
+            {locale === "ja" ? "パスワードをお忘れですか" : "Forgot Password"}
+          </Title>
         </div>
         {isWorking && <Spinner />}
         <Formik
@@ -35,20 +39,34 @@ const ForgotPasswordPage: React.FC = () => {
             email: "",
           }}
           validationSchema={Yup.object().shape({
-            email: Yup.string().email().required(),
+            email: Yup.string()
+              .email(
+                locale === "ja"
+                  ? "有効なメールアドレスを入力してください"
+                  : "Please enter a valid email address"
+              )
+              .required(
+                locale === "ja"
+                  ? "メールアドレスを入力してください"
+                  : "Email is required"
+              ),
           })}
           onSubmit={async (values) => {
             setIsWorking(true);
             try {
               await forgotPassword(values.email);
               toast.success(
-                "Password reset email sent. Please check your inbox."
+                locale === "ja"
+                  ? "パスワード再設定メールを送信しました。受信トレイをご確認ください。"
+                  : "Password reset email sent. Please check your inbox."
               );
             } catch (error) {
               const errorMessage =
                 error instanceof Error
                   ? error.message
-                  : "An error occurred. Please try again.";
+                  : locale === "ja"
+                    ? "エラーが発生しました。もう一度お試しください。"
+                    : "An error occurred. Please try again.";
               toast.error(errorMessage);
             } finally {
               setIsWorking(false);
@@ -59,14 +77,14 @@ const ForgotPasswordPage: React.FC = () => {
             <Form className="w-full flex flex-col items-stretch mt-2">
               <TextFormField
                 name="email"
-                label="Email"
+                label={t(locale, "auth.email")}
                 type="email"
                 placeholder="email@yourcompany.com"
               />
 
               <div className="flex">
                 <Button disabled={isSubmitting} type="submit" width="full">
-                  Reset Password
+                  {t(locale, "auth.resetPassword")}
                 </Button>
               </div>
             </Form>
@@ -75,7 +93,9 @@ const ForgotPasswordPage: React.FC = () => {
         <Spacer rem={1} />
         <div className="flex">
           <div className="mx-auto">
-            <Text as="p">{markdown("[Back to Login](/auth/login)")}</Text>
+            <Text as="p">
+              {markdown(`[${t(locale, "auth.backToLogin")}](/auth/login)`)}
+            </Text>
           </div>
         </div>
       </div>
diff --git a/web/src/app/auth/login/EmailPasswordForm.tsx b/web/src/app/auth/login/EmailPasswordForm.tsx
index 460fa3aa9e..d92c60597a 100644
--- a/web/src/app/auth/login/EmailPasswordForm.tsx
+++ b/web/src/app/auth/login/EmailPasswordForm.tsx
@@ -1,24 +1,25 @@
 "use client";
 
-import { toast } from "@/hooks/useToast";
-import { basicLogin, basicSignup } from "@/lib/user";
-import { Button } from "@opal/components";
+import { useMemo, useState } from "react";
+import Link from "next/link";
 import { Form, Formik } from "formik";
 import * as Yup from "yup";
+
+import { toast } from "@/hooks/useToast";
+import { basicLogin, basicSignup } from "@/lib/user";
 import { requestEmailVerification } from "../lib";
-import { useMemo, useState } from "react";
-import { Spinner } from "@/components/Spinner";
-import Link from "next/link";
 import { useUser } from "@/providers/UserProvider";
+import { useCaptcha } from "@/lib/hooks/useCaptcha";
+import { validateInternalRedirect } from "@/lib/auth/redirectValidation";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
+import { Spinner } from "@/components/Spinner";
 import { FormikField } from "@/refresh-components/form/FormikField";
 import { FormField } from "@/refresh-components/form/FormField";
-import { InputTypeIn } from "@opal/components";
 import PasswordInputTypeIn from "@/refresh-components/inputs/PasswordInputTypeIn";
-import { validateInternalRedirect } from "@/lib/auth/redirectValidation";
 import { APIFormFieldState } from "@/refresh-components/form/types";
 import { SvgArrowRightCircle } from "@opal/icons";
-import { useCaptcha } from "@/lib/hooks/useCaptcha";
-import { Spacer } from "@opal/components";
+import { Button, InputTypeIn, Spacer } from "@opal/components";
 
 interface EmailPasswordFormProps {
   isSignup?: boolean;
@@ -29,6 +30,32 @@ interface EmailPasswordFormProps {
   isJoin?: boolean;
 }
 
+function getRequestError(locale: string) {
+  return locale === "ja" ? "不明なエラー" : "Unknown error";
+}
+
+function mapServerError(locale: string, detail: unknown) {
+  if (detail === "REGISTER_USER_ALREADY_EXISTS") {
+    return locale === "ja"
+      ? "指定したメールアドレスですでにアカウントが存在します。"
+      : "An account already exists with the specified email.";
+  }
+
+  if (detail === "LOGIN_BAD_CREDENTIALS") {
+    return t(locale as "en" | "ja", "auth.invalidEmailOrPassword");
+  }
+
+  if (detail === "NO_WEB_LOGIN_AND_HAS_NO_PASSWORD") {
+    return t(locale as "en" | "ja", "auth.createAnAccountToSetPassword");
+  }
+
+  if (typeof detail === "string" && detail) {
+    return detail;
+  }
+
+  return getRequestError(locale);
+}
+
 export default function EmailPasswordForm({
   isSignup = false,
   shouldVerify,
@@ -38,26 +65,44 @@ export default function EmailPasswordForm({
   isJoin = false,
 }: EmailPasswordFormProps) {
   const { user, authTypeMetadata } = useUser();
+  const { locale } = useLocale();
   const passwordMinLength = authTypeMetadata?.passwordMinLength ?? 8;
-  const [isWorking, setIsWorking] = useState<boolean>(false);
+  const [isWorking, setIsWorking] = useState(false);
   const [apiStatus, setApiStatus] = useState<APIFormFieldState>("loading");
   const [showApiMessage, setShowApiMessage] = useState(false);
-  const [errorMessage, setErrorMessage] = useState<string>("");
+  const [errorMessage, setErrorMessage] = useState("");
   const { getCaptchaToken } = useCaptcha();
 
   const apiMessages = useMemo(
     () => ({
       loading: isSignup
         ? isJoin
-          ? "Joining..."
-          : "Creating account..."
-        : "Signing in...",
+          ? t(locale, "auth.joining")
+          : t(locale, "auth.creatingAccount")
+        : t(locale, "auth.signingIn"),
       success: isSignup
-        ? "Account created. Signing in..."
-        : "Signed in successfully.",
+        ? t(locale, "auth.accountCreatedSigningIn")
+        : t(locale, "auth.signedInSuccessfully"),
       error: errorMessage,
     }),
-    [isSignup, isJoin, errorMessage]
+    [errorMessage, isJoin, isSignup, locale]
+  );
+
+  const validationSchema = useMemo(
+    () =>
+      Yup.object().shape({
+        email: Yup.string()
+          .email(locale === "ja" ? "有効なメールアドレスを入力してください" : "Please enter a valid email address")
+          .required(locale === "ja" ? "メールアドレスを入力してください" : "Email is required")
+          .transform((value) => value.toLowerCase()),
+        password: Yup.string()
+          .min(
+            passwordMinLength,
+            t(locale, "auth.passwordTooShort", { min: passwordMinLength })
+          )
+          .required(locale === "ja" ? "パスワードを入力してください" : "Password is required"),
+      }),
+    [locale, passwordMinLength]
   );
 
   return (
@@ -69,34 +114,18 @@ export default function EmailPasswordForm({
           email: defaultEmail ? defaultEmail.toLowerCase() : "",
           password: "",
         }}
-        validateOnChange={true}
-        validateOnBlur={true}
-        validationSchema={Yup.object().shape({
-          email: Yup.string()
-            .email()
-            .required()
-            .transform((value) => value.toLowerCase()),
-          password: Yup.string()
-            .min(
-              passwordMinLength,
-              `Password must be at least ${passwordMinLength} characters`
-            )
-            .required(),
-        })}
-        onSubmit={async (values: { email: string; password: string }) => {
-          // Ensure email is lowercase
-          const email: string = values.email.toLowerCase();
+        validateOnChange
+        validateOnBlur
+        validationSchema={validationSchema}
+        onSubmit={async (values) => {
+          const email = values.email.toLowerCase();
           setShowApiMessage(true);
           setApiStatus("loading");
           setErrorMessage("");
 
           if (isSignup) {
-            // login is fast, no need to show a spinner
             setIsWorking(true);
-
-            // Get captcha token for signup (if captcha is enabled)
             const captchaToken = await getCaptchaToken("signup");
-
             const response = await basicSignup(
               email,
               values.password,
@@ -107,27 +136,34 @@ export default function EmailPasswordForm({
             if (!response.ok) {
               setIsWorking(false);
 
-              const errorBody: any = await response.json();
-              const errorDetail = errorBody.detail;
-              let errorMsg: string = "Unknown error";
-              if (errorDetail === "REGISTER_USER_ALREADY_EXISTS") {
-                errorMsg =
-                  "An account already exists with the specified email.";
-              } else if (typeof errorDetail === "string" && errorDetail) {
-                errorMsg = errorDetail;
-              }
+              const errorBody = await response.json().catch(() => ({}));
+              const errorDetail = errorBody?.detail;
+              let errorMsg = mapServerError(locale, errorDetail);
+
               if (response.status === 429) {
-                errorMsg = "Too many requests. Please try again later.";
+                errorMsg =
+                  locale === "ja"
+                    ? "リクエストが集中しています。しばらくしてから再試行してください。"
+                    : "Too many requests. Please try again later.";
               }
+
               setErrorMessage(errorMsg);
               setApiStatus("error");
-              toast.error(`Failed to sign up - ${errorMsg}`);
+              toast.error(
+                locale === "ja"
+                  ? `サインアップに失敗しました - ${errorMsg}`
+                  : `Failed to sign up - ${errorMsg}`
+              );
               setIsWorking(false);
               return;
-            } else {
-              setApiStatus("success");
-              toast.success("Account created successfully. Please log in.");
             }
+
+            setApiStatus("success");
+            toast.success(
+              locale === "ja"
+                ? "アカウントを作成しました。ログインしてください。"
+                : "Account created successfully. Please log in."
+            );
           }
 
           const loginCaptchaToken = await getCaptchaToken("login");
@@ -136,138 +172,140 @@ export default function EmailPasswordForm({
             values.password,
             loginCaptchaToken
           );
+
           if (loginResponse.ok) {
             setApiStatus("success");
+
             if (isSignup && shouldVerify) {
               await requestEmailVerification(email);
-              // Use window.location.href to force a full page reload,
-              // ensuring app re-initializes with the new state (including
-              // server-side provider values)
               window.location.href = "/auth/waiting-on-verification";
-            } else {
-              // The searchparam is purely for multi tenant developement purposes.
-              // It replicates the behavior of the case where a user
-              // has signed up with email / password as the only user to an instance
-              // and has just completed verification
-              const validatedNextUrl = validateInternalRedirect(nextUrl);
-              window.location.href = validatedNextUrl
-                ? validatedNextUrl
-                : `/app${isSignup && !isJoin ? "?new_team=true" : ""}`;
-            }
-          } else {
-            setIsWorking(false);
-            const errorDetail: any = (await loginResponse.json()).detail;
-            let errorMsg: string = "Unknown error";
-            if (errorDetail === "LOGIN_BAD_CREDENTIALS") {
-              errorMsg = "Invalid email or password";
-            } else if (errorDetail === "NO_WEB_LOGIN_AND_HAS_NO_PASSWORD") {
-              errorMsg = "Create an account to set a password";
-            } else if (typeof errorDetail === "string") {
-              errorMsg = errorDetail;
-            }
-            if (loginResponse.status === 429) {
-              errorMsg = "Too many requests. Please try again later.";
+              return;
             }
-            setErrorMessage(errorMsg);
-            setApiStatus("error");
-            toast.error(`Failed to login - ${errorMsg}`);
+
+            const validatedNextUrl = validateInternalRedirect(nextUrl);
+            window.location.href = validatedNextUrl
+              ? validatedNextUrl
+              : `/app${isSignup && !isJoin ? "?new_team=true" : ""}`;
+            return;
+          }
+
+          setIsWorking(false);
+          const loginBody = await loginResponse.json().catch(() => ({}));
+          const errorDetail = loginBody?.detail;
+          let errorMsg = mapServerError(locale, errorDetail);
+
+          if (loginResponse.status === 429) {
+            errorMsg =
+              locale === "ja"
+                ? "リクエストが集中しています。しばらくしてから再試行してください。"
+                : "Too many requests. Please try again later.";
           }
+
+          setErrorMessage(errorMsg);
+          setApiStatus("error");
+          toast.error(
+            locale === "ja"
+              ? `ログインに失敗しました - ${errorMsg}`
+              : `Failed to login - ${errorMsg}`
+          );
         }}
       >
-        {({ isSubmitting, isValid, dirty, values }) => {
-          return (
-            <Form className="gap-y-3">
-              <FormikField<string>
-                name="email"
-                render={(field, helper, meta, state) => (
-                  <FormField name="email" state={state} className="w-full">
-                    <FormField.Label>Email Address</FormField.Label>
-                    <FormField.Control>
-                      <InputTypeIn
-                        {...field}
-                        onChange={(e) => {
-                          if (showApiMessage && apiStatus === "error") {
-                            setShowApiMessage(false);
-                            setErrorMessage("");
-                            setApiStatus("loading");
-                          }
-                          field.onChange(e);
-                        }}
-                        placeholder="email@yourcompany.com"
-                        data-testid="email"
-                        autoComplete="username"
-                        variant={apiStatus === "error" ? "error" : undefined}
-                      />
-                    </FormField.Control>
-                  </FormField>
-                )}
-              />
+        {({ isSubmitting, isValid, dirty }) => (
+          <Form className="gap-y-3">
+            <FormikField<string>
+              name="email"
+              render={(field, _helper, fieldMeta, state) => (
+                <FormField name="email" state={state} className="w-full">
+                  <FormField.Label>{t(locale, "auth.emailAddress")}</FormField.Label>
+                  <FormField.Control>
+                    <InputTypeIn
+                      {...field}
+                      onChange={(event) => {
+                        if (showApiMessage && apiStatus === "error") {
+                          setShowApiMessage(false);
+                          setErrorMessage("");
+                          setApiStatus("loading");
+                        }
+                        field.onChange(event);
+                      }}
+                      placeholder="email@yourcompany.com"
+                      data-testid="email"
+                      autoComplete="username"
+                      variant={apiStatus === "error" ? "error" : undefined}
+                    />
+                  </FormField.Control>
+                </FormField>
+              )}
+            />
 
-              <FormikField<string>
-                name="password"
-                render={(field, helper, meta, state) => (
-                  <FormField name="password" state={state} className="w-full">
-                    <FormField.Label>Password</FormField.Label>
-                    <FormField.Control>
-                      <PasswordInputTypeIn
-                        {...field}
-                        onChange={(e) => {
-                          if (showApiMessage && apiStatus === "error") {
-                            setShowApiMessage(false);
-                            setErrorMessage("");
-                            setApiStatus("loading");
-                          }
-                          field.onChange(e);
-                        }}
-                        placeholder="●●●●●●●●●●●●●●"
-                        data-testid="password"
-                        autoComplete={
-                          isSignup ? "new-password" : "current-password"
+            <FormikField<string>
+              name="password"
+              render={(field, _helper, fieldMeta, state) => (
+                <FormField name="password" state={state} className="w-full">
+                  <FormField.Label>{t(locale, "auth.password")}</FormField.Label>
+                  <FormField.Control>
+                    <PasswordInputTypeIn
+                      {...field}
+                      onChange={(event) => {
+                        if (showApiMessage && apiStatus === "error") {
+                          setShowApiMessage(false);
+                          setErrorMessage("");
+                          setApiStatus("loading");
                         }
-                        error={apiStatus === "error"}
-                      />
-                    </FormField.Control>
-                    {isSignup && !showApiMessage && (
-                      <FormField.Message
-                        messages={{
-                          idle: `Password must be at least ${passwordMinLength} characters`,
-                          error: meta.error,
-                          success: `Password must be at least ${passwordMinLength} characters`,
-                        }}
-                      />
-                    )}
-                    {showApiMessage && (
-                      <FormField.APIMessage
-                        state={apiStatus}
-                        messages={apiMessages}
-                      />
-                    )}
-                  </FormField>
-                )}
-              />
+                        field.onChange(event);
+                      }}
+                      placeholder="••••••••"
+                      data-testid="password"
+                      autoComplete={isSignup ? "new-password" : "current-password"}
+                      error={apiStatus === "error"}
+                    />
+                  </FormField.Control>
+                  {isSignup && !showApiMessage && (
+                    <FormField.Message
+                      messages={{
+                        idle: t(locale, "auth.passwordTooShort", {
+                          min: passwordMinLength,
+                        }),
+                        error: fieldMeta.error,
+                        success: t(locale, "auth.passwordTooShort", {
+                          min: passwordMinLength,
+                        }),
+                      }}
+                    />
+                  )}
+                  {showApiMessage && (
+                    <FormField.APIMessage state={apiStatus} messages={apiMessages} />
+                  )}
+                </FormField>
+              )}
+            />
+
+            <Spacer rem={0.25} />
+            <Button
+              disabled={isSubmitting || !isValid || !dirty}
+              type="submit"
+              width="full"
+              rightIcon={SvgArrowRightCircle}
+            >
+              {isJoin
+                ? t(locale, "auth.join")
+                : isSignup
+                  ? t(locale, "auth.createAccount")
+                  : t(locale, "auth.signIn")}
+            </Button>
 
-              <Spacer rem={0.25} />
-              <Button
-                disabled={isSubmitting || !isValid || !dirty}
-                type="submit"
-                width="full"
-                rightIcon={SvgArrowRightCircle}
+            {user?.is_anonymous_user && (
+              <Link
+                href="/app"
+                className="text-xs text-action-link-05 cursor-pointer text-center w-full font-medium mx-auto"
               >
-                {isJoin ? "Join" : isSignup ? "Create Account" : "Sign In"}
-              </Button>
-              {user?.is_anonymous_user && (
-                <Link
-                  href="/app"
-                  className="text-xs text-action-link-05 cursor-pointer text-center w-full font-medium mx-auto"
-                >
-                  <span className="hover:border-b hover:border-dotted hover:border-action-link-05">
-                    or continue as guest
-                  </span>
-                </Link>
-              )}
-            </Form>
-          );
-        }}
+                <span className="hover:border-b hover:border-dotted hover:border-action-link-05">
+                  {t(locale, "auth.continueAsGuest")}
+                </span>
+              </Link>
+            )}
+          </Form>
+        )}
       </Formik>
     </>
   );
diff --git a/web/src/app/auth/login/LoginPage.tsx b/web/src/app/auth/login/LoginPage.tsx
index 8f91f95697..a7c552cccd 100644
--- a/web/src/app/auth/login/LoginPage.tsx
+++ b/web/src/app/auth/login/LoginPage.tsx
@@ -8,6 +8,8 @@ import { AuthType, NEXT_PUBLIC_FORGOT_PASSWORD_ENABLED } from "@/lib/constants";
 import { useSendAuthRequiredMessage } from "@/lib/extension/utils";
 import Text from "@/refresh-components/texts/Text";
 import { Button, MessageCard } from "@opal/components";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 interface LoginPageProps {
   authUrl: string | null;
@@ -27,8 +29,8 @@ export default function LoginPage({
   isFirstUser,
 }: LoginPageProps) {
   useSendAuthRequiredMessage();
+  const { locale } = useLocale();
 
-  // Honor any existing nextUrl; only default to new team flow for first users with no nextUrl
   const effectiveNextUrl =
     nextUrl ?? (isFirstUser ? "/app?new_team=true" : null);
 
@@ -37,19 +39,18 @@ export default function LoginPage({
       {verified && (
         <MessageCard
           variant="success"
-          title="Your email has been verified! Please sign in to continue."
+          title={t(locale, "auth.accountVerified")}
         />
       )}
       {authUrl &&
         authTypeMetadata &&
         authTypeMetadata.authType !== AuthType.CLOUD &&
-        // basic auth is handled below w/ the EmailPasswordForm
         authTypeMetadata.authType !== AuthType.BASIC && (
           <div className="flex flex-col w-full gap-4">
             <LoginText />
             <SignInButton
               authorizeUrl={authUrl}
-              authType={authTypeMetadata?.authType}
+              authType={authTypeMetadata.authType}
             />
           </div>
         )}
@@ -61,12 +62,12 @@ export default function LoginPage({
             <>
               <SignInButton
                 authorizeUrl={authUrl}
-                authType={authTypeMetadata?.authType}
+                authType={authTypeMetadata.authType}
               />
               <div className="flex flex-row items-center w-full gap-2">
                 <div className="flex-1 border-t border-text-01" />
                 <Text as="p" text03 mainUiMuted>
-                  or
+                  {t(locale, "common.or")}
                 </Text>
                 <div className="flex-1 border-t border-text-01" />
               </div>
@@ -74,7 +75,9 @@ export default function LoginPage({
           )}
           <EmailPasswordForm shouldVerify={true} nextUrl={effectiveNextUrl} />
           {NEXT_PUBLIC_FORGOT_PASSWORD_ENABLED && (
-            <Button href="/auth/forgot-password">Reset Password</Button>
+            <Button href="/auth/forgot-password">
+              {t(locale, "auth.resetPassword")}
+            </Button>
           )}
         </div>
       )}
@@ -88,7 +91,7 @@ export default function LoginPage({
 
       {!hidePageRedirect && (
         <p className="text-center mt-4">
-          Don&apos;t have an account?{" "}
+          {locale === "ja" ? "アカウントをお持ちでない場合は " : "Don&apos;t have an account? "}
           <span
             onClick={() => {
               if (typeof window !== "undefined" && window.top) {
@@ -99,7 +102,7 @@ export default function LoginPage({
             }}
             className="text-link font-medium cursor-pointer"
           >
-            Create an account
+            {t(locale, "auth.createAccount")}
           </span>
         </p>
       )}
diff --git a/web/src/app/auth/login/LoginText.tsx b/web/src/app/auth/login/LoginText.tsx
index 83561d361c..03925ceeae 100644
--- a/web/src/app/auth/login/LoginText.tsx
+++ b/web/src/app/auth/login/LoginText.tsx
@@ -3,17 +3,20 @@
 import React, { useContext } from "react";
 import { SettingsContext } from "@/providers/SettingsProvider";
 import Text from "@/refresh-components/texts/Text";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export default function LoginText() {
   const settings = useContext(SettingsContext);
+  const { locale, t } = useLocale();
+  const appName =
+    (settings && settings?.enterpriseSettings?.application_name) || "Onyx";
   return (
     <div className="w-full flex flex-col ">
       <Text as="p" headingH2 text05>
-        Welcome to{" "}
-        {(settings && settings?.enterpriseSettings?.application_name) || "Onyx"}
+        {t(locale, "auth.welcomeTo", { name: appName })}
       </Text>
       <Text as="p" text03 mainUiMuted>
-        Your open source AI platform for work
+        {t(locale, "auth.openSourcePlatform")}
       </Text>
     </div>
   );
diff --git a/web/src/app/auth/login/SignInButton.tsx b/web/src/app/auth/login/SignInButton.tsx
index e187dd7cae..aec1664f43 100644
--- a/web/src/app/auth/login/SignInButton.tsx
+++ b/web/src/app/auth/login/SignInButton.tsx
@@ -1,29 +1,3 @@
-/**
- * SignInButton — renders the SSO / OAuth sign-in button on the login page.
- *
- * When reCAPTCHA is enabled for this deployment (NEXT_PUBLIC_RECAPTCHA_SITE_KEY
- * set at build time), the Google/OIDC/SAML OAuth click is intercepted to
- * (1) fetch a reCAPTCHA v3 token for the "oauth" action, (2) POST it to
- * /api/auth/captcha/oauth-verify which sets a signed HttpOnly cookie on the
- * response, and (3) then navigate to the authorize URL. The cookie is sent
- * automatically on the subsequent Google redirect back to our callback,
- * where the backend middleware verifies it.
- *
- * IMPORTANT: This component is rendered as part of the /auth/login page, which
- * is used in healthcheck and monitoring flows that issue headless (non-browser)
- * requests (e.g. `curl`). During server-side rendering of those requests,
- * browser-only globals like `window`, `document`, `navigator`, etc. are NOT
- * available. Even though this file is marked "use client", Next.js still
- * executes the component body on the server during SSR — only hooks like
- * `useEffect` are skipped.
- *
- * Do NOT reference `window` or other browser APIs in the render path of this
- * component. If you need browser globals, gate them behind `useEffect` or
- * `typeof window !== "undefined"` checks inside callbacks/effects — but be
- * aware that Turbopack may optimise away bare `typeof window` guards in the
- * SSR bundle, so prefer `useEffect` for safety.
- */
-
 "use client";
 
 import { useState } from "react";
@@ -33,6 +7,7 @@ import { FcGoogle } from "react-icons/fc";
 import type { IconProps } from "@opal/types";
 import { useCaptcha } from "@/lib/hooks/useCaptcha";
 import Text from "@/refresh-components/texts/Text";
+import { useLocale } from "@/providers/LocaleProvider";
 
 interface SignInButtonProps {
   authorizeUrl: string;
@@ -43,6 +18,7 @@ export default function SignInButton({
   authorizeUrl,
   authType,
 }: SignInButtonProps) {
+  const { locale, t } = useLocale();
   const { getCaptchaToken, isCaptchaEnabled } = useCaptcha();
   const [isVerifying, setIsVerifying] = useState(false);
   const [error, setError] = useState<string | null>(null);
@@ -51,12 +27,12 @@ export default function SignInButton({
   let icon: React.FunctionComponent<IconProps> | undefined;
 
   if (authType === AuthType.GOOGLE_OAUTH || authType === AuthType.CLOUD) {
-    button = "Continue with Google";
+    button = t(locale, "auth.continueWithGoogle");
     icon = FcGoogle;
   } else if (authType === AuthType.OIDC) {
-    button = "Continue with OIDC SSO";
+    button = t(locale, "auth.continueWithOidc");
   } else if (authType === AuthType.SAML) {
-    button = "Continue with SAML SSO";
+    button = t(locale, "auth.continueWithSaml");
   }
 
   if (!button) {
@@ -66,12 +42,12 @@ export default function SignInButton({
   async function handleClick(e: React.MouseEvent) {
     e.preventDefault();
     if (isVerifying) return;
+
     setIsVerifying(true);
     setError(null);
-    // Stays true on the success branch so the button remains disabled until
-    // the browser actually begins unloading for the OAuth redirect — prevents
-    // a double-click window between `window.location.href = ...` and unload.
+
     let navigating = false;
+
     try {
       const token = await getCaptchaToken("oauth");
       if (!token) {
@@ -79,15 +55,21 @@ export default function SignInButton({
         console.error(
           "Captcha: grecaptcha.execute returned no token. The widget may not have loaded yet."
         );
-        setError("grecaptcha.execute returned no token");
+        setError(
+          locale === "ja"
+            ? "reCAPTCHA トークンを取得できませんでした。ページを更新して再試行してください。"
+            : "grecaptcha.execute returned no token"
+        );
         return;
       }
+
       const res = await fetch("/api/auth/captcha/oauth-verify", {
         method: "POST",
         credentials: "include",
         headers: { "Content-Type": "application/json" },
         body: JSON.stringify({ token }),
       });
+
       if (!res.ok) {
         const body = await res.json().catch(() => ({}));
         // eslint-disable-next-line no-console
@@ -97,25 +79,30 @@ export default function SignInButton({
           }`
         );
         setError(
-          "Captcha verification failed. Please refresh your browser and try again."
+          locale === "ja"
+            ? "reCAPTCHA の検証に失敗しました。ブラウザを更新して再試行してください。"
+            : "Captcha verification failed. Please refresh your browser and try again."
         );
         return;
       }
+
       navigating = true;
       window.location.href = authorizeUrl;
     } catch (exc) {
       // eslint-disable-next-line no-console
       console.error("Captcha verify request failed", exc);
-      setError(exc instanceof Error ? exc.message : String(exc));
+      setError(
+        exc instanceof Error
+          ? exc.message
+          : locale === "ja"
+            ? "認証の処理中にエラーが発生しました。"
+            : String(exc)
+      );
     } finally {
       if (!navigating) setIsVerifying(false);
     }
   }
 
-  // Only the Google OAuth callback is gated by CaptchaCookieMiddleware on the
-  // backend. OIDC/SAML callbacks have no cookie requirement, so running the
-  // reCAPTCHA interception for them is wasted friction — and worse, a failed
-  // captcha would block the sign-in entirely.
   const intercepted =
     isCaptchaEnabled &&
     (authType === AuthType.GOOGLE_OAUTH || authType === AuthType.CLOUD);
diff --git a/web/src/app/auth/login/page.tsx b/web/src/app/auth/login/page.tsx
index 8538fbb070..030ab9dbfe 100644
--- a/web/src/app/auth/login/page.tsx
+++ b/web/src/app/auth/login/page.tsx
@@ -10,6 +10,8 @@ import type { Route } from "next";
 import AuthFlowContainer from "@/components/auth/AuthFlowContainer";
 import LoginPage from "./LoginPage";
 import { AuthType } from "@/lib/constants";
+import { cookies, headers } from "next/headers";
+import { detectLocale, t } from "@/lib/i18n";
 
 export interface PageProps {
   searchParams?: Promise<{ [key: string]: string | string[] | undefined }>;
@@ -17,6 +19,12 @@ export interface PageProps {
 
 export default async function Page(props: PageProps) {
   const searchParams = await props.searchParams;
+  const cookieStore = await cookies();
+  const headerStore = await headers();
+  const locale = detectLocale(
+    headerStore.get("accept-language"),
+    cookieStore.get("onyx-locale")?.value
+  );
   const autoRedirectDisabled = searchParams?.disableAutoRedirect === "true";
   const autoRedirectToSignupDisabled =
     searchParams?.autoRedirectToSignup === "false";
@@ -87,7 +95,7 @@ export default async function Page(props: PageProps) {
     (authTypeMetadata.authType === AuthType.GOOGLE_OAUTH ||
       authTypeMetadata.authType === AuthType.OIDC ||
       authTypeMetadata.authType === AuthType.SAML) ? (
-      <>Need access? Reach out to your IT admin to get access.</>
+      <>{t(locale, "auth.needAccess")}</>
     ) : undefined;
 
   return (
diff --git a/web/src/app/auth/signup/ReferralSourceSelector.tsx b/web/src/app/auth/signup/ReferralSourceSelector.tsx
index 79f044bb8e..a956f72154 100644
--- a/web/src/app/auth/signup/ReferralSourceSelector.tsx
+++ b/web/src/app/auth/signup/ReferralSourceSelector.tsx
@@ -1,8 +1,11 @@
 "use client";
 
 import { useState } from "react";
+
 import InputSelect from "@/refresh-components/inputs/InputSelect";
 import { Label } from "@/components/Field";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 interface ReferralSourceSelectorProps {
   defaultValue?: string;
@@ -11,20 +14,21 @@ interface ReferralSourceSelectorProps {
 export default function ReferralSourceSelector({
   defaultValue,
 }: ReferralSourceSelectorProps) {
+  const { locale } = useLocale();
   const [referralSource, setReferralSource] = useState(defaultValue);
 
   const referralOptions = [
-    { value: "search", label: "Search Engine (Google/Bing)" },
-    { value: "friend", label: "Friend/Colleague" },
-    { value: "linkedin", label: "LinkedIn" },
-    { value: "twitter", label: "Twitter" },
-    { value: "hackernews", label: "HackerNews" },
-    { value: "reddit", label: "Reddit" },
-    { value: "youtube", label: "YouTube" },
-    { value: "podcast", label: "Podcast" },
-    { value: "blog", label: "Article/Blog" },
-    { value: "ads", label: "Advertisements" },
-    { value: "other", label: "Other" },
+    { value: "search", label: t(locale, "referral.search") },
+    { value: "friend", label: t(locale, "referral.friend") },
+    { value: "linkedin", label: t(locale, "referral.linkedin") },
+    { value: "twitter", label: t(locale, "referral.twitter") },
+    { value: "hackernews", label: t(locale, "referral.hackernews") },
+    { value: "reddit", label: t(locale, "referral.reddit") },
+    { value: "youtube", label: t(locale, "referral.youtube") },
+    { value: "podcast", label: t(locale, "referral.podcast") },
+    { value: "blog", label: t(locale, "referral.blog") },
+    { value: "ads", label: t(locale, "referral.ads") },
+    { value: "other", label: t(locale, "referral.other") },
   ];
 
   const handleChange = (value: string) => {
@@ -40,10 +44,10 @@ export default function ReferralSourceSelector({
   return (
     <div className="w-full gap-y-2 flex flex-col">
       <Label className="text-text-950" small={false}>
-        How did you hear about us?
+        {t(locale, "referral.heardAboutUs")}
       </Label>
       <InputSelect value={referralSource} onValueChange={handleChange}>
-        <InputSelect.Trigger placeholder="Select an option" />
+        <InputSelect.Trigger placeholder={t(locale, "referral.selectOption")} />
 
         <InputSelect.Content>
           {referralOptions.map((option) => (
diff --git a/web/src/app/auth/signup/page.tsx b/web/src/app/auth/signup/page.tsx
index a92fe8880b..7e06a4b5df 100644
--- a/web/src/app/auth/signup/page.tsx
+++ b/web/src/app/auth/signup/page.tsx
@@ -1,3 +1,6 @@
+import { cookies, headers } from "next/headers";
+import { redirect } from "next/navigation";
+
 import { User } from "@/lib/types";
 import {
   getCurrentUserSS,
@@ -5,7 +8,6 @@ import {
   AuthTypeMetadata,
   getAuthUrlSS,
 } from "@/lib/userSS";
-import { redirect } from "next/navigation";
 import EmailPasswordForm from "../login/EmailPasswordForm";
 import SignInButton from "@/app/auth/login/SignInButton";
 import AuthFlowContainer from "@/components/auth/AuthFlowContainer";
@@ -14,11 +16,19 @@ import AuthErrorDisplay from "@/components/auth/AuthErrorDisplay";
 import Text from "@/refresh-components/texts/Text";
 import { cn } from "@opal/utils";
 import { AuthType } from "@/lib/constants";
+import { detectLocale, t } from "@/lib/i18n";
 
 const Page = async (props: {
   searchParams?: Promise<{ [key: string]: string | string[] | undefined }>;
 }) => {
   const searchParams = await props.searchParams;
+  const cookieStore = await cookies();
+  const headerStore = await headers();
+  const locale = detectLocale(
+    headerStore.get("accept-language"),
+    cookieStore.get("onyx-locale")?.value
+  );
+
   const nextUrl = Array.isArray(searchParams?.next)
     ? searchParams?.next[0]
     : searchParams?.next || null;
@@ -27,9 +37,6 @@ const Page = async (props: {
     ? searchParams?.email[0]
     : searchParams?.email || null;
 
-  // catch cases where the backend is completely unreachable here
-  // without try / catch, will just raise an exception and the page
-  // will not render
   let authTypeMetadata: AuthTypeMetadata | null = null;
   let currentUser: User | null = null;
   try {
@@ -41,16 +48,15 @@ const Page = async (props: {
     console.log(`Some fetch failed for the login page - ${e}`);
   }
 
-  // if user is already logged in, take them to the main app page
   if (currentUser && currentUser.is_active && !currentUser.is_anonymous_user) {
     if (!authTypeMetadata?.requiresVerification || currentUser.is_verified) {
       return redirect("/app");
     }
     return redirect("/auth/waiting-on-verification");
   }
+
   const cloud = authTypeMetadata?.authType === AuthType.CLOUD;
 
-  // only enable this page if basic login is enabled
   if (authTypeMetadata?.authType !== AuthType.BASIC && !cloud) {
     return redirect("/app");
   }
@@ -74,10 +80,12 @@ const Page = async (props: {
         >
           <div className="w-full">
             <Text as="p" headingH2 text05>
-              {cloud ? "Complete your sign up" : "Create account"}
+              {cloud
+                ? t(locale, "auth.completeSignUp")
+                : t(locale, "auth.createAccountTitle")}
             </Text>
             <Text as="p" text03>
-              Get started with Onyx
+              {t(locale, "auth.getStartedWithOnyx")}
             </Text>
           </div>
           {cloud && authUrl && (
@@ -86,7 +94,7 @@ const Page = async (props: {
               <div className="flex items-center w-full my-4">
                 <div className="grow border-t border-border-01" />
                 <Text as="p" mainUiMuted text03 className="mx-2">
-                  or
+                  {t(locale, "common.or")}
                 </Text>
                 <div className="grow border-t border-border-01" />
               </div>
@@ -94,11 +102,9 @@ const Page = async (props: {
           )}
 
           {cloud && (
-            <>
-              <div className="w-full flex flex-col mb-3">
-                <ReferralSourceSelector />
-              </div>
-            </>
+            <div className="w-full flex flex-col mb-3">
+              <ReferralSourceSelector />
+            </div>
           )}
 
           <EmailPasswordForm
diff --git a/web/src/app/auth/verify-email/Verify.tsx b/web/src/app/auth/verify-email/Verify.tsx
index 5864657963..a8bb76a6b1 100644
--- a/web/src/app/auth/verify-email/Verify.tsx
+++ b/web/src/app/auth/verify-email/Verify.tsx
@@ -2,12 +2,13 @@
 
 import { useSearchParams } from "next/navigation";
 import { useCallback, useEffect, useState } from "react";
-import { Text } from "@opal/components";
-import { Spacer } from "@opal/components";
+import { Text, Spacer } from "@opal/components";
+
 import { RequestNewVerificationEmail } from "../waiting-on-verification/RequestNewVerificationEmail";
 import { User } from "@/lib/types";
 import Logo from "@/refresh-components/Logo";
 import { NEXT_PUBLIC_CLOUD_ENABLED } from "@/lib/constants";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export interface VerifyProps {
   user: User | null;
@@ -15,7 +16,7 @@ export interface VerifyProps {
 
 export default function Verify({ user }: VerifyProps) {
   const searchParams = useSearchParams();
-
+  const { locale } = useLocale();
   const [error, setError] = useState("");
 
   const verify = useCallback(async () => {
@@ -24,7 +25,9 @@ export default function Verify({ user }: VerifyProps) {
       searchParams?.get("first_user") === "true" && NEXT_PUBLIC_CLOUD_ENABLED;
     if (!token) {
       setError(
-        "Missing verification token. Try requesting a new verification email."
+        locale === "ja"
+          ? "確認トークンが見つかりません。新しい確認メールを依頼してください。"
+          : "Missing verification token. Try requesting a new verification email."
       );
       return;
     }
@@ -38,8 +41,6 @@ export default function Verify({ user }: VerifyProps) {
     });
 
     if (response.ok) {
-      // Redirect to login page instead of /app so user can log in
-      // from any browser (not dependent on the original signup session)
       const loginUrl = firstUser
         ? "/auth/login?verified=true&first_user=true"
         : "/auth/login?verified=true";
@@ -52,10 +53,12 @@ export default function Verify({ user }: VerifyProps) {
         console.error("Failed to parse verification error response:", e);
       }
       setError(
-        `Failed to verify your email - ${errorDetail}. Please try requesting a new verification email.`
+        locale === "ja"
+          ? `メールアドレスの確認に失敗しました - ${errorDetail}。新しい確認メールを依頼して再試行してください。`
+          : `Failed to verify your email - ${errorDetail}. Please try requesting a new verification email.`
       );
     }
-  }, [searchParams]);
+  }, [locale, searchParams]);
 
   useEffect(() => {
     verify();
@@ -68,7 +71,9 @@ export default function Verify({ user }: VerifyProps) {
         {!error ? (
           <>
             <Spacer rem={0.5} />
-            <Text as="p">Verifying your email...</Text>
+            <Text as="p">
+              {locale === "ja" ? "メールアドレスを確認中..." : "Verifying your email..."}
+            </Text>
           </>
         ) : (
           <div>
@@ -78,9 +83,10 @@ export default function Verify({ user }: VerifyProps) {
             {user && (
               <div className="text-center">
                 <RequestNewVerificationEmail email={user.email}>
-                  {/* TODO(@raunakab): migrate to @opal/components Text */}
                   <p className="text-sm mt-2 text-link">
-                    Get new verification email
+                    {locale === "ja"
+                      ? "新しい確認メールを取得"
+                      : "Get new verification email"}
                   </p>
                 </RequestNewVerificationEmail>
               </div>
diff --git a/web/src/app/auth/waiting-on-verification/RequestNewVerificationEmail.tsx b/web/src/app/auth/waiting-on-verification/RequestNewVerificationEmail.tsx
index 8160122d87..c778fd2ca7 100644
--- a/web/src/app/auth/waiting-on-verification/RequestNewVerificationEmail.tsx
+++ b/web/src/app/auth/waiting-on-verification/RequestNewVerificationEmail.tsx
@@ -1,9 +1,12 @@
 "use client";
 
+import { useState, type JSX } from "react";
+
 import { toast } from "@/hooks/useToast";
 import { requestEmailVerification } from "../lib";
 import { Spinner } from "@/components/Spinner";
-import { useState, JSX } from "react";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 export function RequestNewVerificationEmail({
   children,
@@ -14,6 +17,7 @@ export function RequestNewVerificationEmail({
 }) {
   const [isRequestingVerification, setIsRequestingVerification] =
     useState(false);
+  const { locale } = useLocale();
 
   return (
     <button
@@ -24,11 +28,11 @@ export function RequestNewVerificationEmail({
         setIsRequestingVerification(false);
 
         if (response.ok) {
-          toast.success("A new verification email has been sent!");
+          toast.success(t(locale, "auth.verificationEmailSent"));
         } else {
           const errorDetail = (await response.json()).detail;
           toast.error(
-            `Failed to send a new verification email - ${errorDetail}`
+            t(locale, "auth.verificationEmailFailed", { detail: errorDetail })
           );
         }
       }}
diff --git a/web/src/app/auth/waiting-on-verification/page.tsx b/web/src/app/auth/waiting-on-verification/page.tsx
index 00c59d6456..2a4eda8f21 100644
--- a/web/src/app/auth/waiting-on-verification/page.tsx
+++ b/web/src/app/auth/waiting-on-verification/page.tsx
@@ -1,19 +1,26 @@
+import { cookies, headers } from "next/headers";
+import { redirect } from "next/navigation";
+
 import {
   AuthTypeMetadata,
   getAuthTypeMetadataSS,
   getCurrentUserSS,
 } from "@/lib/userSS";
-import { redirect } from "next/navigation";
 import { User } from "@/lib/types";
 import { RequestNewVerificationEmail } from "./RequestNewVerificationEmail";
 import Logo from "@/refresh-components/Logo";
 import { Text } from "@opal/components";
 import { markdown } from "@opal/utils";
+import { detectLocale, t } from "@/lib/i18n";
 
 export default async function Page() {
-  // catch cases where the backend is completely unreachable here
-  // without try / catch, will just raise an exception and the page
-  // will not render
+  const cookieStore = await cookies();
+  const headerStore = await headers();
+  const locale = detectLocale(
+    headerStore.get("accept-language"),
+    cookieStore.get("onyx-locale")?.value
+  );
+
   let authTypeMetadata: AuthTypeMetadata | null = null;
   let currentUser: User | null = null;
   try {
@@ -40,15 +47,25 @@ export default async function Page() {
         <div className="flex flex-col gap-2">
           <Text as="span">
             {markdown(
-              `Hey, *${currentUser.email}*, it looks like you haven't verified your email yet.\nCheck your inbox for an email from us to get started!`
+              locale === "ja"
+                ? `こんにちは、*${currentUser.email}* さん。まだメールアドレスの確認が完了していないようです。\n開始するには、こちらから届いたメールをご確認ください。`
+                : `Hey, *${currentUser.email}*, it looks like you haven't verified your email yet.\nCheck your inbox for an email from us to get started!`
             )}
           </Text>
           <div className="flex flex-row items-center gap-1">
-            <Text as="span">If you don't see anything, click</Text>
+            <Text as="span">
+              {locale === "ja"
+                ? "もし見当たらない場合は、"
+                : "If you don't see anything, click"}
+            </Text>
             <RequestNewVerificationEmail email={currentUser.email}>
-              <Text as="span">here</Text>
+              <Text as="span">{t(locale, "auth.clickHere")}</Text>
             </RequestNewVerificationEmail>
-            <Text as="span">to request a new email.</Text>
+            <Text as="span">
+              {locale === "ja"
+                ? "をクリックして再送してください。"
+                : "to request a new email."}
+            </Text>
           </div>
         </div>
       </div>
diff --git a/web/src/app/craft/v1/apps/oauth/callback/page.tsx b/web/src/app/craft/v1/apps/oauth/callback/page.tsx
index 434e796db7..f47252f042 100644
--- a/web/src/app/craft/v1/apps/oauth/callback/page.tsx
+++ b/web/src/app/craft/v1/apps/oauth/callback/page.tsx
@@ -7,13 +7,16 @@ import { mutate as globalMutate } from "swr";
 import { SettingsLayouts } from "@opal/layouts";
 import { Button, Card, Text } from "@opal/components";
 import { SvgPlug } from "@opal/icons";
+
 import { CRAFT_APPS_PATH } from "@/app/craft/v1/constants";
 import { SWR_KEYS } from "@/lib/swr-keys";
 import { completeExternalAppOAuthCallback } from "@/app/craft/services/externalAppsService";
+import { useLocale } from "@/providers/LocaleProvider";
 
 type Status = "exchanging" | "success" | "error";
 
 export default function ExternalAppsOAuthCallbackPage() {
+  const { locale } = useLocale();
   const router = useRouter();
   const params = useSearchParams();
   const code = params?.get("code") ?? null;
@@ -22,22 +25,29 @@ export default function ExternalAppsOAuthCallbackPage() {
 
   const [status, setStatus] = useState<Status>("exchanging");
   const [errorMessage, setErrorMessage] = useState<string | null>(null);
-
-  // OAuth `code` is single-use — gate against React Strict Mode and
-  // remount-induced double exchanges, which would 400 on the second.
   const hasExchanged = useRef(false);
 
   useEffect(() => {
     if (slackError) {
       setStatus("error");
-      setErrorMessage(`OAuth was cancelled or denied: ${slackError}`);
+      setErrorMessage(
+        locale === "ja"
+          ? `OAuth がキャンセルされたか拒否されました: ${slackError}`
+          : `OAuth was cancelled or denied: ${slackError}`
+      );
       return;
     }
+
     if (!code || !state) {
       setStatus("error");
-      setErrorMessage("Missing code or state in callback URL.");
+      setErrorMessage(
+        locale === "ja"
+          ? "コールバック URL に code または state がありません。"
+          : "Missing code or state in callback URL."
+      );
       return;
     }
+
     if (hasExchanged.current) return;
     hasExchanged.current = true;
 
@@ -54,31 +64,41 @@ export default function ExternalAppsOAuthCallbackPage() {
     }
 
     exchange();
-  }, [code, state, slackError, router]);
+  }, [code, state, slackError, router, locale]);
 
   return (
     <SettingsLayouts.Root width="sm">
       <SettingsLayouts.Header
         icon={SvgPlug}
-        title="Connecting your app"
-        description="Finishing the OAuth handshake…"
+        title={locale === "ja" ? "アプリを接続しています" : "Connecting your app"}
+        description={
+          locale === "ja"
+            ? "OAuth ハンドシェイクを完了しています..."
+            : "Finishing the OAuth handshake..."
+        }
       />
       <SettingsLayouts.Body>
         <Card background="light" border="solid" rounding="lg">
           <div className="flex flex-col gap-2">
             {status === "exchanging" && (
               <Text font="main-content-body">
-                Exchanging authorization code…
+                {locale === "ja"
+                  ? "認可コードを交換しています..."
+                  : "Exchanging authorization code..."}
               </Text>
             )}
             {status === "success" && (
               <Text font="main-content-body">
-                Connected. Redirecting back to your apps…
+                {locale === "ja"
+                  ? "接続が完了しました。アプリ一覧へ戻ります..."
+                  : "Connected. Redirecting back to your apps..."}
               </Text>
             )}
             {status === "error" && (
               <>
-                <Text font="main-content-body">Connection failed.</Text>
+                <Text font="main-content-body">
+                  {locale === "ja" ? "接続に失敗しました。" : "Connection failed."}
+                </Text>
                 {errorMessage && (
                   <Text font="secondary-body" color="text-03">
                     {errorMessage}
@@ -86,7 +106,7 @@ export default function ExternalAppsOAuthCallbackPage() {
                 )}
                 <div className="pt-2">
                   <Button onClick={() => router.push(CRAFT_APPS_PATH as Route)}>
-                    Back to My Apps
+                    {locale === "ja" ? "アプリ一覧に戻る" : "Back to My Apps"}
                   </Button>
                 </div>
               </>
diff --git a/web/src/app/federated/oauth/callback/page.tsx b/web/src/app/federated/oauth/callback/page.tsx
index b297d27618..888f9373c0 100644
--- a/web/src/app/federated/oauth/callback/page.tsx
+++ b/web/src/app/federated/oauth/callback/page.tsx
@@ -1,26 +1,39 @@
 "use client";
 
 import OAuthCallbackPage from "@/components/oauth/OAuthCallbackPage";
-import { getSourceDisplayName } from "@/lib/sources";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 export default function FederatedOAuthCallbackPage() {
+  const { locale } = useLocale();
+
   const federatedConfig = {
-    processingMessage: "Processing...",
-    processingDetails: "Please wait while we complete the setup.",
-    successMessage: "Success!",
+    processingMessage: t(locale, "oauth.processing"),
+    processingDetails: t(locale, "oauth.federatedSetupProcessing"),
+    successMessage: t(locale, "oauth.success"),
     successDetailsTemplate:
-      "Your {serviceName} authorization completed successfully. You can now use this connector for search.",
-    errorMessage: "Something Went Wrong",
-    backButtonText: "Back to Chat",
-    redirectingMessage: "Redirecting to chat in 2 seconds...",
+      locale === "ja"
+        ? "{serviceName} の認証が完了しました。このコネクタを検索で利用できます。"
+        : "Your {serviceName} authorization completed successfully. You can now use this connector for search.",
+    errorMessage: t(locale, "oauth.somethingWentWrong"),
+    backButtonText: t(locale, "oauth.backToChat"),
+    redirectingMessage: t(locale, "oauth.redirectingInSeconds", {
+      seconds: 2,
+    }),
     autoRedirectDelay: 2000,
     defaultRedirectPath: "/app",
     callbackApiUrl: "/api/federated/callback",
     errorMessageMap: {
       "validation errors":
-        "Configuration error - please check your connector settings",
-      client_secret: "Authentication credentials are missing or invalid",
-      oauth: "OAuth authorization failed",
+        locale === "ja"
+          ? "設定エラーです。コネクタ設定を確認してください"
+          : "Configuration error - please check your connector settings",
+      client_secret:
+        locale === "ja"
+          ? "認証情報が不足しているか無効です"
+          : "Authentication credentials are missing or invalid",
+      oauth:
+        locale === "ja" ? "OAuth 認証に失敗しました" : "OAuth authorization failed",
     },
   };
 
diff --git a/web/src/app/global-error.tsx b/web/src/app/global-error.tsx
index 6fbb642c7c..19588b91e7 100644
--- a/web/src/app/global-error.tsx
+++ b/web/src/app/global-error.tsx
@@ -1,8 +1,8 @@
 "use client";
 
 import * as Sentry from "@sentry/nextjs";
-import NextError from "next/error";
-import { useEffect } from "react";
+import { useEffect, useMemo, useState } from "react";
+import { detectLocale, t, type Locale } from "@/lib/i18n";
 
 // This global error page is necessary to capture errors that occur in the app.
 export default function GlobalError({
@@ -10,6 +10,26 @@ export default function GlobalError({
 }: {
   error: Error & { digest?: string };
 }) {
+  const [locale, setLocale] = useState<Locale>("en");
+
+  useEffect(() => {
+    const stored = window.localStorage.getItem("onyx-locale");
+    const cookieLocale = document.cookie
+      .split("; ")
+      .find((entry) => entry.startsWith("onyx-locale="))
+      ?.split("=")[1];
+    setLocale(detectLocale(window.navigator.language, cookieLocale ?? stored));
+  }, []);
+
+  const copy = useMemo(
+    () => ({
+      title: t(locale, "error.title"),
+      description: t(locale, "error.description"),
+      reload: t(locale, "error.reload"),
+    }),
+    [locale]
+  );
+
   useEffect(() => {
     if (process.env.NEXT_PUBLIC_SENTRY_DSN) {
       Sentry.captureException(error);
@@ -17,12 +37,28 @@ export default function GlobalError({
   }, [error]);
 
   return (
-    <html>
+    <html lang={locale}>
       <body>
-        {/* NextError require  a `statusCode` prop. However, since the App Router
-        does not expose status codes for errors, we simply pass 0 to render a
-        generic error message. */}
-        <NextError statusCode={0} />
+        <main className="flex min-h-screen items-center justify-center bg-background px-6 text-text">
+          <div className="max-w-xl rounded-2xl border border-border bg-surface p-8 shadow-lg">
+            <p className="mb-2 text-sm font-medium text-status-warning-02">
+              {copy.title}
+            </p>
+            <h1 className="mb-4 text-2xl font-semibold">
+              {copy.title}
+            </h1>
+            <p className="mb-6 text-sm leading-6 text-text-secondary">
+              {copy.description}
+            </p>
+            <button
+              type="button"
+              className="rounded-lg bg-action-primary px-4 py-2 text-sm font-medium text-white"
+              onClick={() => window.location.reload()}
+            >
+              {copy.reload}
+            </button>
+          </div>
+        </main>
       </body>
     </html>
   );
diff --git a/web/src/app/layout.tsx b/web/src/app/layout.tsx
index d99822e512..654731ad2d 100644
--- a/web/src/app/layout.tsx
+++ b/web/src/app/layout.tsx
@@ -19,6 +19,9 @@ import LicenseExpiryBanner from "@/sections/LicenseExpiryBanner";
 import CustomAnalyticsScript from "@/providers/CustomAnalyticsScript";
 import ProductGatingWrapper from "@/providers/ProductGatingWrapper";
 import SWRConfigProvider from "@/providers/SWRConfigProvider";
+import { cookies, headers } from "next/headers";
+import { detectLocale } from "@/lib/i18n";
+import { LocaleProvider } from "@/providers/LocaleProvider";
 
 const hankenGrotesk = Hanken_Grotesk({
   subsets: ["latin"],
@@ -60,14 +63,20 @@ export const metadata: Metadata = {
 // all data is fetched client-side via SWR in the provider tree.
 export const dynamic = "force-dynamic";
 
-export default function RootLayout({
+export default async function RootLayout({
   children,
 }: {
   children: React.ReactNode;
 }) {
+  const cookieStore = await cookies();
+  const headerStore = await headers();
+  const cookieLocale = cookieStore.get("onyx-locale")?.value ?? null;
+  const acceptLanguage = headerStore.get("accept-language");
+  const locale = detectLocale(acceptLanguage, cookieLocale);
+
   return (
     <html
-      lang="en"
+      lang={locale}
       className={cn(hankenGrotesk.variable, dmMono.variable)}
       suppressHydrationWarning
     >
@@ -104,24 +113,26 @@ export default function RootLayout({
           <div className="text-text min-h-screen bg-background">
             <TooltipProvider>
               <PHProvider>
-                <SWRConfigProvider>
-                  <AppHealthBanner />
-                  <LicenseExpiryBanner />
-                  <AppProvider>
-                    <DynamicMetadata />
-                    <CustomAnalyticsScript />
-                    <Suspense fallback={null}>
-                      <PostHogPageView />
-                    </Suspense>
-                    <div id={MODAL_ROOT_ID} className="h-screen w-screen">
-                      <ProductGatingWrapper>{children}</ProductGatingWrapper>
-                    </div>
-                    {process.env.NEXT_PUBLIC_POSTHOG_KEY && <WebVitals />}
-                    {process.env.NEXT_PUBLIC_ENABLE_STATS === "true" && (
-                      <StatsOverlayLoader />
-                    )}
-                  </AppProvider>
-                </SWRConfigProvider>
+                <LocaleProvider initialLocale={locale}>
+                  <SWRConfigProvider>
+                    <AppHealthBanner />
+                    <LicenseExpiryBanner />
+                    <AppProvider>
+                      <DynamicMetadata />
+                      <CustomAnalyticsScript />
+                      <Suspense fallback={null}>
+                        <PostHogPageView />
+                      </Suspense>
+                      <div id={MODAL_ROOT_ID} className="h-screen w-screen">
+                        <ProductGatingWrapper>{children}</ProductGatingWrapper>
+                      </div>
+                      {process.env.NEXT_PUBLIC_POSTHOG_KEY && <WebVitals />}
+                      {process.env.NEXT_PUBLIC_ENABLE_STATS === "true" && (
+                        <StatsOverlayLoader />
+                      )}
+                    </AppProvider>
+                  </SWRConfigProvider>
+                </LocaleProvider>
               </PHProvider>
             </TooltipProvider>
           </div>
diff --git a/web/src/app/mcp/oauth/callback/page.tsx b/web/src/app/mcp/oauth/callback/page.tsx
index 892e816912..14050bd990 100644
--- a/web/src/app/mcp/oauth/callback/page.tsx
+++ b/web/src/app/mcp/oauth/callback/page.tsx
@@ -1,25 +1,43 @@
 "use client";
 
 import OAuthCallbackPage from "@/components/oauth/OAuthCallbackPage";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 export default function MCPOAuthCallbackPage() {
+  const { locale } = useLocale();
+
   const mcpConfig = {
-    processingMessage: "Processing...",
-    processingDetails: "Please wait while we complete the MCP server setup.",
-    successMessage: "Success!",
+    processingMessage: t(locale, "oauth.processing"),
+    processingDetails: t(locale, "oauth.mcpSetupProcessing"),
+    successMessage: t(locale, "oauth.success"),
     successDetailsTemplate:
-      "Your {serviceName} authorization completed successfully. You can now use this server's tools in chat.",
-    errorMessage: "Something Went Wrong",
-    backButtonText: "Back to Chat",
-    redirectingMessage: "Redirecting back in 2 seconds...",
+      locale === "ja"
+        ? "{serviceName} の認証が完了しました。このサーバーのツールをチャットで利用できます。"
+        : "Your {serviceName} authorization completed successfully. You can now use this server's tools in chat.",
+    errorMessage: t(locale, "oauth.somethingWentWrong"),
+    backButtonText: t(locale, "oauth.backToChat"),
+    redirectingMessage: t(locale, "oauth.redirectingInSeconds", {
+      seconds: 2,
+    }),
     autoRedirectDelay: 2000,
     defaultRedirectPath: "/app",
     callbackApiUrl: "/api/mcp/oauth/callback",
     errorMessageMap: {
-      "server not found": "MCP server configuration not found",
-      credentials: "Authentication credentials are invalid",
-      oauth: "OAuth authorization failed",
-      validation: "Could not validate connection to MCP server",
+      "server not found":
+        locale === "ja"
+          ? "MCP サーバー設定が見つかりません"
+          : "MCP server configuration not found",
+      credentials:
+        locale === "ja"
+          ? "認証情報が無効です"
+          : "Authentication credentials are invalid",
+      oauth:
+        locale === "ja" ? "OAuth 認証に失敗しました" : "OAuth authorization failed",
+      validation:
+        locale === "ja"
+          ? "MCP サーバーへの接続を検証できませんでした"
+          : "Could not validate connection to MCP server",
     },
   };
 
diff --git a/web/src/app/oauth-config/callback/page.tsx b/web/src/app/oauth-config/callback/page.tsx
index 835139c680..0a14e410d3 100644
--- a/web/src/app/oauth-config/callback/page.tsx
+++ b/web/src/app/oauth-config/callback/page.tsx
@@ -1,19 +1,25 @@
+"use client";
+
 import OAuthCallbackPage from "@/components/oauth/OAuthCallbackPage";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 export default function OAuthConfigCallbackPage() {
+  const { locale } = useLocale();
+
   return (
     <OAuthCallbackPage
       config={{
         callbackApiUrl: "/api/oauth-config/callback",
         defaultRedirectPath: "/app",
-        processingMessage: "Completing Authorization...",
-        processingDetails:
-          "Please wait while we securely store your credentials.",
-        successMessage: "Authorization Successful!",
-        successDetailsTemplate:
-          "You have successfully authorized the tool to access your {serviceName} account.",
-        errorMessage: "Authorization Failed",
-        backButtonText: "Back to Chat",
+        processingMessage: t(locale, "oauth.callbackCompletingAuthorization"),
+        processingDetails: t(locale, "oauth.callbackStoringCredentials"),
+        successMessage: t(locale, "oauth.callbackAuthorizationSuccessful"),
+        successDetailsTemplate: t(locale, "oauth.callbackAuthorizedTool", {
+          serviceName: "{serviceName}",
+        }),
+        errorMessage: t(locale, "oauth.callbackAuthorizationFailed"),
+        backButtonText: t(locale, "oauth.backToChat"),
         autoRedirectDelay: 2000,
       }}
     />
diff --git a/web/src/components/LanguageSwitcher.tsx b/web/src/components/LanguageSwitcher.tsx
new file mode 100644
index 0000000000..9596a9c8d5
--- /dev/null
+++ b/web/src/components/LanguageSwitcher.tsx
@@ -0,0 +1,34 @@
+"use client";
+
+import { useLocale } from "@/providers/LocaleProvider";
+
+export default function LanguageSwitcher() {
+  const { locale, setLocale } = useLocale();
+
+  return (
+    <div className="flex gap-2">
+      <button
+        type="button"
+        className={`rounded-lg px-3 py-1 text-sm font-medium ${
+          locale === "ja"
+            ? "bg-action-primary text-white"
+            : "border border-border bg-surface text-text"
+        }`}
+        onClick={() => setLocale("ja")}
+      >
+        日本語
+      </button>
+      <button
+        type="button"
+        className={`rounded-lg px-3 py-1 text-sm font-medium ${
+          locale === "en"
+            ? "bg-action-primary text-white"
+            : "border border-border bg-surface text-text"
+        }`}
+        onClick={() => setLocale("en")}
+      >
+        English
+      </button>
+    </div>
+  );
+}
diff --git a/web/src/components/auth/AuthFlowContainer.tsx b/web/src/components/auth/AuthFlowContainer.tsx
index 2296d7711d..e215139471 100644
--- a/web/src/components/auth/AuthFlowContainer.tsx
+++ b/web/src/components/auth/AuthFlowContainer.tsx
@@ -1,5 +1,7 @@
 import Link from "next/link";
 import { SvgOnyxLogo } from "@opal/logos";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 export default function AuthFlowContainer({
   children,
@@ -10,6 +12,7 @@ export default function AuthFlowContainer({
   authState?: "signup" | "login" | "join";
   footerContent?: React.ReactNode;
 }) {
+  const { locale } = useLocale();
   return (
     <div className="p-4 flex flex-col items-center justify-center min-h-screen bg-background">
       <div className="w-full max-w-md flex items-start flex-col bg-background-tint-00 rounded-16 shadow-lg shadow-02 p-6">
@@ -20,12 +23,12 @@ export default function AuthFlowContainer({
         <div className="text-sm mt-6 text-center w-full text-text-03 mainUiBody mx-auto">
           {footerContent ?? (
             <>
-              New to Onyx?{" "}
+              {locale === "ja" ? "Onyx を初めて使いますか？" : "New to Onyx? "}{" "}
               <Link
                 href="/auth/signup"
                 className="text-text-05 mainUiAction underline transition-colors duration-200"
               >
-                Create an Account
+                {t(locale, "auth.createAccount")}
               </Link>
             </>
           )}
@@ -33,12 +36,12 @@ export default function AuthFlowContainer({
       )}
       {authState === "signup" && (
         <div className="text-sm mt-6 text-center w-full text-text-03 mainUiBody mx-auto">
-          Already have an account?{" "}
+          {locale === "ja" ? "すでにアカウントがありますか？" : "Already have an account? "}{" "}
           <Link
             href="/auth/login?autoRedirectToSignup=false"
             className="text-text-05 mainUiAction underline transition-colors duration-200"
           >
-            Sign In
+            {t(locale, "auth.signIn")}
           </Link>
         </div>
       )}
diff --git a/web/src/components/errorPages/ErrorPage.tsx b/web/src/components/errorPages/ErrorPage.tsx
index 28d376efcb..4622e1b308 100644
--- a/web/src/components/errorPages/ErrorPage.tsx
+++ b/web/src/components/errorPages/ErrorPage.tsx
@@ -1,48 +1,51 @@
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 import ErrorPageLayout from "@/components/errorPages/ErrorPageLayout";
 import Text from "@/refresh-components/texts/Text";
 import { DOCS_BASE_URL } from "@/lib/constants";
 import { SvgAlertCircle } from "@opal/icons";
 
 export default function Error() {
+  const { locale } = useLocale();
   return (
     <ErrorPageLayout>
       <div className="flex flex-row items-center gap-2">
         <Text as="p" headingH2>
-          We encountered an issue
+          {t(locale, "error.title")}
         </Text>
         <SvgAlertCircle className="w-6 h-6 stroke-text-04" />
       </div>
 
       <Text as="p" text03>
-        It seems there was a problem loading your Onyx settings. This could be
-        due to a configuration issue or incomplete setup.
+        {t(locale, "health.backendUnavailableBody")}
       </Text>
 
       <Text as="p" text03>
-        If you&apos;re an admin, please review our{" "}
+        {locale === "ja" ? "管理者の方は、" : "If you're an admin, please review our "}
         <a
           className="text-action-link-05"
           href={`${DOCS_BASE_URL}?utm_source=app&utm_medium=error_page&utm_campaign=config_error`}
           target="_blank"
           rel="noopener noreferrer"
         >
-          documentation
+          {locale === "ja" ? "ドキュメント" : "documentation"}
         </a>{" "}
-        for proper configuration steps. If you&apos;re a user, please contact
-        your admin for assistance.
+        {locale === "ja"
+          ? "を確認してください。一般ユーザーの方は管理者にお問い合わせください。"
+          : "for proper configuration steps. If you're a user, please contact your admin for assistance."}
       </Text>
 
       <Text as="p" text03>
-        Need help? Join our{" "}
+        {locale === "ja" ? "サポートが必要ですか？" : "Need help? Join our "}
         <a
           className="text-action-link-05"
           href="https://discord.gg/4NA5SbzrWb"
           target="_blank"
           rel="noopener noreferrer"
         >
-          Discord community
+          {locale === "ja" ? "Discord コミュニティ" : "Discord community"}
         </a>{" "}
-        for support.
+        {locale === "ja" ? "でサポートを受けられます。" : "for support."}
       </Text>
     </ErrorPageLayout>
   );
diff --git a/web/src/components/oauth/OAuthCallbackPage.tsx b/web/src/components/oauth/OAuthCallbackPage.tsx
index a912a433f3..83b8ae9b2f 100644
--- a/web/src/components/oauth/OAuthCallbackPage.tsx
+++ b/web/src/components/oauth/OAuthCallbackPage.tsx
@@ -6,6 +6,8 @@ import type { Route } from "next";
 import { SvgCheck, SvgAlertTriangle } from "@opal/icons";
 import CardSection from "@/components/admin/CardSection";
 import { Button } from "@opal/components";
+import { useLocale } from "@/providers/LocaleProvider";
+import { t } from "@/lib/i18n";
 
 interface OAuthCallbackConfig {
   // UI customization
@@ -35,12 +37,13 @@ interface OAuthCallbackPageProps {
 export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
   const router = useRouter();
   const searchParams = useSearchParams();
+  const { locale } = useLocale();
 
   const [statusMessage, setStatusMessage] = useState(
-    config.processingMessage || "Processing..."
+    config.processingMessage || t(locale, "oauth.processing")
   );
   const [statusDetails, setStatusDetails] = useState(
-    config.processingDetails || "Please wait while we complete the setup."
+    config.processingDetails || t(locale, "oauth.processingDetails")
   );
   const [isError, setIsError] = useState(false);
   const [isSuccess, setIsSuccess] = useState(false);
@@ -96,10 +99,11 @@ export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
     const handleOAuthCallback = async () => {
       // Handle OAuth error from provider
       if (error) {
-        setStatusMessage(config.errorMessage || "Authorization Failed");
+        setStatusMessage(
+          config.errorMessage || t(locale, "oauth.authorizationFailed")
+        );
         setStatusDetails(
-          errorDescription ||
-            "The authorization was cancelled or failed. Please try again."
+          errorDescription || t(locale, "oauth.authorizationCancelled")
         );
         setIsError(true);
         setIsLoading(false);
@@ -108,10 +112,8 @@ export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
 
       // Validate required parameters
       if (!code || !state) {
-        setStatusMessage("Invalid Request");
-        setStatusDetails(
-          "The authorization request was incomplete. Please try again."
-        );
+        setStatusMessage(t(locale, "oauth.invalidRequest"));
+        setStatusDetails(t(locale, "oauth.incompleteAuthorizationRequest"));
         setIsError(true);
         setIsLoading(false);
         return;
@@ -132,7 +134,7 @@ export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
         });
 
         if (!response.ok) {
-          let errorMessage = "Failed to complete authorization";
+          let errorMessage = t(locale, "oauth.failedToCompleteAuthorization");
           try {
             const errorData = await response.json();
             if (errorData.detail && config.errorMessageMap) {
@@ -179,16 +181,16 @@ export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
         const redirectUrl = new URL(sanitizedPath, window.location.origin);
         redirectUrl.searchParams.set("message", "oauth_connected");
         setRedirectPath(redirectUrl.pathname + redirectUrl.search);
-        setStatusMessage(config.successMessage || "Success!");
+        setStatusMessage(config.successMessage || t(locale, "oauth.success"));
 
         const successDetails = config.successDetailsTemplate
           ? config.successDetailsTemplate.replace(
               "{serviceName}",
               result.serviceName || "service"
             )
-          : `Your ${
-              result.serviceName || "service"
-            } authorization completed successfully.`;
+          : t(locale, "oauth.callbackAuthorizedService", {
+              serviceName: result.serviceName || "service",
+            });
 
         setStatusDetails(successDetails);
         setIsSuccess(true);
@@ -196,11 +198,13 @@ export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
         setIsLoading(false);
       } catch (error) {
         console.error("OAuth callback error:", error);
-        setStatusMessage(config.errorMessage || "Something Went Wrong");
+        setStatusMessage(
+          config.errorMessage || t(locale, "oauth.somethingWentWrong")
+        );
         setStatusDetails(
           error instanceof Error
             ? error.message
-            : "An error occurred during the OAuth process. Please try again."
+            : t(locale, "oauth.oauthProcessError")
         );
         setIsError(true);
         setIsLoading(false);
@@ -210,7 +214,7 @@ export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
     handleOAuthCallback();
     // Run once; the single-use callback must not re-fire (see hasProcessedRef).
     // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, []);
+  }, [code, config, error, errorDescription, locale, router, searchParams, state]);
 
   const getStatusIcon = () => {
     if (isLoading) {
@@ -261,8 +265,10 @@ export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
             {isSuccess && secondsLeft !== null && (
               <div className="bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 rounded-lg p-4 mb-6">
                 <p className="text-green-800 dark:text-green-200 text-sm">
-                  Redirecting in {secondsLeft}{" "}
-                  {secondsLeft === 1 ? "second" : "seconds"}...
+                  {config.redirectingMessage ||
+                    t(locale, "oauth.redirectingInSeconds", {
+                      seconds: secondsLeft,
+                    })}
                 </p>
               </div>
             )}
@@ -278,14 +284,14 @@ export default function OAuthCallbackPage({ config }: OAuthCallbackPageProps) {
                     }}
                     width="full"
                   >
-                    {config.backButtonText || "Back to Chat"}
+                    {config.backButtonText || t(locale, "oauth.backToChat")}
                   </Button>
                 </div>
               )}
 
               {isLoading && (
                 <p className="text-sm text-gray-500 dark:text-gray-400">
-                  This may take a few moments...
+                  {t(locale, "oauth.takeMoments")}
                 </p>
               )}
             </div>
diff --git a/web/src/lib/admin-routes.ts b/web/src/lib/admin-routes.ts
index b63b9b9c51..79f161b1e3 100644
--- a/web/src/lib/admin-routes.ts
+++ b/web/src/lib/admin-routes.ts
@@ -50,176 +50,176 @@ export const ADMIN_ROUTES = {
   INDEXING_STATUS: {
     path: "/admin/indexing/status",
     icon: SvgBookOpen,
-    title: "Existing Connectors",
-    sidebarLabel: "Existing Connectors",
+    title: "既存のコネクタ",
+    sidebarLabel: "既存のコネクタ",
   },
   ADD_CONNECTOR: {
     path: "/admin/add-connector",
     icon: SvgUploadCloud,
-    title: "Add Connector",
-    sidebarLabel: "Add Connector",
+    title: "コネクタを追加",
+    sidebarLabel: "コネクタを追加",
   },
   DOCUMENT_SETS: {
     path: "/admin/documents/sets",
     icon: SvgFiles,
-    title: "Document Sets",
-    sidebarLabel: "Document Sets",
+    title: "ドキュメントセット",
+    sidebarLabel: "ドキュメントセット",
   },
   DOCUMENT_EXPLORER: {
     path: "/admin/documents/explorer",
     icon: SvgZoomIn,
-    title: "Document Explorer",
-    sidebarLabel: "Explorer",
+    title: "ドキュメントエクスプローラー",
+    sidebarLabel: "エクスプローラー",
   },
   DOCUMENT_FEEDBACK: {
     path: "/admin/documents/feedback",
     icon: SvgThumbsUp,
-    title: "Document Feedback",
-    sidebarLabel: "Feedback",
+    title: "ドキュメントフィードバック",
+    sidebarLabel: "フィードバック",
   },
   AGENTS: {
     path: "/admin/agents",
     icon: SvgOnyxOctagon,
-    title: "Agents",
-    sidebarLabel: "Agents",
+    title: "エージェント",
+    sidebarLabel: "エージェント",
   },
   SLACK_BOTS: {
     path: "/admin/bots",
     icon: SvgSlack,
-    title: "Slack Integration",
-    sidebarLabel: "Slack Integration",
+    title: "Slack 連携",
+    sidebarLabel: "Slack 連携",
   },
   DISCORD_BOTS: {
     path: "/admin/discord-bot",
     icon: SvgDiscord,
-    title: "Discord Integration",
-    sidebarLabel: "Discord Integration",
+    title: "Discord 連携",
+    sidebarLabel: "Discord 連携",
   },
   MCP_ACTIONS: {
     path: "/admin/actions/mcp",
     icon: SvgMcp,
-    title: "MCP Actions",
-    sidebarLabel: "MCP Actions",
+    title: "MCP アクション",
+    sidebarLabel: "MCP アクション",
   },
   OPENAPI_ACTIONS: {
     path: "/admin/actions/open-api",
     icon: SvgActions,
-    title: "OpenAPI Actions",
-    sidebarLabel: "OpenAPI Actions",
+    title: "OpenAPI アクション",
+    sidebarLabel: "OpenAPI アクション",
   },
   STANDARD_ANSWERS: {
     path: "/admin/standard-answer",
     icon: SvgClipboard,
-    title: "Standard Answers",
-    sidebarLabel: "Standard Answers",
+    title: "標準回答",
+    sidebarLabel: "標準回答",
   },
   GROUPS: {
     path: "/admin/groups",
     icon: SvgUsers,
-    title: "Manage User Groups",
-    sidebarLabel: "Groups",
+    title: "ユーザーグループ管理",
+    sidebarLabel: "グループ",
   },
   CHAT_PREFERENCES: {
     path: "/admin/configuration/chat-preferences",
     icon: SvgBubbleText,
-    title: "Chat Preferences",
-    sidebarLabel: "Chat Preferences",
+    title: "チャット設定",
+    sidebarLabel: "チャット設定",
   },
   LLM_MODELS: {
     path: "/admin/configuration/language-models",
     icon: SvgCpu,
-    title: "Language Models",
-    sidebarLabel: "Language Models",
+    title: "言語モデル",
+    sidebarLabel: "言語モデル",
   },
   WEB_SEARCH: {
     path: "/admin/configuration/web-search",
     icon: SvgGlobe,
-    title: "Web Search",
-    sidebarLabel: "Web Search",
+    title: "Web 検索",
+    sidebarLabel: "Web 検索",
   },
   IMAGE_GENERATION: {
     path: "/admin/configuration/image-generation",
     icon: SvgImage,
-    title: "Image Generation",
-    sidebarLabel: "Image Generation",
+    title: "画像生成",
+    sidebarLabel: "画像生成",
   },
   VOICE: {
     path: "/admin/configuration/voice",
     icon: SvgAudio,
-    title: "Voice",
-    sidebarLabel: "Voice",
+    title: "音声",
+    sidebarLabel: "音声",
   },
   CODE_INTERPRETER: {
     path: "/admin/configuration/code-interpreter",
     icon: SvgTerminal,
-    title: "Code Interpreter",
-    sidebarLabel: "Code Interpreter",
+    title: "コードインタープリター",
+    sidebarLabel: "コードインタープリター",
   },
   INDEX_SETTINGS: {
     path: "/admin/configuration/index-settings",
     icon: SvgSearchMenu,
-    title: "Index Settings",
-    sidebarLabel: "Index Settings",
+    title: "インデックス設定",
+    sidebarLabel: "インデックス設定",
   },
   DOCUMENT_PROCESSING: {
     path: "/admin/configuration/document-processing",
     icon: SvgFileText,
-    title: "Document Processing",
-    sidebarLabel: "Document Processing",
+    title: "ドキュメント処理",
+    sidebarLabel: "ドキュメント処理",
   },
   USERS: {
     path: "/admin/users",
     icon: SvgUser,
-    title: "Users & Requests",
-    sidebarLabel: "Users",
+    title: "ユーザーとリクエスト",
+    sidebarLabel: "ユーザー",
   },
   API_KEYS: {
     path: "/admin/service-accounts",
     icon: SvgUserKey,
-    title: "Service Accounts",
-    sidebarLabel: "Service Accounts",
+    title: "サービスアカウント",
+    sidebarLabel: "サービスアカウント",
   },
   TOKEN_RATE_LIMITS: {
     path: "/admin/token-rate-limits",
     icon: SvgProgressBars,
-    title: "Spending Limits",
-    sidebarLabel: "Spending Limits",
+    title: "利用上限",
+    sidebarLabel: "利用上限",
   },
   USAGE: {
     path: "/admin/performance/usage",
     icon: SvgActivity,
-    title: "Usage Statistics",
-    sidebarLabel: "Usage Statistics",
+    title: "利用統計",
+    sidebarLabel: "利用統計",
   },
   QUERY_HISTORY: {
     path: "/admin/performance/query-history",
     icon: SvgHistory,
-    title: "Query History",
-    sidebarLabel: "Query History",
+    title: "クエリ履歴",
+    sidebarLabel: "クエリ履歴",
   },
   CUSTOM_ANALYTICS: {
     path: "/admin/performance/custom-analytics",
     icon: SvgBarChart,
-    title: "Custom Analytics",
-    sidebarLabel: "Custom Analytics",
+    title: "カスタム分析",
+    sidebarLabel: "カスタム分析",
   },
   THEME: {
     path: "/admin/theme",
     icon: SvgPaintBrush,
-    title: "Appearance & Theming",
-    sidebarLabel: "Appearance & Theming",
+    title: "外観とテーマ",
+    sidebarLabel: "外観とテーマ",
   },
   BILLING: {
     path: "/admin/billing",
     icon: SvgWallet,
-    title: "Plans & Billing",
-    sidebarLabel: "Plans & Billing",
+    title: "プランと請求",
+    sidebarLabel: "プランと請求",
   },
   HOOKS: {
     path: "/admin/hooks",
     icon: SvgShareWebhook,
-    title: "Hook Extensions",
-    sidebarLabel: "Hook Extensions",
+    title: "フック拡張",
+    sidebarLabel: "フック拡張",
   },
   SCIM: {
     path: "/admin/scim",
@@ -230,14 +230,14 @@ export const ADMIN_ROUTES = {
   DEBUG: {
     path: "/admin/debug",
     icon: SvgDownload,
-    title: "Debug Logs",
-    sidebarLabel: "Debug Logs",
+    title: "デバッグログ",
+    sidebarLabel: "デバッグログ",
   },
   SECURITY_HARDENING: {
     path: "/admin/security",
     icon: SvgShield,
-    title: "Security and Hardening",
-    sidebarLabel: "Security and Hardening",
+    title: "セキュリティと強化",
+    sidebarLabel: "セキュリティと強化",
   },
   // Prefix-only entries used for layout matching — not rendered as sidebar
   // items or page headers.
diff --git a/web/src/lib/i18n.ts b/web/src/lib/i18n.ts
new file mode 100644
index 0000000000..ffc7ee8254
--- /dev/null
+++ b/web/src/lib/i18n.ts
@@ -0,0 +1,630 @@
+export const locales = ["en", "ja"] as const;
+
+export type Locale = (typeof locales)[number];
+
+type MessageMap = {
+  common: {
+    cancel: string;
+    retry: string;
+    close: string;
+    loading: string;
+    or: string;
+    fold: string;
+  };
+  auth: {
+    loggedOutTitle: string;
+    sessionExpiredBody: string;
+    logIn: string;
+    logOut: string;
+    welcomeTo: string;
+    openSourcePlatform: string;
+    needAccess: string;
+    signIn: string;
+    createAccount: string;
+    continueWithGoogle: string;
+    continueWithOidc: string;
+    continueWithSaml: string;
+    resetPassword: string;
+    backToLogin: string;
+    emailAddress: string;
+    email: string;
+    password: string;
+    accountVerified: string;
+    createNewOrganization: string;
+    accountNotFound: string;
+    accountNotFoundBody: string;
+    accountNotFoundDifferentEmail: string;
+    completeSignUp: string;
+    createAccountTitle: string;
+    getStartedWithOnyx: string;
+    join: string;
+    joining: string;
+    creatingAccount: string;
+    signingIn: string;
+    signedInSuccessfully: string;
+    accountCreatedSigningIn: string;
+    passwordTooShort: string;
+    invalidEmailOrPassword: string;
+    createAnAccountToSetPassword: string;
+    continueAsGuest: string;
+    authenticationError: string;
+    loginAttemptProblem: string;
+    possibleIssues: string;
+    incorrectOrExpiredLoginCredentials: string;
+    temporaryAuthenticationSystemDisruption: string;
+    accountAccessRestrictionsOrPermissions: string;
+    returnToLoginPage: string;
+    contactSupport: string;
+    contactAdministrator: string;
+    verificationEmailSent: string;
+    verificationEmailFailed: string;
+    checkInbox: string;
+    requestNewVerificationEmail: string;
+    noVerificationEmailYet: string;
+    clickHere: string;
+    toRequestNewEmail: string;
+  };
+  health: {
+    backendUnavailableTitle: string;
+    backendUnavailableBody: string;
+  };
+  sidebar: {
+    recents: string;
+    trySendingMessage: string;
+    settings: string;
+    notifications: string;
+    helpFaq: string;
+    agents: string;
+    projects: string;
+    newSession: string;
+    searchChats: string;
+    exploreAgents: string;
+    moreAgents: string;
+    newProject: string;
+    adminPanel: string;
+    curatorPanel: string;
+    failedMoveChat: string;
+    failedLogout: string;
+  };
+  referral: {
+    heardAboutUs: string;
+    selectOption: string;
+    search: string;
+    friend: string;
+    linkedin: string;
+    twitter: string;
+    hackernews: string;
+    reddit: string;
+    youtube: string;
+    podcast: string;
+    blog: string;
+    ads: string;
+    other: string;
+  };
+  chat: {
+    share: string;
+    rename: string;
+    moveToProject: string;
+    removeFromProject: string;
+    removeFromProjectNamed: string;
+    delete: string;
+    deleteChatTitle: string;
+    deleteChatBody: string;
+    createNewProject: string;
+    createProjectNamed: string;
+    searchProjects: string;
+    searchChatsAndProjects: string;
+    noResults: string;
+    noChatsOrProjectsYet: string;
+    noMoreResults: string;
+    recent: string;
+    recentSessions: string;
+    sessions: string;
+    projects: string;
+    showAll: string;
+    open: string;
+    select: string;
+    failedDeleteChat: string;
+    failedCreateProject: string;
+  };
+  tools: {
+    searchTools: string;
+    noToolsAvailable: string;
+    noToolsFound: string;
+    showAllTools: string;
+    showOnlyEnabled: string;
+    showOnlyEnabledTools: string;
+    disableAllTools: string;
+    enableAll: string;
+  };
+  oauth: {
+    processing: string;
+    processingDetails: string;
+    invalidRequest: string;
+    incompleteAuthorizationRequest: string;
+    authorizationFailed: string;
+    authorizationCancelled: string;
+    failedToCompleteAuthorization: string;
+    success: string;
+    somethingWentWrong: string;
+    oauthProcessError: string;
+    takeMoments: string;
+    backToChat: string;
+    redirectingInSeconds: string;
+    callbackCompletingAuthorization: string;
+    callbackStoringCredentials: string;
+    callbackAuthorizationSuccessful: string;
+    callbackAuthorizationFailed: string;
+    callbackAuthorizedService: string;
+    callbackAuthorizedTool: string;
+    mcpSetupProcessing: string;
+    federatedSetupProcessing: string;
+  };
+  license: {
+    expiresOn: string;
+    expiresTomorrow: string;
+    expiredOn: string;
+    graceDaysRemaining: string;
+    renewWithin30Days: string;
+    renewWithin2Weeks: string;
+    renewWithin24Hours: string;
+  };
+  error: {
+    title: string;
+    description: string;
+    reload: string;
+  };
+};
+
+export const messages: Record<Locale, MessageMap> = {
+  en: {
+    common: {
+      cancel: "Cancel",
+      retry: "Retry",
+      close: "Close",
+      loading: "Loading",
+      or: "or",
+      fold: "Fold",
+    },
+    auth: {
+      loggedOutTitle: "You Have Been Logged Out",
+      sessionExpiredBody:
+        "Your session has expired. Please log in again to continue.",
+      logIn: "Log In",
+      logOut: "Log Out",
+      welcomeTo: "Welcome to {name}",
+      openSourcePlatform: "Your open source AI platform for work",
+      needAccess: "Need access? Reach out to your IT admin to get access.",
+      signIn: "Sign In",
+      createAccount: "Create an account",
+      continueWithGoogle: "Continue with Google",
+      continueWithOidc: "Continue with OIDC SSO",
+      continueWithSaml: "Continue with SAML SSO",
+      resetPassword: "Reset Password",
+      backToLogin: "Back to Login",
+      emailAddress: "Email Address",
+      email: "Email",
+      password: "Password",
+      accountVerified: "Your email has been verified! Please sign in to continue.",
+      createNewOrganization: "Create New Organization",
+      accountNotFound: "Account Not Found",
+      accountNotFoundBody:
+        "We couldn't find your account in our records. To access Onyx, you need to either:",
+      accountNotFoundDifferentEmail: "Have an account with a different email?",
+      completeSignUp: "Complete your sign up",
+      createAccountTitle: "Create account",
+      getStartedWithOnyx: "Get started with Onyx",
+      join: "Join",
+      joining: "Joining...",
+      creatingAccount: "Creating account...",
+      signingIn: "Signing in...",
+      signedInSuccessfully: "Signed in successfully.",
+      accountCreatedSigningIn: "Account created. Signing in...",
+      passwordTooShort: "Password must be at least {min} characters",
+      invalidEmailOrPassword: "Invalid email or password",
+      createAnAccountToSetPassword: "Create an account to set a password",
+      continueAsGuest: "or continue as guest",
+      authenticationError: "Authentication Error",
+      loginAttemptProblem: "There was a problem with your login attempt.",
+      possibleIssues: "Possible Issues:",
+      incorrectOrExpiredLoginCredentials: "Incorrect or expired login credentials",
+      temporaryAuthenticationSystemDisruption:
+        "Temporary authentication system disruption",
+      accountAccessRestrictionsOrPermissions:
+        "Account access restrictions or permissions",
+      returnToLoginPage: "Return to Login Page",
+      contactSupport:
+        "If you continue to experience problems, please reach out to the Onyx team at",
+      contactAdministrator:
+        "If you continue to experience problems, please reach out to your system administrator for assistance.",
+      verificationEmailSent: "A new verification email has been sent!",
+      verificationEmailFailed:
+        "Failed to send a new verification email - {detail}",
+      checkInbox: "Check your inbox for an email from us to get started!",
+      requestNewVerificationEmail: "If you don't see anything, click",
+      noVerificationEmailYet: "it looks like you haven't verified your email yet.",
+      clickHere: "here",
+      toRequestNewEmail: "to request a new email.",
+    },
+    health: {
+      backendUnavailableTitle: "The backend is currently unavailable",
+      backendUnavailableBody:
+        "If this is your initial setup or you just updated your Onyx deployment, this is likely because the backend is still starting up. Give it a minute or two, and then refresh the page. If that does not work, make sure the backend is set up and/or contact an administrator.",
+    },
+    sidebar: {
+      recents: "Recents",
+      trySendingMessage:
+        "Try sending a message! Your chat history will appear here.",
+      settings: "Settings",
+      notifications: "Notifications",
+      helpFaq: "Help & FAQ",
+      agents: "Agents",
+      projects: "Projects",
+      newSession: "New Session",
+      searchChats: "Search Chats",
+      exploreAgents: "Explore Agents",
+      moreAgents: "More Agents",
+      newProject: "New Project",
+      adminPanel: "Admin Panel",
+      curatorPanel: "Curator Panel",
+      failedMoveChat: "Failed to move chat. Please try again.",
+      failedLogout: "Failed to logout",
+    },
+    referral: {
+      heardAboutUs: "How did you hear about us?",
+      selectOption: "Select an option",
+      search: "Search Engine (Google / Bing)",
+      friend: "Friend / Colleague",
+      linkedin: "LinkedIn",
+      twitter: "X / Twitter",
+      hackernews: "Hacker News",
+      reddit: "Reddit",
+      youtube: "YouTube",
+      podcast: "Podcast",
+      blog: "Article / Blog",
+      ads: "Advertisements",
+      other: "Other",
+    },
+    chat: {
+      share: "Share",
+      rename: "Rename",
+      moveToProject: "Move to Project",
+      removeFromProject: "Remove from Project",
+      removeFromProjectNamed: "Remove from {name}",
+      delete: "Delete",
+      deleteChatTitle: "Delete Chat",
+      deleteChatBody:
+        "Are you sure you want to delete this chat? This action cannot be undone.",
+      createNewProject: "Create New Project",
+      createProjectNamed: "Create New Project \"{name}\"",
+      searchProjects: "Search Projects",
+      searchChatsAndProjects: "Search chat sessions, projects...",
+      noResults: "No results found",
+      noChatsOrProjectsYet: "No chats or projects yet",
+      noMoreResults: "No more results",
+      recent: "Recent",
+      recentSessions: "Recent Sessions",
+      sessions: "Sessions",
+      projects: "Projects",
+      showAll: "Show all",
+      open: "Open",
+      select: "Select",
+      failedDeleteChat: "Failed to delete chat. Please try again.",
+      failedCreateProject: "Failed to create project. Please try again.",
+    },
+    tools: {
+      searchTools: "Search tools...",
+      noToolsAvailable: "No tools available",
+      noToolsFound: "No tools found",
+      showAllTools: "Show all tools",
+      showOnlyEnabled: "Show only enabled",
+      showOnlyEnabledTools: "Show only enabled tools",
+      disableAllTools: "Disable all tools",
+      enableAll: "Enable all",
+    },
+    oauth: {
+      processing: "Processing...",
+      processingDetails: "Please wait while we complete the setup.",
+      invalidRequest: "Invalid Request",
+      incompleteAuthorizationRequest:
+        "The authorization request was incomplete. Please try again.",
+      authorizationFailed: "Authorization Failed",
+      authorizationCancelled:
+        "The authorization was cancelled or failed. Please try again.",
+      failedToCompleteAuthorization: "Failed to complete authorization",
+      success: "Success!",
+      somethingWentWrong: "Something Went Wrong",
+      oauthProcessError:
+        "An error occurred during the OAuth process. Please try again.",
+      takeMoments: "This may take a few moments...",
+      backToChat: "Back to Chat",
+      redirectingInSeconds: "Redirecting in {seconds} seconds...",
+      callbackCompletingAuthorization: "Completing Authorization...",
+      callbackStoringCredentials:
+        "Please wait while we securely store your credentials.",
+      callbackAuthorizationSuccessful: "Authorization Successful!",
+      callbackAuthorizationFailed: "Authorization Failed",
+      callbackAuthorizedService:
+        "You have successfully authorized the service to access your {serviceName} account.",
+      callbackAuthorizedTool:
+        "You have successfully authorized the tool to access your {serviceName} account.",
+      mcpSetupProcessing: "Please wait while we complete the MCP server setup.",
+      federatedSetupProcessing: "Please wait while we complete the setup.",
+    },
+    license: {
+      expiresOn: "Your Onyx license expires on {date}.",
+      expiresTomorrow: "Your Onyx license expires tomorrow ({date}).",
+      expiredOn: "Your Onyx license expired on {date}.",
+      graceDaysRemaining:
+        "{days} grace day{plural} remaining before access is gated. Renew now.",
+      renewWithin30Days:
+        "Renewal is due in approximately 30 days. Contact your Onyx representative to renew.",
+      renewWithin2Weeks:
+        "Renewal is due in approximately 2 weeks. Complete renewal soon to avoid service interruption.",
+      renewWithin24Hours:
+        "Renewal is due within 24 hours. Renew now to avoid service interruption.",
+    },
+    error: {
+      title: "Something went wrong",
+      description:
+        "An unexpected error occurred while loading Onyx. You can try reloading the page, or return to the home screen and try again.",
+      reload: "Reload page",
+    },
+  },
+  ja: {
+    common: {
+      cancel: "キャンセル",
+      retry: "再試行",
+      close: "閉じる",
+      loading: "読み込み中",
+      or: "または",
+      fold: "折りたたむ",
+    },
+    auth: {
+      loggedOutTitle: "ログアウトしました",
+      sessionExpiredBody:
+        "セッションの有効期限が切れました。続行するには再度ログインしてください。",
+      logIn: "ログイン",
+      logOut: "ログアウト",
+      welcomeTo: "{name} へようこそ",
+      openSourcePlatform: "仕事のためのオープンソース AI プラットフォーム",
+      needAccess: "アクセスが必要な場合は、IT 管理者にご連絡ください。",
+      signIn: "サインイン",
+      createAccount: "アカウントを作成",
+      continueWithGoogle: "Google で続行",
+      continueWithOidc: "OIDC SSO で続行",
+      continueWithSaml: "SAML SSO で続行",
+      resetPassword: "パスワードをリセット",
+      backToLogin: "ログイン画面に戻る",
+      emailAddress: "メールアドレス",
+      email: "メールアドレス",
+      password: "パスワード",
+      accountVerified:
+        "メールアドレスが確認されました。続行するにはサインインしてください。",
+      createNewOrganization: "新しい組織を作成",
+      accountNotFound: "アカウントが見つかりません",
+      accountNotFoundBody:
+        "記録内にアカウントが見つかりませんでした。Onyx を利用するには、次のいずれかが必要です。",
+      accountNotFoundDifferentEmail:
+        "別のメールアドレスでアカウントをお持ちですか。",
+      completeSignUp: "サインアップを完了",
+      createAccountTitle: "アカウントを作成",
+      getStartedWithOnyx: "Onyx を始めましょう",
+      join: "参加",
+      joining: "参加中...",
+      creatingAccount: "アカウントを作成中...",
+      signingIn: "サインイン中...",
+      signedInSuccessfully: "サインインしました。",
+      accountCreatedSigningIn:
+        "アカウントを作成しました。サインインしています...",
+      passwordTooShort: "パスワードは {min} 文字以上にしてください",
+      invalidEmailOrPassword:
+        "メールアドレスまたはパスワードが正しくありません",
+      createAnAccountToSetPassword:
+        "パスワードを設定するにはアカウントを作成してください",
+      continueAsGuest: "またはゲストとして続行",
+      authenticationError: "認証エラー",
+      loginAttemptProblem: "ログイン処理中に問題が発生しました。",
+      possibleIssues: "考えられる原因:",
+      incorrectOrExpiredLoginCredentials:
+        "ログイン情報が正しくない、または期限切れです",
+      temporaryAuthenticationSystemDisruption:
+        "認証システムが一時的に不安定です",
+      accountAccessRestrictionsOrPermissions:
+        "アカウントのアクセス制限または権限の問題",
+      returnToLoginPage: "ログイン画面に戻る",
+      contactSupport:
+        "問題が続く場合は、Onyx チームまでご連絡ください。",
+      contactAdministrator:
+        "問題が続く場合は、システム管理者にお問い合わせください。",
+      verificationEmailSent: "確認メールを再送しました。",
+      verificationEmailFailed: "確認メールの再送に失敗しました - {detail}",
+      checkInbox:
+        "開始するには、受信トレイに届いたメールをご確認ください。",
+      requestNewVerificationEmail: "届いていない場合は、",
+      noVerificationEmailYet:
+        "まだメールアドレスの確認が完了していないようです。",
+      clickHere: "こちら",
+      toRequestNewEmail: "をクリックして新しいメールを依頼してください。",
+    },
+    health: {
+      backendUnavailableTitle: "バックエンドを利用できません",
+      backendUnavailableBody:
+        "初期セットアップ中、または Onyx のデプロイ直後であれば、バックエンドの起動待ちが原因の可能性があります。1、2 分待ってからページを再読み込みしてください。それでも解消しない場合は、バックエンドの設定を確認するか、管理者にお問い合わせください。",
+    },
+    sidebar: {
+      recents: "最近の会話",
+      trySendingMessage:
+        "メッセージを送ってみましょう。チャット履歴がここに表示されます。",
+      settings: "設定",
+      notifications: "通知",
+      helpFaq: "ヘルプと FAQ",
+      agents: "エージェント",
+      projects: "プロジェクト",
+      newSession: "新しいセッション",
+      searchChats: "チャットを検索",
+      exploreAgents: "エージェントを探す",
+      moreAgents: "エージェント一覧",
+      newProject: "新しいプロジェクト",
+      adminPanel: "管理パネル",
+      curatorPanel: "キュレーターパネル",
+      failedMoveChat: "チャットの移動に失敗しました。もう一度お試しください。",
+      failedLogout: "ログアウトに失敗しました",
+    },
+    referral: {
+      heardAboutUs: "Onyx をどこで知りましたか。",
+      selectOption: "選択してください",
+      search: "検索エンジン（Google / Bing）",
+      friend: "友人・同僚",
+      linkedin: "LinkedIn",
+      twitter: "X / Twitter",
+      hackernews: "Hacker News",
+      reddit: "Reddit",
+      youtube: "YouTube",
+      podcast: "Podcast",
+      blog: "記事 / ブログ",
+      ads: "広告",
+      other: "その他",
+    },
+    chat: {
+      share: "共有",
+      rename: "名前を変更",
+      moveToProject: "プロジェクトへ移動",
+      removeFromProject: "プロジェクトから外す",
+      removeFromProjectNamed: "{name} から外す",
+      delete: "削除",
+      deleteChatTitle: "チャットを削除",
+      deleteChatBody:
+        "このチャットを削除してもよろしいですか。この操作は元に戻せません。",
+      createNewProject: "新しいプロジェクトを作成",
+      createProjectNamed: "新しいプロジェクト「{name}」を作成",
+      searchProjects: "プロジェクトを検索",
+      searchChatsAndProjects: "チャットやプロジェクトを検索...",
+      noResults: "結果が見つかりません",
+      noChatsOrProjectsYet: "チャットやプロジェクトはまだありません",
+      noMoreResults: "これ以上結果はありません",
+      recent: "最近",
+      recentSessions: "最近のセッション",
+      sessions: "セッション",
+      projects: "プロジェクト",
+      showAll: "すべて表示",
+      open: "開く",
+      select: "選択",
+      failedDeleteChat:
+        "チャットの削除に失敗しました。もう一度お試しください。",
+      failedCreateProject:
+        "プロジェクトの作成に失敗しました。もう一度お試しください。",
+    },
+    tools: {
+      searchTools: "ツールを検索...",
+      noToolsAvailable: "利用可能なツールはありません",
+      noToolsFound: "ツールが見つかりません",
+      showAllTools: "すべてのツールを表示",
+      showOnlyEnabled: "有効なものだけ表示",
+      showOnlyEnabledTools: "有効なツールのみ表示",
+      disableAllTools: "すべてのツールを無効化",
+      enableAll: "すべて有効化",
+    },
+    oauth: {
+      processing: "処理中...",
+      processingDetails: "セットアップを完了するまでしばらくお待ちください。",
+      invalidRequest: "無効なリクエストです",
+      incompleteAuthorizationRequest:
+        "認証リクエストが不完全です。もう一度お試しください。",
+      authorizationFailed: "認証に失敗しました",
+      authorizationCancelled:
+        "認証がキャンセルされたか失敗しました。もう一度お試しください。",
+      failedToCompleteAuthorization: "認証を完了できませんでした",
+      success: "成功しました",
+      somethingWentWrong: "問題が発生しました",
+      oauthProcessError:
+        "OAuth の処理中にエラーが発生しました。もう一度お試しください。",
+      takeMoments: "完了まで少し時間がかかる場合があります...",
+      backToChat: "チャットに戻る",
+      redirectingInSeconds: "{seconds} 秒後に移動します...",
+      callbackCompletingAuthorization: "認証を完了しています...",
+      callbackStoringCredentials:
+        "資格情報を安全に保存しています。しばらくお待ちください。",
+      callbackAuthorizationSuccessful: "認証が完了しました",
+      callbackAuthorizationFailed: "認証に失敗しました",
+      callbackAuthorizedService:
+        "{serviceName} アカウントへのアクセス許可が正常に完了しました。",
+      callbackAuthorizedTool:
+        "{serviceName} アカウントにツールがアクセスできるよう認証しました。",
+      mcpSetupProcessing:
+        "MCP サーバーのセットアップを完了しています。しばらくお待ちください。",
+      federatedSetupProcessing:
+        "セットアップを完了しています。しばらくお待ちください。",
+    },
+    license: {
+      expiresOn: "Onyx ライセンスの有効期限は {date} です。",
+      expiresTomorrow:
+        "Onyx ライセンスの有効期限は明日（{date}）です。",
+      expiredOn:
+        "Onyx ライセンスの有効期限は {date} に切れています。",
+      graceDaysRemaining:
+        "アクセス制限まで残り {days} 日の猶予があります。今すぐ更新してください。",
+      renewWithin30Days:
+        "更新期限はおおよそ 30 日以内です。Onyx の担当者に連絡して更新してください。",
+      renewWithin2Weeks:
+        "更新期限はおおよそ 2 週間以内です。サービス停止を避けるため、早めに更新してください。",
+      renewWithin24Hours:
+        "更新期限は 24 時間以内です。サービス停止を避けるため、今すぐ更新してください。",
+    },
+    error: {
+      title: "問題が発生しました",
+      description:
+        "Onyx の読み込み中に予期しないエラーが発生しました。ページを再読み込みするか、ホーム画面に戻って再度お試しください。",
+      reload: "ページを再読み込み",
+    },
+  },
+};
+
+export function isLocale(value: string | null | undefined): value is Locale {
+  return value === "en" || value === "ja";
+}
+
+export function detectLocale(
+  acceptLanguage?: string | null,
+  cookieLocale?: string | null
+): Locale {
+  if (isLocale(cookieLocale)) return cookieLocale;
+  if (acceptLanguage?.toLowerCase().includes("ja")) return "ja";
+  return "en";
+}
+
+function interpolate(template: string, vars?: Record<string, string | number>) {
+  if (!vars) return template;
+  return template.replace(/\{(\w+)\}/g, (_, key: string) => {
+    const value = vars[key];
+    return value === undefined ? `{${key}}` : String(value);
+  });
+}
+
+export function t(
+  locale: Locale,
+  path: string,
+  vars?: Record<string, string | number>
+): string {
+  const parts = path.split(".");
+  let current: unknown = messages[locale];
+
+  for (const part of parts) {
+    if (!current || typeof current !== "object" || !(part in current)) {
+      return path;
+    }
+    current = (current as Record<string, unknown>)[part];
+  }
+
+  if (typeof current !== "string") return path;
+  return interpolate(current, vars);
+}
+
+export function formatDate(locale: Locale, value: string | null): string {
+  if (!value) return locale === "ja" ? "近日中" : "soon";
+  const date = new Date(value);
+  if (Number.isNaN(date.getTime())) return value;
+  return new Intl.DateTimeFormat(locale === "ja" ? "ja-JP" : "en-US", {
+    dateStyle: "medium",
+  }).format(date);
+}
diff --git a/web/src/providers/LocaleProvider.tsx b/web/src/providers/LocaleProvider.tsx
new file mode 100644
index 0000000000..021aa86d5e
--- /dev/null
+++ b/web/src/providers/LocaleProvider.tsx
@@ -0,0 +1,67 @@
+"use client";
+
+import Cookies from "js-cookie";
+import { createContext, useContext, useEffect, useMemo, useState } from "react";
+import { detectLocale, formatDate, isLocale, Locale, t } from "@/lib/i18n";
+
+const LOCALE_COOKIE = "onyx-locale";
+const LOCALE_STORAGE_KEY = "onyx-locale";
+
+type LocaleContextValue = {
+  locale: Locale;
+  setLocale: (locale: Locale) => void;
+  t: typeof t;
+  formatDate: typeof formatDate;
+};
+
+const LocaleContext = createContext<LocaleContextValue | null>(null);
+
+export function LocaleProvider({
+  children,
+  initialLocale,
+}: {
+  children: React.ReactNode;
+  initialLocale: Locale;
+}) {
+  const [locale, setLocaleState] = useState<Locale>(initialLocale);
+
+  useEffect(() => {
+    const stored = window.localStorage.getItem(LOCALE_STORAGE_KEY);
+    if (isLocale(stored)) {
+      setLocaleState(stored);
+      return;
+    }
+    setLocaleState(detectLocale(window.navigator.language));
+  }, []);
+
+  useEffect(() => {
+    document.documentElement.lang = locale;
+    document.documentElement.dir = "ltr";
+    window.localStorage.setItem(LOCALE_STORAGE_KEY, locale);
+    Cookies.set(LOCALE_COOKIE, locale, { expires: 365 });
+  }, [locale]);
+
+  const value = useMemo<LocaleContextValue>(
+    () => ({
+      locale,
+      setLocale: setLocaleState,
+      t: (currentLocale, path, vars) => t(currentLocale ?? locale, path, vars),
+      formatDate: (currentLocale, value) =>
+        formatDate(currentLocale ?? locale, value),
+    }),
+    [locale]
+  );
+
+  return (
+    <LocaleContext.Provider value={value}>{children}</LocaleContext.Provider>
+  );
+}
+
+export function useLocale() {
+  const context = useContext(LocaleContext);
+  if (!context) {
+    throw new Error("useLocale must be used within a LocaleProvider");
+  }
+  return context;
+}
+
diff --git a/web/src/sections/AppHealthBanner.tsx b/web/src/sections/AppHealthBanner.tsx
index 9049d92e11..546e3b92fd 100644
--- a/web/src/sections/AppHealthBanner.tsx
+++ b/web/src/sections/AppHealthBanner.tsx
@@ -14,9 +14,11 @@ import { SvgAlertTriangle, SvgLogOut } from "@opal/icons";
 import { Content } from "@opal/layouts";
 import { useCurrentUser } from "@/hooks/useCurrentUser";
 import { getExtensionContext } from "@/lib/extension/utils";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export default function AppHealthBanner() {
   const router = useRouter();
+  const { locale, t } = useLocale();
   const { error } = useSWR(SWR_KEYS.health, errorHandlingFetcher);
   const [expired, setExpired] = useState(false);
   const [dismissed, setDismissed] = useState(false);
@@ -231,14 +233,17 @@ export default function AppHealthBanner() {
     return (
       <Modal open>
         <Modal.Content width="sm" height="sm">
-          <Modal.Header icon={SvgLogOut} title="You Have Been Logged Out" />
+          <Modal.Header
+            icon={SvgLogOut}
+            title={t(locale, "auth.loggedOutTitle")}
+          />
           <Modal.Body>
             <p className="text-sm">
-              Your session has expired. Please log in again to continue.
+              {t(locale, "auth.sessionExpiredBody")}
             </p>
           </Modal.Body>
           <Modal.Footer>
-            <Button onClick={handleLogin}>Log In</Button>
+            <Button onClick={handleLogin}>{t(locale, "auth.logIn")}</Button>
           </Modal.Footer>
         </Modal.Content>
       </Modal>
@@ -256,8 +261,8 @@ export default function AppHealthBanner() {
       <div className="fixed top-0 left-0 z-101 w-full bg-status-error-01 p-3">
         <Content
           icon={SvgAlertTriangle}
-          title="The backend is currently unavailable"
-          description="If this is your initial setup or you just updated your Onyx deployment, this is likely because the backend is still starting up. Give it a minute or two, and then refresh the page. If that does not work, make sure the backend is setup and/or contact an administrator."
+          title={t(locale, "health.backendUnavailableTitle")}
+          description={t(locale, "health.backendUnavailableBody")}
           sizePreset="main-content"
           variant="section"
         />
diff --git a/web/src/sections/LicenseExpiryBanner.tsx b/web/src/sections/LicenseExpiryBanner.tsx
index 2d29431b6b..6d0be5c773 100644
--- a/web/src/sections/LicenseExpiryBanner.tsx
+++ b/web/src/sections/LicenseExpiryBanner.tsx
@@ -5,6 +5,8 @@ import { usePathname } from "next/navigation";
 import { MessageCard } from "@opal/components";
 import type { ExpiryWarningStage } from "@/lib/billing/interfaces";
 import { useLicense } from "@/hooks/useLicense";
+import { formatDate, t } from "@/lib/i18n";
+import { useLocale } from "@/providers/LocaleProvider";
 
 const DISMISS_STORAGE_KEY = "license-expiry-banner-dismissed";
 
@@ -19,42 +21,39 @@ interface BannerCopy {
 function buildCopy(
   stage: ExpiryWarningStage,
   expiresAt: string | null,
-  graceDaysRemaining: number
+  graceDaysRemaining: number,
+  locale: "en" | "ja"
 ): BannerCopy | null {
-  const expiresDisplay = expiresAt
-    ? new Date(expiresAt).toLocaleDateString()
-    : "soon";
+  const expiresDisplay = formatDate(locale, expiresAt);
 
   if (stage === "t_30d") {
     return {
-      title: `Your Onyx license expires on ${expiresDisplay}.`,
-      description:
-        "Renewal is due in approximately 30 days. Contact your Onyx representative to renew.",
+      title: t(locale, "license.expiresOn", { date: expiresDisplay }),
+      description: t(locale, "license.renewWithin30Days"),
       variant: "warning",
     };
   }
   if (stage === "t_14d") {
     return {
-      title: `Your Onyx license expires on ${expiresDisplay}.`,
-      description:
-        "Renewal is due in approximately 2 weeks. Complete renewal soon to avoid service interruption.",
+      title: t(locale, "license.expiresOn", { date: expiresDisplay }),
+      description: t(locale, "license.renewWithin2Weeks"),
       variant: "warning",
     };
   }
   if (stage === "t_1d") {
     return {
-      title: `Your Onyx license expires tomorrow (${expiresDisplay}).`,
-      description:
-        "Renewal is due within 24 hours. Renew now to avoid service interruption.",
+      title: t(locale, "license.expiresTomorrow", { date: expiresDisplay }),
+      description: t(locale, "license.renewWithin24Hours"),
       variant: "error",
     };
   }
   if (stage === "grace") {
     return {
-      title: `Your Onyx license expired on ${expiresDisplay}.`,
-      description: `${graceDaysRemaining} grace day${
-        graceDaysRemaining === 1 ? "" : "s"
-      } remaining before access is gated. Renew now.`,
+      title: t(locale, "license.expiredOn", { date: expiresDisplay }),
+      description: t(locale, "license.graceDaysRemaining", {
+        days: graceDaysRemaining,
+        plural: graceDaysRemaining === 1 ? "" : "s",
+      }),
       variant: "error",
     };
   }
@@ -93,7 +92,8 @@ export function LicenseExpiryBannerView({
   graceDaysRemaining,
   onDismiss,
 }: LicenseExpiryBannerViewProps) {
-  const copy = buildCopy(stage, expiresAt, graceDaysRemaining);
+  const { locale } = useLocale();
+  const copy = buildCopy(stage, expiresAt, graceDaysRemaining, locale);
   if (!copy) return null;
 
   return (
diff --git a/web/src/sections/actions/ActionCardHeader.tsx b/web/src/sections/actions/ActionCardHeader.tsx
index 0a2b3b3c04..4aca9552ff 100644
--- a/web/src/sections/actions/ActionCardHeader.tsx
+++ b/web/src/sections/actions/ActionCardHeader.tsx
@@ -10,6 +10,7 @@ import type { IconProps } from "@opal/types";
 import Truncated from "@/refresh-components/texts/Truncated";
 import { SvgEdit } from "@opal/icons";
 import { useActionCardContext } from "@/sections/actions/ActionCardContext";
+import { useLocale } from "@/providers/LocaleProvider";
 
 interface ActionCardHeaderProps {
   title: string;
@@ -30,6 +31,8 @@ function ActionCardHeader({
 }: ActionCardHeaderProps) {
   const [isRenaming, setIsRenaming] = useState(false);
   const { isHovered } = useActionCardContext();
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
 
   const isConnected = status === ActionStatus.CONNECTED;
   const isPending = status === ActionStatus.PENDING;
@@ -102,7 +105,7 @@ function ActionCardHeader({
               text03
               className="shrink-0 whitespace-nowrap"
             >
-              (Not Authenticated)
+              {isJa ? "(未認証)" : "(Not Authenticated)"}
             </Text>
           )}
           {isDisconnected && !isRenaming && (
@@ -112,19 +115,19 @@ function ActionCardHeader({
               text02
               className="shrink-0 whitespace-nowrap"
             >
-              (Disconnected)
+              {isJa ? "(切断済み)" : "(Disconnected)"}
             </Text>
           )}
           {showRenameIcon && (
             // TODO(@raunakab): migrate to opal Button once className/iconClassName is resolved
             <IconButton
               icon={SvgEdit}
-              tooltip="Rename"
+              tooltip={isJa ? "名前を変更" : "Rename"}
               internal
               tertiary
               onClick={handleRenameClick}
               className="h-6 w-6 opacity-70 hover:opacity-100"
-              aria-label={`Rename ${title}`}
+              aria-label={isJa ? `${title} の名前を変更` : `Rename ${title}`}
             />
           )}
         </div>
diff --git a/web/src/sections/actions/Actions.tsx b/web/src/sections/actions/Actions.tsx
index e803b47393..652ff4d986 100644
--- a/web/src/sections/actions/Actions.tsx
+++ b/web/src/sections/actions/Actions.tsx
@@ -12,6 +12,7 @@ import {
 } from "@opal/icons";
 import { useActionCardContext } from "@/sections/actions/ActionCardContext";
 import { cn } from "@opal/utils";
+import { useLocale } from "@/providers/LocaleProvider";
 
 interface ActionsProps {
   status: ActionStatus;
@@ -40,12 +41,17 @@ const Actions = React.memo(
     onToggleTools,
   }: ActionsProps) => {
     const { isHovered: isParentHovered } = useActionCardContext();
+    const { locale } = useLocale();
+    const isJa = locale === "ja";
     const showViewToolsButton =
       (status === ActionStatus.CONNECTED ||
         status === ActionStatus.FETCHING ||
         status === ActionStatus.DISCONNECTED) &&
       !isToolsExpanded &&
       onToggleTools;
+    const viewToolsLabel = isJa
+      ? `${toolCount ?? 0}件のツールを表示`
+      : `View ${toolCount ?? 0} tool${toolCount !== 1 ? "s" : ""}`;
 
     // Connected state
     if (status === ActionStatus.CONNECTED || status === ActionStatus.FETCHING) {
@@ -60,23 +66,31 @@ const Actions = React.memo(
                     ? "opacity-100 translate-x-0 pointer-events-auto"
                     : "opacity-0 translate-x-2 pointer-events-none"
                 )}
-              >
-                <Button
-                  icon={SvgUnplug}
-                  tooltip="Disconnect Server"
-                  prominence="tertiary"
-                  onClick={onDisconnect}
-                  aria-label={`Disconnect ${serverName} server`}
-                />
-              </div>
-            )}
+                >
+                  <Button
+                    icon={SvgUnplug}
+                    tooltip={isJa ? "サーバーを切断" : "Disconnect Server"}
+                    prominence="tertiary"
+                    onClick={onDisconnect}
+                    aria-label={
+                      isJa
+                        ? `${serverName} サーバーを切断`
+                        : `Disconnect ${serverName} server`
+                    }
+                  />
+                </div>
+              )}
             {onManage && (
               <Button
                 icon={SvgSettings}
-                tooltip="Manage Server"
+                tooltip={isJa ? "サーバーを管理" : "Manage Server"}
                 prominence="tertiary"
                 onClick={onManage}
-                aria-label={`Manage ${serverName} server`}
+                aria-label={
+                  isJa
+                    ? `${serverName} サーバーを管理`
+                    : `Manage ${serverName} server`
+                }
               />
             )}
           </div>
@@ -85,11 +99,17 @@ const Actions = React.memo(
               prominence="tertiary"
               onClick={onToggleTools}
               rightIcon={SvgChevronDown}
-              aria-label={`View tools for ${serverName}`}
+              aria-label={
+                isJa
+                  ? `${serverName} のツールを表示`
+                  : `View tools for ${serverName}`
+              }
             >
               {status === ActionStatus.FETCHING
-                ? "Fetching tools..."
-                : `View ${toolCount ?? 0} tool${toolCount !== 1 ? "s" : ""}`}
+                ? isJa
+                  ? "ツールを取得中..."
+                  : "Fetching tools..."
+                : viewToolsLabel}
             </Button>
           )}
         </div>
@@ -105,9 +125,13 @@ const Actions = React.memo(
               prominence="tertiary"
               onClick={onAuthenticate}
               rightIcon={SvgArrowExchange}
-              aria-label={`Authenticate and connect to ${serverName}`}
+              aria-label={
+                isJa
+                  ? `${serverName} を認証して接続`
+                  : `Authenticate and connect to ${serverName}`
+              }
             >
-              Authenticate
+              {isJa ? "認証" : "Authenticate"}
             </Button>
           )}
           <div
@@ -121,19 +145,27 @@ const Actions = React.memo(
             {onDelete && (
               <Button
                 icon={SvgTrash}
-                tooltip="Delete Server"
+                tooltip={isJa ? "サーバーを削除" : "Delete Server"}
                 prominence="tertiary"
                 onClick={onDelete}
-                aria-label={`Delete ${serverName} server`}
+                aria-label={
+                  isJa
+                    ? `${serverName} サーバーを削除`
+                    : `Delete ${serverName} server`
+                }
               />
             )}
             {onManage && (
               <Button
                 icon={SvgSettings}
-                tooltip="Manage Server"
+                tooltip={isJa ? "サーバーを管理" : "Manage Server"}
                 prominence="tertiary"
                 onClick={onManage}
-                aria-label={`Manage ${serverName} server`}
+                aria-label={
+                  isJa
+                    ? `${serverName} サーバーを管理`
+                    : `Manage ${serverName} server`
+                }
               />
             )}
           </div>
@@ -150,18 +182,26 @@ const Actions = React.memo(
               prominence="secondary"
               onClick={onReconnect}
               rightIcon={SvgPlug}
-              aria-label={`Reconnect to ${serverName}`}
+              aria-label={
+                isJa
+                  ? `${serverName} に再接続`
+                  : `Reconnect to ${serverName}`
+              }
             >
-              Reconnect
+              {isJa ? "再接続" : "Reconnect"}
             </Button>
           )}
           {onManage && (
             <Button
               icon={SvgSettings}
-              tooltip="Manage Server"
+              tooltip={isJa ? "サーバーを管理" : "Manage Server"}
               prominence="tertiary"
               onClick={onManage}
-              aria-label={`Manage ${serverName} server`}
+              aria-label={
+                isJa
+                  ? `${serverName} サーバーを管理`
+                  : `Manage ${serverName} server`
+              }
             />
           )}
         </div>
@@ -171,9 +211,13 @@ const Actions = React.memo(
             prominence="tertiary"
             onClick={onToggleTools}
             rightIcon={SvgChevronDown}
-            aria-label={`View tools for ${serverName}`}
+            aria-label={
+              isJa
+                ? `${serverName} のツールを表示`
+                : `View tools for ${serverName}`
+            }
           >
-            {`View ${toolCount ?? 0} tool${toolCount !== 1 ? "s" : ""}`}
+            {viewToolsLabel}
           </Button>
         )}
       </div>
diff --git a/web/src/sections/actions/MCPActionCard.tsx b/web/src/sections/actions/MCPActionCard.tsx
index eb43aebb36..f5ef46e78c 100644
--- a/web/src/sections/actions/MCPActionCard.tsx
+++ b/web/src/sections/actions/MCPActionCard.tsx
@@ -32,36 +32,24 @@ import Text from "@/refresh-components/texts/Text";
 import { timeAgo } from "@opal/time";
 import { cn } from "@opal/utils";
 import Modal from "@/refresh-components/layouts/ConfirmationModalLayout";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export interface MCPActionCardProps {
-  // Server identification
   serverId: number;
   server: MCPServer;
-
-  // Core content
   title: string;
   description: string;
   logo?: React.FunctionComponent<IconProps>;
-
-  // Status
   status: ActionStatus;
-
-  // Initial expanded state
   initialExpanded?: boolean;
-
-  // Tool count (only for connected state)
   toolCount?: number;
-
-  // Actions
   onDisconnect?: () => void;
   onManage?: () => void;
   onEdit?: () => void;
   onDelete?: () => Promise<void> | void;
-  onAuthenticate?: () => void; // For pending state
-  onReconnect?: () => void; // For disconnected state
-  onRename?: (serverId: number, newName: string) => Promise<void>; // For renaming
-
-  // Tool-related actions (now includes SWR mutate function for optimistic updates)
+  onAuthenticate?: () => void;
+  onReconnect?: () => void;
+  onRename?: (serverId: number, newName: string) => Promise<void>;
   onToolToggle?: (
     serverId: number,
     toolId: string,
@@ -78,12 +66,9 @@ export interface MCPActionCardProps {
     enabled: boolean,
     mutate: KeyedMutator<ToolSnapshot[]>
   ) => void;
-
-  // Optional styling
   className?: string;
 }
 
-// Main Component
 export default function MCPActionCard({
   serverId,
   server,
@@ -105,18 +90,18 @@ export default function MCPActionCard({
   onUpdateToolsStatus,
   className,
 }: MCPActionCardProps) {
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
   const [isToolsExpanded, setIsToolsExpanded] = useState(initialExpanded);
   const [searchQuery, setSearchQuery] = useState("");
   const [showOnlyEnabled, setShowOnlyEnabled] = useState(false);
   const [isToolsRefreshing, setIsToolsRefreshing] = useState(false);
   const deleteModal = useCreateModal();
 
-  // Update expanded state when initialExpanded changes
   const hasInitializedExpansion = useRef(false);
   const previousStatus = useRef<MCPServerStatus>(server.status);
   const hasRetriedTools = useRef(false);
 
-  // Apply initial expansion only once per component lifetime
   useEffect(() => {
     if (initialExpanded && !hasInitializedExpansion.current) {
       setIsToolsExpanded(true);
@@ -124,7 +109,6 @@ export default function MCPActionCard({
     }
   }, [initialExpanded]);
 
-  // Collapse tools when server becomes disconnected or awaiting auth
   useEffect(() => {
     if (
       server.status === MCPServerStatus.DISCONNECTED ||
@@ -134,10 +118,8 @@ export default function MCPActionCard({
     }
   }, [server.status]);
 
-  // Lazy load tools only when expanded
   const { tools, isLoading, mutate } = useServerTools(server, isToolsExpanded);
 
-  // Retry tools fetch when server transitions from FETCHING_TOOLS to CONNECTED
   useEffect(() => {
     const statusChanged =
       previousStatus.current === MCPServerStatus.FETCHING_TOOLS &&
@@ -151,24 +133,20 @@ export default function MCPActionCard({
       mutate();
     }
 
-    // Update previous status
     previousStatus.current = server.status;
   }, [server.status, tools.length, mutate]);
 
   const isNotAuthenticated = status === ActionStatus.PENDING;
 
-  // Filter tools based on search query and enabled status
   const filteredTools = useMemo(() => {
     if (!tools) return [];
 
     let filtered = tools;
 
-    // Filter by enabled status if showOnlyEnabled is true
     if (showOnlyEnabled) {
       filtered = filtered.filter((tool) => tool.isEnabled);
     }
 
-    // Filter by search query
     if (searchQuery.trim()) {
       const query = searchQuery.toLowerCase();
       filtered = filtered.filter(
@@ -200,7 +178,6 @@ export default function MCPActionCard({
     setShowOnlyEnabled((prev) => !prev);
   };
 
-  // Build the actions component
   const actionsComponent = useMemo(
     () => (
       <Actions
@@ -245,7 +222,6 @@ export default function MCPActionCard({
     }, 1000);
   };
 
-  // Left action for ToolsList footer
   const leftAction = useMemo(() => {
     const lastRefreshedText = timeAgo(server.last_refreshed_at);
 
@@ -255,23 +231,19 @@ export default function MCPActionCard({
           icon={isToolsRefreshing ? SvgSimpleLoader : SvgRefreshCw}
           prominence="internal"
           onClick={handleRefreshTools}
-          tooltip="Refresh tools"
-          aria-label="Refresh tools"
+          tooltip={isJa ? "ツールを再取得" : "Refresh tools"}
+          aria-label={isJa ? "ツールを再取得" : "Refresh tools"}
         />
         {lastRefreshedText && (
           <Text as="p" text03 mainUiBody className="whitespace-nowrap">
-            Tools last refreshed {lastRefreshedText}
+            {isJa
+              ? `最終更新: ${lastRefreshedText}`
+              : `Tools last refreshed ${lastRefreshedText}`}
           </Text>
         )}
       </div>
     );
-  }, [
-    server.last_refreshed_at,
-    serverId,
-    mutate,
-    onRefreshTools,
-    isToolsRefreshing,
-  ]);
+  }, [isJa, isToolsRefreshing, onRefreshTools, server.last_refreshed_at, serverId, mutate]);
 
   return (
     <>
@@ -290,7 +262,7 @@ export default function MCPActionCard({
         onSearchQueryChange={setSearchQuery}
         onFold={handleFold}
         className={className}
-        ariaLabel={`${title} MCP server card`}
+        ariaLabel={isJa ? `${title} のMCPサーバーカード` : `${title} MCP server card`}
       >
         <ToolsList
           isFetching={
@@ -306,8 +278,8 @@ export default function MCPActionCard({
           }}
           isEmpty={filteredTools.length === 0}
           searchQuery={searchQuery}
-          emptyMessage="No tools available"
-          emptySearchMessage="No tools found"
+          emptyMessage={isJa ? "利用可能なツールはありません" : "No tools available"}
+          emptySearchMessage={isJa ? "ツールが見つかりません" : "No tools found"}
           leftAction={leftAction}
         >
           {filteredTools.map((tool) => (
@@ -332,7 +304,7 @@ export default function MCPActionCard({
           icon={({ className }) => (
             <SvgTrash className={cn(className, "stroke-action-danger-05")} />
           )}
-          title="Delete MCP server"
+          title={isJa ? "MCPサーバーを削除" : "Delete MCP server"}
           onClose={() => deleteModal.toggle(false)}
           submit={
             <Button
@@ -343,22 +315,32 @@ export default function MCPActionCard({
                   await onDelete();
                   deleteModal.toggle(false);
                 } catch (error) {
-                  // Keep modal open if deletion fails; caller should surface error feedback.
                   console.error("Failed to delete MCP server", error);
                 }
               }}
             >
-              Delete
+              {isJa ? "削除" : "Delete"}
             </Button>
           }
         >
           <div className="flex flex-col gap-4">
             <Text as="p" text03>
-              All tools connected to <b>{title}</b> will be removed. Deletion is
-              irreversible.
+              {isJa ? (
+                <>
+                  <b>{title}</b>
+                  に接続されているすべてのツールが削除されます。この操作は元に戻せません。
+                </>
+              ) : (
+                <>
+                  All tools connected to <b>{title}</b> will be removed. Deletion is
+                  irreversible.
+                </>
+              )}
             </Text>
             <Text as="p" text03>
-              Are you sure you want to delete this MCP server?
+              {isJa
+                ? "このMCPサーバーを削除してもよろしいですか？"
+                : "Are you sure you want to delete this MCP server?"}
             </Text>
           </div>
         </Modal>
diff --git a/web/src/sections/actions/MCPPageContent.tsx b/web/src/sections/actions/MCPPageContent.tsx
index c632080fb8..b5be8fbeaa 100644
--- a/web/src/sections/actions/MCPPageContent.tsx
+++ b/web/src/sections/actions/MCPPageContent.tsx
@@ -28,8 +28,11 @@ import {
 import { useSearchParams } from "next/navigation";
 import { useRouter } from "next/navigation";
 import useMcpServers from "@/hooks/useMcpServers";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export default function MCPPageContent() {
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
   // Data fetching
   const {
     mcpData,
@@ -89,15 +92,23 @@ export default function MCPPageContent() {
 
           await refreshMCPServerTools(serverIdInt);
 
-          toast.success("Successfully connected and fetched tools");
+          toast.success(
+            isJa
+              ? "接続が完了し、ツールを取得しました"
+              : "Successfully connected and fetched tools"
+          );
 
           await mutateMcpServers();
         } catch (error) {
           console.error("Failed to fetch tools:", error);
           toast.error(
-            `Failed to fetch tools: ${
-              error instanceof Error ? error.message : "Unknown error"
-            }`
+            isJa
+              ? `ツールの取得に失敗しました: ${
+                  error instanceof Error ? error.message : "不明なエラー"
+                }`
+              : `Failed to fetch tools: ${
+                  error instanceof Error ? error.message : "Unknown error"
+                }`
           );
           await mutateMcpServers();
         }
@@ -170,7 +181,11 @@ export default function MCPPageContent() {
         MCPServerStatus.DISCONNECTED
       );
 
-      toast.success("MCP Server disconnected successfully");
+      toast.success(
+        isJa
+          ? "MCPサーバーを切断しました"
+          : "MCP Server disconnected successfully"
+      );
 
       await mutateMcpServers();
       disconnectModal.toggle(false);
@@ -180,7 +195,9 @@ export default function MCPPageContent() {
       toast.error(
         error instanceof Error
           ? error.message
-          : "Failed to disconnect MCP Server"
+          : isJa
+            ? "MCPサーバーの切断に失敗しました"
+            : "Failed to disconnect MCP Server"
       );
     } finally {
       setIsDisconnecting(false);
@@ -194,7 +211,9 @@ export default function MCPPageContent() {
     try {
       await deleteMCPServer(activeServer.id);
 
-      toast.success("MCP Server deleted successfully");
+      toast.success(
+        isJa ? "MCPサーバーを削除しました" : "MCP Server deleted successfully"
+      );
 
       await mutateMcpServers();
       disconnectModal.toggle(false);
@@ -202,7 +221,11 @@ export default function MCPPageContent() {
     } catch (error) {
       console.error("Error deleting server:", error);
       toast.error(
-        error instanceof Error ? error.message : "Failed to delete MCP Server"
+        error instanceof Error
+          ? error.message
+          : isJa
+            ? "MCPサーバーの削除に失敗しました"
+            : "Failed to delete MCP Server"
       );
     } finally {
       setIsDisconnecting(false);
@@ -239,13 +262,19 @@ export default function MCPPageContent() {
       try {
         await deleteMCPServer(serverId);
 
-        toast.success("MCP Server deleted successfully");
+        toast.success(
+          isJa ? "MCPサーバーを削除しました" : "MCP Server deleted successfully"
+        );
 
         await mutateMcpServers();
       } catch (error) {
         console.error("Error deleting server:", error);
         toast.error(
-          error instanceof Error ? error.message : "Failed to delete MCP Server"
+          error instanceof Error
+            ? error.message
+            : isJa
+              ? "MCPサーバーの削除に失敗しました"
+              : "Failed to delete MCP Server"
         );
       }
     },
@@ -278,15 +307,23 @@ export default function MCPPageContent() {
 
         await refreshMCPServerTools(serverId);
 
-        toast.success("Successfully connected and fetched tools");
+        toast.success(
+          isJa
+            ? "接続が完了し、ツールを取得しました"
+            : "Successfully connected and fetched tools"
+        );
 
         await mutateMcpServers();
       } catch (error) {
         console.error("Failed to fetch tools:", error);
         toast.error(
-          `Failed to fetch tools: ${
-            error instanceof Error ? error.message : "Unknown error"
-          }`
+          isJa
+            ? `ツールの取得に失敗しました: ${
+                error instanceof Error ? error.message : "不明なエラー"
+              }`
+            : `Failed to fetch tools: ${
+                error instanceof Error ? error.message : "Unknown error"
+              }`
         );
         await mutateMcpServers();
       }
@@ -299,16 +336,22 @@ export default function MCPPageContent() {
       try {
         await updateMCPServerStatus(serverId, MCPServerStatus.CONNECTED);
 
-        toast.success("MCP Server reconnected successfully");
+        toast.success(
+          isJa
+            ? "MCPサーバーに再接続しました"
+            : "MCP Server reconnected successfully"
+        );
 
         await mutateMcpServers();
       } catch (error) {
         console.error("Error reconnecting server:", error);
         toast.error(
-          error instanceof Error
-            ? error.message
+        error instanceof Error
+          ? error.message
+          : isJa
+            ? "MCPサーバーへの再接続に失敗しました"
             : "Failed to reconnect MCP Server"
-        );
+      );
       }
     },
     [mutateMcpServers]
@@ -338,7 +381,11 @@ export default function MCPPageContent() {
         // Revalidate to get fresh data from server
         await mutateServerTools();
 
-        toast.success(`Tool ${enabled ? "enabled" : "disabled"} successfully`);
+        toast.success(
+          isJa
+            ? `ツールを${enabled ? "有効化" : "無効化"}しました`
+            : `Tool ${enabled ? "enabled" : "disabled"} successfully`
+        );
       } catch (error) {
         console.error("Error toggling tool:", error);
 
@@ -346,7 +393,11 @@ export default function MCPPageContent() {
         await mutateServerTools();
 
         toast.error(
-          error instanceof Error ? error.message : "Failed to update tool"
+          error instanceof Error
+            ? error.message
+            : isJa
+              ? "ツールの更新に失敗しました"
+              : "Failed to update tool"
         );
       }
     },
@@ -368,11 +419,17 @@ export default function MCPPageContent() {
         // Also refresh the servers list to update tool counts
         await mutateMcpServers();
 
-        toast.success("Tools refreshed successfully");
+        toast.success(
+          isJa ? "ツールを再取得しました" : "Tools refreshed successfully"
+        );
       } catch (error) {
         console.error("Error refreshing tools:", error);
         toast.error(
-          error instanceof Error ? error.message : "Failed to refresh tools"
+          error instanceof Error
+            ? error.message
+            : isJa
+              ? "ツールの再取得に失敗しました"
+              : "Failed to refresh tools"
         );
       }
     },
@@ -388,7 +445,7 @@ export default function MCPPageContent() {
     ) => {
       try {
         if (toolIds.length === 0) {
-          toast.info("No tools to disable");
+          toast.info(isJa ? "対象のツールがありません" : "No tools to disable");
           return;
         }
 
@@ -409,9 +466,11 @@ export default function MCPPageContent() {
         await mutateServerTools();
 
         toast.success(
-          `${result.updated_count} tool${
-            result.updated_count !== 1 ? "s" : ""
-          } ${enabled ? "enabled" : "disabled"} successfully`
+          isJa
+            ? `ツールを${result.updated_count}件${enabled ? "有効化" : "無効化"}しました`
+            : `${result.updated_count} tool${
+                result.updated_count !== 1 ? "s" : ""
+              } ${enabled ? "enabled" : "disabled"} successfully`
         );
       } catch (error) {
         console.error(
@@ -425,7 +484,9 @@ export default function MCPPageContent() {
         toast.error(
           error instanceof Error
             ? error.message
-            : `Failed to ${enabled ? "enable" : "disable"} all tools`
+            : isJa
+              ? `ツールの一括${enabled ? "有効化" : "無効化"}に失敗しました`
+              : `Failed to ${enabled ? "enable" : "disable"} all tools`
         );
       }
     },
@@ -449,12 +510,20 @@ export default function MCPPageContent() {
     async (serverId: number, newName: string) => {
       try {
         await updateMCPServer(serverId, { name: newName });
-        toast.success("MCP Server renamed successfully");
+        toast.success(
+          isJa
+            ? "MCPサーバー名を更新しました"
+            : "MCP Server renamed successfully"
+        );
         await mutateMcpServers();
       } catch (error) {
         console.error("Error renaming server:", error);
         toast.error(
-          error instanceof Error ? error.message : "Failed to rename MCP Server"
+          error instanceof Error
+            ? error.message
+            : isJa
+              ? "MCPサーバー名の更新に失敗しました"
+              : "Failed to rename MCP Server"
         );
         throw error; // Re-throw so ButtonRenaming can handle it
       }
@@ -492,8 +561,13 @@ export default function MCPPageContent() {
           searchQuery={searchQuery}
           onSearchQueryChange={setSearchQuery}
           onAction={handleAddServer}
-          actionLabel="Add MCP Server"
-          emptyStateText="Connect MCP server to add custom actions."
+          placeholder={isJa ? "MCPサーバーを検索..." : "Search MCP servers..."}
+          actionLabel={isJa ? "MCPサーバーを追加" : "Add MCP Server"}
+          emptyStateText={
+            isJa
+              ? "MCPサーバーに接続してカスタムアクションを追加します。"
+              : "Connect MCP server to add custom actions."
+          }
         />
       </div>
 
diff --git a/web/src/sections/actions/OpenApiActionCard.tsx b/web/src/sections/actions/OpenApiActionCard.tsx
index f3539aaeb7..a9e86b6fae 100644
--- a/web/src/sections/actions/OpenApiActionCard.tsx
+++ b/web/src/sections/actions/OpenApiActionCard.tsx
@@ -15,6 +15,7 @@ import Modal from "@/refresh-components/layouts/ConfirmationModalLayout";
 import { Button } from "@opal/components";
 import Text from "@/refresh-components/texts/Text";
 import { cn } from "@opal/utils";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export interface OpenApiActionCardProps {
   tool: ToolSnapshot;
@@ -35,6 +36,8 @@ export default function OpenApiActionCard({
   mutateOpenApiTools,
   onOpenDisconnectModal,
 }: OpenApiActionCardProps) {
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
   const [isToolsExpanded, setIsToolsExpanded] = useState(false);
   const [searchQuery, setSearchQuery] = useState("");
   const [updatingStatus, setUpdatingStatus] = useState(false);
@@ -166,20 +169,30 @@ export default function OpenApiActionCard({
         searchQuery={searchQuery}
         onSearchQueryChange={setSearchQuery}
         onFold={handleFold}
-        ariaLabel={`${tool.name} OpenAPI action card`}
+        ariaLabel={
+          isJa
+            ? `${tool.name} のOpenAPIアクションカード`
+            : `${tool.name} OpenAPI action card`
+        }
       >
         <ToolsList
           isEmpty={filteredTools.length === 0}
           searchQuery={searchQuery}
-          emptyMessage="No actions defined for this OpenAPI schema"
-          emptySearchMessage="No actions match your search"
+          emptyMessage={
+            isJa
+              ? "このOpenAPIスキーマにはアクションが定義されていません"
+              : "No actions defined for this OpenAPI schema"
+          }
+          emptySearchMessage={
+            isJa ? "検索条件に一致するアクションがありません" : "No actions match your search"
+          }
           className="gap-2"
         >
           {filteredTools.map((method) => (
             <ToolItem
               key={`${tool.id}-${method.method}-${method.path}-${method.name}`}
               name={method.name}
-              description={method.summary || "No summary provided"}
+              description={method.summary || (isJa ? "説明はありません" : "No summary provided")}
               variant="openapi"
               openApiMetadata={{
                 method: method.method,
@@ -195,7 +208,7 @@ export default function OpenApiActionCard({
           icon={({ className }) => (
             <SvgTrash className={cn(className, "stroke-action-danger-05")} />
           )}
-          title="Delete OpenAPI action"
+          title={isJa ? "OpenAPIアクションを削除" : "Delete OpenAPI action"}
           onClose={() => deleteModal.toggle(false)}
           submit={
             <Button
@@ -205,17 +218,27 @@ export default function OpenApiActionCard({
                 deleteModal.toggle(false);
               }}
             >
-              Delete
+              {isJa ? "削除" : "Delete"}
             </Button>
           }
         >
           <div className="flex flex-col gap-4">
             <Text as="p" text03>
-              This will permanently delete the OpenAPI action <b>{tool.name}</b>{" "}
-              and its configuration.
+              {isJa ? (
+                <>
+                  OpenAPIアクション <b>{tool.name}</b> とその設定を完全に削除します。
+                </>
+              ) : (
+                <>
+                  This will permanently delete the OpenAPI action <b>{tool.name}</b>{" "}
+                  and its configuration.
+                </>
+              )}
             </Text>
             <Text as="p" text03>
-              Are you sure you want to delete this OpenAPI action?
+              {isJa
+                ? "このOpenAPIアクションを削除してもよろしいですか？"
+                : "Are you sure you want to delete this OpenAPI action?"}
             </Text>
           </div>
         </Modal>
diff --git a/web/src/sections/actions/OpenApiPageContent.tsx b/web/src/sections/actions/OpenApiPageContent.tsx
index 7e123aaf05..21b88e5e0a 100644
--- a/web/src/sections/actions/OpenApiPageContent.tsx
+++ b/web/src/sections/actions/OpenApiPageContent.tsx
@@ -17,8 +17,11 @@ import { updateToolStatus } from "@/lib/tools/mcpService";
 import DisconnectEntityModal from "./modals/DisconnectEntityModal";
 import ActionCardSkeleton from "./skeleton/ActionCardSkeleton";
 import useOpenApiTools from "@/hooks/useOpenApiTools";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export default function OpenApiPageContent() {
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
   const {
     openApiTools,
     mutateOpenApiTools,
@@ -66,7 +69,11 @@ export default function OpenApiPageContent() {
   const handleConnect = useCallback(
     async (values: OpenAPIAuthFormValues) => {
       if (!selectedTool) {
-        throw new Error("No OpenAPI action selected for authentication.");
+        throw new Error(
+          isJa
+            ? "認証対象のOpenAPIアクションが選択されていません。"
+            : "No OpenAPI action selected for authentication."
+        );
       }
 
       try {
@@ -113,9 +120,13 @@ export default function OpenApiPageContent() {
           }
 
           toast.success(
-            `${selectedTool.name} authentication ${
-              selectedTool.oauth_config_id ? "updated" : "saved"
-            } successfully.`
+            isJa
+              ? `${selectedTool.name} の認証設定を${
+                  selectedTool.oauth_config_id ? "更新" : "保存"
+                }しました。`
+              : `${selectedTool.name} authentication ${
+                  selectedTool.oauth_config_id ? "updated" : "saved"
+                } successfully.`
           );
         } else if (values.authMethod === "custom-header") {
           const customHeaders = values.headers
@@ -136,7 +147,9 @@ export default function OpenApiPageContent() {
           }
 
           toast.success(
-            `${selectedTool.name} authentication headers saved successfully.`
+            isJa
+              ? `${selectedTool.name} の認証ヘッダーを保存しました。`
+              : `${selectedTool.name} authentication headers saved successfully.`
           );
         } else if (values.authMethod === "pt-oauth") {
           const response = await updateCustomTool(selectedTool.id, {
@@ -148,7 +161,9 @@ export default function OpenApiPageContent() {
             throw new Error(response.error);
           }
           toast.success(
-            `${selectedTool.name} authentication passthrough saved successfully.`
+            isJa
+              ? `${selectedTool.name} の認証パススルー設定を保存しました。`
+              : `${selectedTool.name} authentication passthrough saved successfully.`
           );
         }
 
@@ -158,12 +173,14 @@ export default function OpenApiPageContent() {
         const message =
           error instanceof Error
             ? error.message
-            : "Failed to save authentication settings.";
+            : isJa
+              ? "認証設定の保存に失敗しました。"
+              : "Failed to save authentication settings.";
         toast.error(message);
         throw error;
       }
     },
-    [selectedTool, mutateOpenApiTools]
+    [isJa, selectedTool, mutateOpenApiTools]
   );
 
   const handleManageTool = useCallback(
@@ -187,21 +204,31 @@ export default function OpenApiPageContent() {
       try {
         await updateToolStatus(tool.id, false);
 
-        toast.success(`${tool.name} has been disconnected.`);
+        toast.success(
+          isJa
+            ? `${tool.name} を切断しました。`
+            : `${tool.name} has been disconnected.`
+        );
 
         await mutateOpenApiTools();
       } catch (error) {
         const message =
           error instanceof Error
             ? error.message
-            : "Failed to disconnect OpenAPI action.";
+            : isJa
+              ? "OpenAPIアクションの切断に失敗しました。"
+              : "Failed to disconnect OpenAPI action.";
         toast.error(message);
         throw error instanceof Error
           ? error
-          : new Error("Failed to disconnect OpenAPI action.");
+          : new Error(
+              isJa
+                ? "OpenAPIアクションの切断に失敗しました。"
+                : "Failed to disconnect OpenAPI action."
+            );
       }
     },
-    [mutateOpenApiTools]
+    [isJa, mutateOpenApiTools]
   );
 
   const handleOpenDisconnectModal = useCallback(
@@ -234,24 +261,33 @@ export default function OpenApiPageContent() {
         setIsDeleting(true);
         const response = await deleteCustomTool(tool.id);
         if (response.data) {
-          toast.success(`${tool.name} deleted successfully.`);
+          toast.success(
+            isJa
+              ? `${tool.name} を削除しました。`
+              : `${tool.name} deleted successfully.`
+          );
           await mutateOpenApiTools();
         } else {
-          throw new Error(response.error || "Failed to delete tool.");
+          throw new Error(
+            response.error ||
+              (isJa ? "アクションの削除に失敗しました。" : "Failed to delete tool.")
+          );
         }
       } catch (error) {
         console.error("Failed to delete OpenAPI tool", error);
         toast.error(
           error instanceof Error
             ? error.message
-            : "An unexpected error occurred while deleting the tool."
+            : isJa
+              ? "アクション削除中に予期しないエラーが発生しました。"
+              : "An unexpected error occurred while deleting the tool."
         );
         throw error;
       } finally {
         setIsDeleting(false);
       }
     },
-    [mutateOpenApiTools]
+    [isJa, mutateOpenApiTools]
   );
 
   const handleDeleteToolFromModal = useCallback(async () => {
@@ -291,33 +327,43 @@ export default function OpenApiPageContent() {
         if (response.error) {
           throw new Error(response.error);
         }
-        toast.success("OpenAPI action renamed successfully");
+        toast.success(
+          isJa
+            ? "OpenAPIアクション名を更新しました"
+            : "OpenAPI action renamed successfully"
+        );
         await mutateOpenApiTools();
       } catch (error) {
         console.error("Error renaming tool:", error);
         toast.error(
           error instanceof Error
             ? error.message
-            : "Failed to rename OpenAPI action"
+            : isJa
+              ? "OpenAPIアクション名の更新に失敗しました"
+              : "Failed to rename OpenAPI action"
         );
         throw error; // Re-throw so ButtonRenaming can handle it
       }
     },
-    [mutateOpenApiTools]
+    [isJa, mutateOpenApiTools]
   );
 
   const authenticationModalTitle = useMemo(() => {
     if (!selectedTool) {
-      return "Authenticate OpenAPI Action";
+      return isJa ? "OpenAPIアクションを認証" : "Authenticate OpenAPI Action";
     }
     const hasExistingAuth =
       Boolean(selectedTool.oauth_config_id) ||
       Boolean(selectedTool.custom_headers?.length);
     const prefix = hasExistingAuth
-      ? "Update authentication for"
-      : "Authenticate";
+      ? isJa
+        ? "認証設定を更新:"
+        : "Update authentication for"
+      : isJa
+        ? "認証:"
+        : "Authenticate";
     return `${prefix} ${selectedTool.name}`;
-  }, [selectedTool]);
+  }, [isJa, selectedTool]);
 
   const authenticationDefaultMethod = useMemo<AuthMethod>(() => {
     if (!selectedTool) {
@@ -355,8 +401,15 @@ export default function OpenApiPageContent() {
           searchQuery={searchQuery}
           onSearchQueryChange={setSearchQuery}
           onAction={handleAddAction}
-          actionLabel="Add OpenAPI Action"
-          emptyStateText="Add custom actions from OpenAPI schemas."
+          placeholder={
+            isJa ? "OpenAPIアクションを検索..." : "Search OpenAPI actions..."
+          }
+          actionLabel={isJa ? "OpenAPIアクションを追加" : "Add OpenAPI Action"}
+          emptyStateText={
+            isJa
+              ? "OpenAPIスキーマからカスタムアクションを追加します。"
+              : "Add custom actions from OpenAPI schemas."
+          }
         />
       </div>
 
diff --git a/web/src/sections/actions/PerUserAuthConfig.tsx b/web/src/sections/actions/PerUserAuthConfig.tsx
index 02ef732b5d..e73e24d49b 100644
--- a/web/src/sections/actions/PerUserAuthConfig.tsx
+++ b/web/src/sections/actions/PerUserAuthConfig.tsx
@@ -10,6 +10,7 @@ import Text from "@/refresh-components/texts/Text";
 import { Divider } from "@opal/components";
 import type { MCPAuthFormValues } from "@/sections/actions/modals/MCPAuthenticationModal";
 import { SvgUser } from "@opal/icons";
+import { useLocale } from "@/providers/LocaleProvider";
 
 interface PerUserAuthConfigProps {
   values: MCPAuthFormValues;
@@ -23,6 +24,8 @@ export function PerUserAuthConfig({
   values,
   setFieldValue,
 }: PerUserAuthConfigProps) {
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
   // Use draft state for KeyValue array (like in LLMConnectionFieldsCustom)
   const [headersDraft, setHeadersDraft] = useState<KeyValue[]>(
     Object.entries(values.auth_template?.headers || {}).map(([key, value]) => ({
@@ -104,30 +107,35 @@ export function PerUserAuthConfig({
     <div className="flex flex-col gap-4 -mx-2 px-2 py-2 bg-background-tint-00 rounded-12">
       {/* Authentication Headers */}
       <FormField name="auth_template.headers" state="idle">
-        <FormField.Label>Authentication Headers</FormField.Label>
+        <FormField.Label>
+          {isJa ? "認証ヘッダー" : "Authentication Headers"}
+        </FormField.Label>
         <FormField.Control asChild>
           <InputKeyValue
-            keyTitle="Header Name"
-            valueTitle="Header Value"
+            keyTitle={isJa ? "ヘッダー名" : "Header Name"}
+            valueTitle={isJa ? "ヘッダー値" : "Header Value"}
             items={headersDraft}
             onChange={handleHeadersChange}
             mode="fixed-line"
             layout="equal"
-            addButtonLabel="Add Header"
+            addButtonLabel={isJa ? "ヘッダーを追加" : "Add Header"}
           />
         </FormField.Control>
         <FormField.Description>
-          Format headers for each user to fill in their individual credentials.
-          Use placeholders like{" "}
+          {isJa
+            ? "各ユーザーが個別の認証情報を入力できるようにヘッダーを設定します。"
+            : "Format headers for each user to fill in their individual credentials."}{" "}
+          {isJa ? "プレースホルダー例:" : "Use placeholders like"}{" "}
           <Text text03 secondaryMono className="inline">
             {"{api_key}"}
           </Text>{" "}
-          or{" "}
+          {isJa ? "または" : "or"}{" "}
           <Text text03 secondaryMono className="inline">
             {"{user_email}"}
           </Text>
-          . Users will be prompted to provide values for placeholders (except
-          user_email).
+          {isJa
+            ? " を使用できます。ユーザーは user_email を除くプレースホルダー値の入力を求められます。"
+            : ". Users will be prompted to provide values for placeholders (except user_email)."}
         </FormField.Description>
       </FormField>
 
@@ -141,11 +149,12 @@ export function PerUserAuthConfig({
               <SvgUser className="w-4 h-4 stroke-text-04 mt-0.5" />
               <div className="flex flex-col gap-1">
                 <Text text04 secondaryAction as="p">
-                  Only for your own account
+                  {isJa ? "自分のアカウントのみ" : "Only for your own account"}
                 </Text>
                 <Text text03 secondaryBody as="p">
-                  The following credentials will not be shared with your
-                  organization.
+                  {isJa
+                    ? "以下の認証情報は組織内では共有されません。"
+                    : "The following credentials will not be shared with your organization."}
                 </Text>
               </div>
             </div>
@@ -166,9 +175,11 @@ export function PerUserAuthConfig({
                     state="idle"
                   >
                     <FormField.Label>
-                      {field
-                        .replace(/_/g, " ")
-                        .replace(/\b\w/g, (l) => l.toUpperCase())}
+                      {isJa
+                        ? field.replace(/_/g, " ")
+                        : field
+                            .replace(/_/g, " ")
+                            .replace(/\b\w/g, (l) => l.toUpperCase())}
                     </FormField.Label>
                     <FormField.Control asChild>
                       <InputTypeIn
@@ -178,7 +189,11 @@ export function PerUserAuthConfig({
                         onChange={(e) =>
                           updateUserCredential(field, e.target.value)
                         }
-                        placeholder={`Enter ${field.replace(/_/g, " ")}`}
+                        placeholder={
+                          isJa
+                            ? `${field.replace(/_/g, " ")} を入力`
+                            : `Enter ${field.replace(/_/g, " ")}`
+                        }
                       />
                     </FormField.Control>
                   </FormField>
diff --git a/web/src/sections/actions/ToolsList.tsx b/web/src/sections/actions/ToolsList.tsx
index 425f46b0ae..d86871b40a 100644
--- a/web/src/sections/actions/ToolsList.tsx
+++ b/web/src/sections/actions/ToolsList.tsx
@@ -2,37 +2,28 @@
 
 import React from "react";
 import { cn } from "@opal/utils";
-import Text from "@/refresh-components/texts/Text";
 import { Button } from "@opal/components";
+import { SvgEye, SvgXCircle } from "@opal/icons";
+
+import Text from "@/refresh-components/texts/Text";
 import FadingEdgeContainer from "@/refresh-components/FadingEdgeContainer";
 import ToolItemSkeleton from "@/sections/actions/skeleton/ToolItemSkeleton";
 import EnabledCount from "@/refresh-components/EnabledCount";
-import { SvgEye, SvgXCircle } from "@opal/icons";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export interface ToolsListProps {
-  // Loading state
   isFetching?: boolean;
-
-  // Tool count for footer
   totalCount?: number;
   enabledCount?: number;
   showOnlyEnabled?: boolean;
   onToggleShowOnlyEnabled?: () => void;
   onUpdateToolsStatus?: (enabled: boolean) => void;
-
-  // Empty state of filtered tools
   isEmpty?: boolean;
   searchQuery?: string;
   emptyMessage?: string;
   emptySearchMessage?: string;
-
-  // Content
   children?: React.ReactNode;
-
-  // Left action (for refresh button and last verified text)
   leftAction?: React.ReactNode;
-
-  // Styling
   className?: string;
 }
 
@@ -45,12 +36,17 @@ const ToolsList: React.FC<ToolsListProps> = ({
   onUpdateToolsStatus,
   isEmpty = false,
   searchQuery,
-  emptyMessage = "No tools available",
-  emptySearchMessage = "No tools found",
+  emptyMessage,
+  emptySearchMessage,
   children,
   leftAction,
   className,
 }) => {
+  const { locale, t } = useLocale();
+  const resolvedEmptyMessage =
+    emptyMessage ?? t(locale, "tools.noToolsAvailable");
+  const resolvedEmptySearchMessage =
+    emptySearchMessage ?? t(locale, "tools.noToolsFound");
   const showFooter =
     totalCount !== undefined && enabledCount !== undefined && totalCount > 0;
 
@@ -70,7 +66,7 @@ const ToolsList: React.FC<ToolsListProps> = ({
         ) : isEmpty ? (
           <div className="flex items-center justify-center w-full py-8">
             <Text as="p" text03 mainUiBody>
-              {searchQuery ? emptySearchMessage : emptyMessage}
+              {searchQuery ? resolvedEmptySearchMessage : resolvedEmptyMessage}
             </Text>
           </div>
         ) : (
@@ -78,14 +74,11 @@ const ToolsList: React.FC<ToolsListProps> = ({
         )}
       </FadingEdgeContainer>
 
-      {/* Footer showing enabled tool count with filter toggle */}
-      {showFooter && !(totalCount === 0) && !isFetching && (
+      {showFooter && !isFetching && (
         <div className="pt-2 px-2">
           <div className="flex items-center justify-between gap-2 w-full">
-            {/* Left action area */}
             {leftAction}
 
-            {/* Right action area */}
             <div className="flex items-center gap-1 ml-auto">
               {enabledCount > 0 && (
                 <EnabledCount
@@ -102,12 +95,14 @@ const ToolsList: React.FC<ToolsListProps> = ({
                   onClick={onToggleShowOnlyEnabled}
                   interaction={showOnlyEnabled ? "hover" : "rest"}
                   tooltip={
-                    showOnlyEnabled ? "Show all tools" : "Show only enabled"
+                    showOnlyEnabled
+                      ? t(locale, "tools.showAllTools")
+                      : t(locale, "tools.showOnlyEnabled")
                   }
                   aria-label={
                     showOnlyEnabled
-                      ? "Show all tools"
-                      : "Show only enabled tools"
+                      ? t(locale, "tools.showAllTools")
+                      : t(locale, "tools.showOnlyEnabledTools")
                   }
                 />
               )}
@@ -117,8 +112,8 @@ const ToolsList: React.FC<ToolsListProps> = ({
                   prominence="tertiary"
                   size="sm"
                   onClick={() => onUpdateToolsStatus(false)}
-                  tooltip="Disable all tools"
-                  aria-label="Disable all tools"
+                  tooltip={t(locale, "tools.disableAllTools")}
+                  aria-label={t(locale, "tools.disableAllTools")}
                 />
               )}
               {onUpdateToolsStatus && enabledCount === 0 && (
@@ -126,7 +121,7 @@ const ToolsList: React.FC<ToolsListProps> = ({
                   prominence="tertiary"
                   onClick={() => onUpdateToolsStatus(true)}
                 >
-                  Enable all
+                  {t(locale, "tools.enableAll")}
                 </Button>
               )}
             </div>
@@ -136,6 +131,7 @@ const ToolsList: React.FC<ToolsListProps> = ({
     </>
   );
 };
+
 ToolsList.displayName = "ToolsList";
 
 export default ToolsList;
diff --git a/web/src/sections/actions/ToolsSection.tsx b/web/src/sections/actions/ToolsSection.tsx
index bc0755ced5..f3751b8a9f 100644
--- a/web/src/sections/actions/ToolsSection.tsx
+++ b/web/src/sections/actions/ToolsSection.tsx
@@ -2,9 +2,10 @@
 
 import React from "react";
 import { cn } from "@opal/utils";
-import { Button } from "@opal/components";
-import { InputTypeIn } from "@opal/components";
+import { Button, InputTypeIn } from "@opal/components";
 import { SvgFold } from "@opal/icons";
+import { useLocale } from "@/providers/LocaleProvider";
+
 interface ToolsSectionProps {
   onFold?: () => void;
   searchQuery: string;
@@ -18,6 +19,8 @@ const ToolsSection: React.FC<ToolsSectionProps> = ({
   onSearchQueryChange,
   className,
 }) => {
+  const { locale, t } = useLocale();
+
   const handleSearchChange = (e: React.ChangeEvent<HTMLInputElement>) => {
     onSearchQueryChange(e.target.value);
   };
@@ -25,11 +28,10 @@ const ToolsSection: React.FC<ToolsSectionProps> = ({
   return (
     <div className={cn("w-full", className)}>
       <div className="flex gap-1 items-center w-full transition-all duration-300 ease-in-out px-2 pb-2">
-        {/* Search Bar */}
         <div className="flex-1 min-w-[160px]">
           <InputTypeIn
-            placeholder="Search tools…"
-            aria-label="Search tools"
+            placeholder={t(locale, "tools.searchTools")}
+            aria-label={t(locale, "tools.searchTools")}
             value={searchQuery}
             onChange={handleSearchChange}
             searchIcon
@@ -37,12 +39,10 @@ const ToolsSection: React.FC<ToolsSectionProps> = ({
           />
         </div>
 
-        {/* Actions */}
         <div className="flex gap-1 items-center p-1">
-          {/* Fold Button */}
           {onFold && (
             <Button prominence="tertiary" onClick={onFold} rightIcon={SvgFold}>
-              Fold
+              {t(locale, "common.fold")}
             </Button>
           )}
         </div>
@@ -52,4 +52,5 @@ const ToolsSection: React.FC<ToolsSectionProps> = ({
 };
 
 ToolsSection.displayName = "ToolsSection";
+
 export default ToolsSection;
diff --git a/web/src/sections/actions/modals/AddMCPServerModal.tsx b/web/src/sections/actions/modals/AddMCPServerModal.tsx
index a82694c299..4e92fd39ef 100644
--- a/web/src/sections/actions/modals/AddMCPServerModal.tsx
+++ b/web/src/sections/actions/modals/AddMCPServerModal.tsx
@@ -1,6 +1,6 @@
 "use client";
 
-import { useState } from "react";
+import { useMemo, useState } from "react";
 import { Formik, Form } from "formik";
 import * as Yup from "yup";
 import Modal from "@/refresh-components/Modal";
@@ -20,6 +20,7 @@ import { ModalCreationInterface } from "@/refresh-components/contexts/ModalConte
 import { SvgCheckCircle, SvgServer, SvgUnplug } from "@opal/icons";
 import { Section } from "@/layouts/general-layouts";
 import Text from "@/refresh-components/texts/Text";
+import { useLocale } from "@/providers/LocaleProvider";
 
 interface AddMCPServerModalProps {
   skipOverlay?: boolean;
@@ -32,14 +33,6 @@ interface AddMCPServerModalProps {
   mutateMcpServers?: () => Promise<void>;
 }
 
-const validationSchema = Yup.object().shape({
-  name: Yup.string().required("Server name is required"),
-  description: Yup.string(),
-  server_url: Yup.string()
-    .url("Must be a valid URL")
-    .required("Server URL is required"),
-});
-
 export default function AddMCPServerModal({
   skipOverlay = false,
   activeServer,
@@ -50,7 +43,22 @@ export default function AddMCPServerModal({
   mutateMcpServers,
 }: AddMCPServerModalProps) {
   const { isOpen, toggle } = useModal();
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
   const [isSubmitting, setIsSubmitting] = useState(false);
+  const validationSchema = useMemo(
+    () =>
+      Yup.object().shape({
+        name: Yup.string().required(
+          isJa ? "サーバー名は必須です" : "Server name is required"
+        ),
+        description: Yup.string(),
+        server_url: Yup.string()
+          .url(isJa ? "有効なURLを入力してください" : "Must be a valid URL")
+          .required(isJa ? "サーバーURLは必須です" : "Server URL is required"),
+      }),
+    [isJa]
+  );
 
   // Use activeServer from props
   const server = activeServer;
@@ -80,13 +88,21 @@ export default function AddMCPServerModal({
       if (isEditMode && server) {
         // Update existing server
         await updateMCPServer(server.id, values);
-        toast.success("MCP Server updated successfully");
+        toast.success(
+          isJa
+            ? "MCPサーバー設定を更新しました"
+            : "MCP Server updated successfully"
+        );
         await mutateMcpServers?.();
       } else {
         // Create new server
         const createdServer = await createMCPServer(values);
 
-        toast.success("MCP Server created successfully");
+        toast.success(
+          isJa
+            ? "MCPサーバーを追加しました"
+            : "MCP Server created successfully"
+        );
 
         await mutateMcpServers?.();
 
@@ -106,7 +122,9 @@ export default function AddMCPServerModal({
       toast.error(
         error instanceof Error
           ? error.message
-          : `Failed to ${isEditMode ? "update" : "create"} MCP server`
+          : isJa
+            ? `MCPサーバーの${isEditMode ? "更新" : "追加"}に失敗しました`
+            : `Failed to ${isEditMode ? "update" : "create"} MCP server`
       );
     } finally {
       setIsSubmitting(false);
@@ -135,32 +153,53 @@ export default function AddMCPServerModal({
             <Form>
               <Modal.Header
                 icon={SvgServer}
-                title={isEditMode ? "Manage MCP Server" : "Add MCP Server"}
+                title={
+                  isEditMode
+                    ? isJa
+                      ? "MCPサーバーを管理"
+                      : "Manage MCP Server"
+                    : isJa
+                      ? "MCPサーバーを追加"
+                      : "Add MCP Server"
+                }
                 description={
                   isEditMode
-                    ? "Update your MCP server configuration and manage authentication."
-                    : "Connect MCP (Model Context Protocol) server to add custom actions."
+                    ? isJa
+                      ? "MCPサーバーの設定を更新し、接続状態を管理します。"
+                      : "Update your MCP server configuration and manage authentication."
+                    : isJa
+                      ? "MCP (Model Context Protocol) サーバーに接続してカスタムアクションを追加します。"
+                      : "Connect MCP (Model Context Protocol) server to add custom actions."
                 }
                 onClose={() => handleModalClose(false)}
               />
 
               <Modal.Body>
-                <InputVertical withLabel="name" title="Server Name">
+                <InputVertical
+                  withLabel="name"
+                  title={isJa ? "サーバー名" : "Server Name"}
+                >
                   <InputTypeInField
                     name="name"
-                    placeholder="Name your MCP server"
+                    placeholder={
+                      isJa ? "MCPサーバー名を入力" : "Name your MCP server"
+                    }
                     autoFocus
                   />
                 </InputVertical>
 
                 <InputVertical
                   withLabel="description"
-                  title="Description"
-                  suffix="optional"
+                  title={isJa ? "説明" : "Description"}
+                  suffix={isJa ? "任意" : "optional"}
                 >
                   <InputTextAreaField
                     name="description"
-                    placeholder="More details about the MCP server"
+                    placeholder={
+                      isJa
+                        ? "MCPサーバーの詳細を入力"
+                        : "More details about the MCP server"
+                    }
                     rows={3}
                   />
                 </InputVertical>
@@ -169,8 +208,12 @@ export default function AddMCPServerModal({
 
                 <InputVertical
                   withLabel="server_url"
-                  title="MCP Server URL"
-                  subDescription="Only connect to servers you trust. You are responsible for actions taken with this connection and keeping your tools updated."
+                  title={isJa ? "MCPサーバーURL" : "MCP Server URL"}
+                  subDescription={
+                    isJa
+                      ? "信頼できるサーバーにのみ接続してください。この接続で実行される操作とツールの最新状態の維持は利用者の責任です。"
+                      : "Only connect to servers you trust. You are responsible for actions taken with this connection and keeping your tools updated."
+                  }
                 >
                   <InputTypeInField
                     name="server_url"
@@ -196,14 +239,24 @@ export default function AddMCPServerModal({
                           width="fit"
                         >
                           <SvgCheckCircle className="w-4 h-4 stroke-status-success-05" />
-                          <Text>Authenticated &amp; Connected</Text>
+                          <Text>
+                            {isJa
+                              ? "認証済み・接続済み"
+                              : "Authenticated & Connected"}
+                          </Text>
                         </Section>
                         <Text secondaryBody text03>
                           {server.auth_type === "OAUTH"
-                            ? `OAuth connected to ${server.owner}`
+                            ? isJa
+                              ? `${server.owner} にOAuth接続済み`
+                              : `OAuth connected to ${server.owner}`
                             : server.auth_type === "API_TOKEN"
-                              ? "API token configured"
-                              : "Connected"}
+                              ? isJa
+                                ? "APIトークン設定済み"
+                                : "API token configured"
+                              : isJa
+                                ? "接続済み"
+                                : "Connected"}
                         </Text>
                       </Section>
                       <Section
@@ -216,7 +269,7 @@ export default function AddMCPServerModal({
                           icon={SvgUnplug}
                           prominence="tertiary"
                           type="button"
-                          tooltip="Disconnect Server"
+                          tooltip={isJa ? "サーバーを切断" : "Disconnect Server"}
                           onClick={handleDisconnectClick}
                         />
                         <Button
@@ -228,7 +281,7 @@ export default function AddMCPServerModal({
                             handleAuthenticate(server.id);
                           }}
                         >
-                          Edit Configs
+                          {isJa ? "設定を編集" : "Edit Configs"}
                         </Button>
                       </Section>
                     </Section>
@@ -242,7 +295,7 @@ export default function AddMCPServerModal({
                   type="button"
                   onClick={() => handleModalClose(false)}
                 >
-                  Cancel
+                  {isJa ? "キャンセル" : "Cancel"}
                 </Button>
                 <Button
                   disabled={isSubmitting || !isValid || !dirty}
@@ -250,11 +303,19 @@ export default function AddMCPServerModal({
                 >
                   {isSubmitting
                     ? isEditMode
-                      ? "Saving..."
-                      : "Adding..."
+                      ? isJa
+                        ? "保存中..."
+                        : "Saving..."
+                      : isJa
+                        ? "追加中..."
+                        : "Adding..."
                     : isEditMode
-                      ? "Save Changes"
-                      : "Add Server"}
+                      ? isJa
+                        ? "変更を保存"
+                        : "Save Changes"
+                      : isJa
+                        ? "サーバーを追加"
+                        : "Add Server"}
                 </Button>
               </Modal.Footer>
             </Form>
diff --git a/web/src/sections/actions/modals/AddOpenAPIActionModal.tsx b/web/src/sections/actions/modals/AddOpenAPIActionModal.tsx
index 68e1c6167d..36b9b84f36 100644
--- a/web/src/sections/actions/modals/AddOpenAPIActionModal.tsx
+++ b/web/src/sections/actions/modals/AddOpenAPIActionModal.tsx
@@ -34,6 +34,7 @@ import InfoBlock from "@/refresh-components/messages/InfoBlock";
 import { getActionIcon } from "@/lib/tools/mcpUtils";
 import { Section } from "@/layouts/general-layouts";
 import { EmptyMessageCard } from "@opal/components";
+import { useLocale } from "@/providers/LocaleProvider";
 
 interface AddOpenAPIActionModalProps {
   skipOverlay?: boolean;
@@ -82,6 +83,8 @@ function FormContent({
   onEditAuthentication,
   onDisconnectTool,
 }: FormContentProps) {
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
   const { values, setFieldValue, setFieldError, dirty, isSubmitting } =
     useFormikContext<OpenAPIActionFormValues>();
 
@@ -104,9 +107,12 @@ function FormContent({
       setFieldValue("definition", formatted);
       setFieldError("definition", "");
     } catch {
-      setFieldError("definition", "Invalid JSON format");
+      setFieldError(
+        "definition",
+        isJa ? "JSON形式が正しくありません" : "Invalid JSON format"
+      );
     }
-  }, [values.definition, setFieldValue, setFieldError]);
+  }, [isJa, values.definition, setFieldValue, setFieldError]);
 
   const validateDefinition = useCallback(
     async (
@@ -142,10 +148,13 @@ function FormContent({
         }
       } catch {
         setMethodSpecs(null);
-        setFieldError("definition", "Invalid JSON format");
+        setFieldError(
+          "definition",
+          isJa ? "JSON形式が正しくありません" : "Invalid JSON format"
+        );
       }
     },
-    []
+    [isJa]
   );
 
   const debouncedValidateDefinition = useMemo(
@@ -153,17 +162,35 @@ function FormContent({
     [validateDefinition]
   );
 
-  const modalTitle = isEditMode ? "Edit OpenAPI action" : "Add OpenAPI action";
+  const modalTitle = isEditMode
+    ? isJa
+      ? "OpenAPIアクションを編集"
+      : "Edit OpenAPI action"
+    : isJa
+      ? "OpenAPIアクションを追加"
+      : "Add OpenAPI action";
   const modalDescription = isEditMode
-    ? "Update the OpenAPI schema for this action."
-    : "Add OpenAPI schema to add custom actions.";
+    ? isJa
+      ? "このアクションのOpenAPIスキーマを更新します。"
+      : "Update the OpenAPI schema for this action."
+    : isJa
+      ? "OpenAPIスキーマを追加してカスタムアクションを作成します。"
+      : "Add OpenAPI schema to add custom actions.";
   const primaryButtonLabel = isSubmitting
     ? isEditMode
-      ? "Saving..."
-      : "Adding..."
+      ? isJa
+        ? "保存中..."
+        : "Saving..."
+      : isJa
+        ? "追加中..."
+        : "Adding..."
     : isEditMode
-      ? "Save Changes"
-      : "Add Action";
+      ? isJa
+        ? "変更を保存"
+        : "Save Changes"
+      : isJa
+        ? "アクションを追加"
+        : "Add Action";
 
   const hasOAuthConfig = Boolean(existingTool?.oauth_config_id);
   const hasCustomHeaders =
@@ -178,17 +205,25 @@ function FormContent({
     }
     if (hasOAuthConfig) {
       return existingTool.oauth_config_name
-        ? `OAuth connected via ${existingTool.oauth_config_name}`
-        : "OAuth authentication configured";
+        ? isJa
+          ? `${existingTool.oauth_config_name} でOAuth接続済み`
+          : `OAuth connected via ${existingTool.oauth_config_name}`
+        : isJa
+          ? "OAuth認証設定済み"
+          : "OAuth authentication configured";
     }
     if (hasCustomHeaders) {
-      return "Custom authentication headers configured";
+      return isJa
+        ? "カスタム認証ヘッダー設定済み"
+        : "Custom authentication headers configured";
     }
     if (hasPassthroughAuth) {
-      return "Passthrough authentication enabled";
+      return isJa
+        ? "パススルー認証が有効です"
+        : "Passthrough authentication enabled";
     }
     return "";
-  }, [existingTool, hasOAuthConfig, hasCustomHeaders, hasPassthroughAuth]);
+  }, [existingTool, hasOAuthConfig, hasCustomHeaders, hasPassthroughAuth, isJa]);
 
   const showAuthenticationStatus = Boolean(
     isEditMode && existingTool?.enabled && hasAuthenticationConfigured
@@ -236,13 +271,19 @@ function FormContent({
       <Modal.Body>
         <InputVertical
           withLabel="definition"
-          title="OpenAPI Schema Definition"
+          title={isJa ? "OpenAPIスキーマ定義" : "OpenAPI Schema Definition"}
           subDescription={markdown(
-            `Specify an OpenAPI schema that defines the APIs you want to make available as part of this action. ` +
-              `You can use the placeholders \`CHAT_SESSION_ID\`, \`MESSAGE_ID\`, \`USER_ID\`, and \`USER_EMAIL\` ` +
-              `anywhere in the schema (e.g. server URL, paths, parameter defaults) and they will be replaced with the ` +
-              `current request's values at call time. ` +
-              `Learn more about [OpenAPI actions](${DOCS_ADMINS_PATH}/actions/openapi).`
+            isJa
+              ? `このアクションで利用可能にしたいAPIを定義するOpenAPIスキーマを指定してください。` +
+                ` スキーマ内の任意の場所（例: サーバーURL、パス、パラメータの既定値）で` +
+                ` \`CHAT_SESSION_ID\`、\`MESSAGE_ID\`、\`USER_ID\`、\`USER_EMAIL\` のプレースホルダーを使用できます。` +
+                ` 呼び出し時に現在のリクエスト値へ置き換えられます。` +
+                ` 詳細は [OpenAPIアクション](${DOCS_ADMINS_PATH}/actions/openapi) を参照してください。`
+              : `Specify an OpenAPI schema that defines the APIs you want to make available as part of this action. ` +
+                `You can use the placeholders \`CHAT_SESSION_ID\`, \`MESSAGE_ID\`, \`USER_ID\`, and \`USER_EMAIL\` ` +
+                `anywhere in the schema (e.g. server URL, paths, parameter defaults) and they will be replaced with the ` +
+                `current request's values at call time. ` +
+                `Learn more about [OpenAPI actions](${DOCS_ADMINS_PATH}/actions/openapi).`
           )}
         >
           <Hoverable.Root group="definitionField" width="full">
@@ -258,13 +299,13 @@ function FormContent({
                         prominence="tertiary"
                         size="sm"
                         getCopyText={() => values.definition}
-                        tooltip="Copy definition"
+                        tooltip={isJa ? "定義をコピー" : "Copy definition"}
                       />
                       <Button
                         prominence="tertiary"
                         size="sm"
                         icon={SvgBracketCurly}
-                        tooltip="Format definition"
+                        tooltip={isJa ? "定義を整形" : "Format definition"}
                         onClick={handleFormat}
                       />
                     </div>
@@ -274,7 +315,11 @@ function FormContent({
               <InputTextAreaField
                 name="definition"
                 rows={14}
-                placeholder="Enter your OpenAPI schema here"
+                placeholder={
+                  isJa
+                    ? "ここにOpenAPIスキーマを入力してください"
+                    : "Enter your OpenAPI schema here"
+                }
                 className="font-main-ui-mono"
               />
             </div>
@@ -296,7 +341,11 @@ function FormContent({
               <InfoBlock
                 icon={SvgAlertCircle}
                 title={url || ""}
-                description="URL found in the schema. Only connect to servers you trust."
+                description={
+                  isJa
+                    ? "スキーマ内でURLが見つかりました。信頼できるサーバーにのみ接続してください。"
+                    : "URL found in the schema. Only connect to servers you trust."
+                }
               />
             )}
             <Divider paddingParallel="fit" paddingPerpendicular="fit" />
@@ -305,7 +354,9 @@ function FormContent({
                 <ToolItem
                   key={`${method.method}-${method.path}-${method.name}`}
                   name={method.name}
-                  description={method.summary || "No summary provided"}
+                  description={
+                    method.summary || (isJa ? "説明はありません" : "No summary provided")
+                  }
                   variant="openapi"
                   openApiMetadata={{
                     method: method.method,
@@ -318,9 +369,13 @@ function FormContent({
         ) : (
           <EmptyMessageCard
             sizePreset="main-ui"
-            title="No Actions Found"
+            title={isJa ? "アクションが見つかりません" : "No Actions Found"}
             icon={SvgActions}
-            description="Provide OpenAPI schema to preview actions here."
+            description={
+              isJa
+                ? "OpenAPIスキーマを入力すると、ここにアクションのプレビューが表示されます。"
+                : "Provide OpenAPI schema to preview actions here."
+            }
           />
         )}
 
@@ -341,8 +396,12 @@ function FormContent({
                 <SvgCheckCircle className="w-4 h-4 stroke-status-success-05" />
                 <Text>
                   {existingTool?.enabled
-                    ? "Authenticated & Enabled"
-                    : "Authentication configured"}
+                    ? isJa
+                      ? "認証済み・有効"
+                      : "Authenticated & Enabled"
+                    : isJa
+                      ? "認証設定済み"
+                      : "Authentication configured"}
                 </Text>
               </Section>
               {authenticationDescription && (
@@ -361,7 +420,7 @@ function FormContent({
                 icon={SvgUnplug}
                 prominence="tertiary"
                 type="button"
-                tooltip="Disable action"
+                tooltip={isJa ? "アクションを無効化" : "Disable action"}
                 onClick={() => {
                   if (!existingTool || !onDisconnectTool) {
                     return;
@@ -375,7 +434,7 @@ function FormContent({
                 type="button"
                 onClick={handleEditAuthenticationClick}
               >
-                Edit Configs
+                {isJa ? "設定を編集" : "Edit Configs"}
               </Button>
             </Section>
           </Section>
@@ -389,7 +448,7 @@ function FormContent({
           type="button"
           onClick={handleClose}
         >
-          Cancel
+          {isJa ? "キャンセル" : "Cancel"}
         </Button>
         <Button disabled={isSubmitting || !dirty} type="submit">
           {primaryButtonLabel}
@@ -409,6 +468,8 @@ export default function AddOpenAPIActionModal({
   onDisconnectTool,
 }: AddOpenAPIActionModalProps) {
   const { isOpen, toggle } = useModal();
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
 
   const handleModalClose = useCallback(
     (open: boolean) => {
@@ -439,7 +500,11 @@ export default function AddOpenAPIActionModal({
       parsedDefinition = parseJsonWithTrailingCommas(values.definition);
     } catch (error) {
       console.error("Error parsing OpenAPI definition:", error);
-      toast.error("Invalid JSON format in OpenAPI schema definition");
+      toast.error(
+        isJa
+          ? "OpenAPIスキーマ定義のJSON形式が正しくありません"
+          : "Invalid JSON format in OpenAPI schema definition"
+      );
       return;
     }
 
@@ -475,7 +540,11 @@ export default function AddOpenAPIActionModal({
         if (response.error) {
           toast.error(response.error);
         } else {
-          toast.success("OpenAPI action updated successfully");
+          toast.success(
+            isJa
+              ? "OpenAPIアクションを更新しました"
+              : "OpenAPI action updated successfully"
+          );
           handleClose();
           if (response.data && onUpdate) {
             onUpdate(response.data);
@@ -483,7 +552,11 @@ export default function AddOpenAPIActionModal({
         }
       } catch (error) {
         console.error("Error updating OpenAPI action:", error);
-        toast.error("Failed to update OpenAPI action");
+        toast.error(
+          isJa
+            ? "OpenAPIアクションの更新に失敗しました"
+            : "Failed to update OpenAPI action"
+        );
       }
       return;
     }
@@ -500,7 +573,11 @@ export default function AddOpenAPIActionModal({
       if (response.error) {
         toast.error(response.error);
       } else {
-        toast.success("OpenAPI action created successfully");
+        toast.success(
+          isJa
+            ? "OpenAPIアクションを追加しました"
+            : "OpenAPI action created successfully"
+        );
         handleClose();
         if (response.data && onSuccess) {
           onSuccess(response.data);
@@ -508,7 +585,11 @@ export default function AddOpenAPIActionModal({
       }
     } catch (error) {
       console.error("Error creating OpenAPI action:", error);
-      toast.error("Failed to create OpenAPI action");
+      toast.error(
+        isJa
+          ? "OpenAPIアクションの追加に失敗しました"
+          : "Failed to create OpenAPI action"
+      );
     }
   };
 
diff --git a/web/src/sections/actions/modals/DisconnectEntityModal.tsx b/web/src/sections/actions/modals/DisconnectEntityModal.tsx
index 6dff38fda1..1066838c76 100644
--- a/web/src/sections/actions/modals/DisconnectEntityModal.tsx
+++ b/web/src/sections/actions/modals/DisconnectEntityModal.tsx
@@ -7,6 +7,7 @@ import Text from "@/refresh-components/texts/Text";
 import { cn } from "@opal/utils";
 import { markdown } from "@opal/utils";
 import { SvgUnplug } from "@opal/icons";
+import { useLocale } from "@/providers/LocaleProvider";
 interface DisconnectEntityModalProps {
   isOpen: boolean;
   onClose: () => void;
@@ -27,6 +28,8 @@ export default function DisconnectEntityModal({
   skipOverlay = false,
 }: DisconnectEntityModalProps) {
   const disconnectButtonRef = useRef<HTMLButtonElement>(null);
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
 
   if (!name) return null;
 
@@ -52,17 +55,22 @@ export default function DisconnectEntityModal({
           icon={({ className }) => (
             <SvgUnplug className={cn(className, "stroke-action-danger-05")} />
           )}
-          title={markdown(`Disconnect *${name}*`)}
+          title={
+            isJa
+              ? markdown(`*${name}* を切断`)
+              : markdown(`Disconnect *${name}*`)
+          }
           onClose={onClose}
         />
 
         <Modal.Body>
           <Text as="p" text03 mainUiBody>
-            All tools connected to {name} will stop working. You can reconnect
-            to this server later if needed.
+            {isJa
+              ? `${name} に接続されているすべてのツールが停止します。必要に応じて後で再接続できます。`
+              : `All tools connected to ${name} will stop working. You can reconnect to this server later if needed.`}
           </Text>
           <Text as="p" text03 mainUiBody>
-            Are you sure you want to proceed?
+            {isJa ? "このまま続行しますか？" : "Are you sure you want to proceed?"}
           </Text>
         </Modal.Body>
 
@@ -72,7 +80,7 @@ export default function DisconnectEntityModal({
             prominence="secondary"
             onClick={onClose}
           >
-            Cancel
+            {isJa ? "キャンセル" : "Cancel"}
           </Button>
           {onConfirmDisconnectAndDelete && (
             <Button
@@ -81,7 +89,7 @@ export default function DisconnectEntityModal({
               prominence="secondary"
               onClick={onConfirmDisconnectAndDelete}
             >
-              Disconnect &amp; Delete
+              {isJa ? "切断して削除" : "Disconnect & Delete"}
             </Button>
           )}
           <Button
@@ -90,7 +98,13 @@ export default function DisconnectEntityModal({
             onClick={onConfirmDisconnect}
             ref={disconnectButtonRef}
           >
-            {isDisconnecting ? "Disconnecting..." : "Disconnect"}
+            {isDisconnecting
+              ? isJa
+                ? "切断中..."
+                : "Disconnecting..."
+              : isJa
+                ? "切断"
+                : "Disconnect"}
           </Button>
         </Modal.Footer>
       </Modal.Content>
diff --git a/web/src/sections/actions/modals/MCPAuthenticationModal.tsx b/web/src/sections/actions/modals/MCPAuthenticationModal.tsx
index 0325355ae9..afb5b720e4 100644
--- a/web/src/sections/actions/modals/MCPAuthenticationModal.tsx
+++ b/web/src/sections/actions/modals/MCPAuthenticationModal.tsx
@@ -38,6 +38,7 @@ import { toast } from "@/hooks/useToast";
 import { SvgArrowExchange } from "@opal/icons";
 import { useAuthType } from "@/lib/hooks";
 import { AuthType } from "@/lib/constants";
+import { useLocale } from "@/providers/LocaleProvider";
 
 interface MCPAuthenticationModalProps {
   mcpServer: MCPServer | null;
@@ -143,6 +144,9 @@ export default function MCPAuthenticationModal({
   onTriggerFetchTools,
   mutateMcpServers,
 }: MCPAuthenticationModalProps) {
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
+  const tr = (en: string, ja: string) => (isJa ? ja : en);
   const { isOpen, toggle } = useModal();
   const [activeAuthTab, setActiveAuthTab] = useState<"per-user" | "admin">(
     "per-user"
@@ -325,7 +329,11 @@ export default function MCPAuthenticationModal({
       try {
         const parsed = JSON.parse(values.oauth_additional_auth_params);
         if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) {
-          throw new Error("Additional auth params must be a JSON object");
+          throw new Error(
+            isJa
+              ? "追加認証パラメータは JSON オブジェクトである必要があります"
+              : "Additional auth params must be a JSON object"
+          );
         }
         parsedAdditionalAuthParams = Object.fromEntries(
           Object.entries(parsed).map(([key, value]) => [key, String(value)])
@@ -333,8 +341,12 @@ export default function MCPAuthenticationModal({
       } catch (error) {
         throw new Error(
           error instanceof Error
-            ? `Invalid additional auth params JSON: ${error.message}`
-            : "Invalid additional auth params JSON"
+            ? isJa
+              ? `追加認証パラメータ JSON が無効です: ${error.message}`
+              : `Invalid additional auth params JSON: ${error.message}`
+            : isJa
+              ? "追加認証パラメータ JSON が無効です"
+              : "Invalid additional auth params JSON"
         );
       }
     }
@@ -406,7 +418,13 @@ export default function MCPAuthenticationModal({
         await upsertMCPServer(serverData);
 
       if (serverError || !serverResult) {
-        throw new Error(serverError || "Failed to save server configuration");
+        throw new Error(
+          serverError ||
+            tr(
+              "Failed to save server configuration",
+              "サーバー設定の保存に失敗しました"
+            )
+        );
       }
 
       // Step 2: Update status to AWAITING_AUTH after successful config save
@@ -440,7 +458,12 @@ export default function MCPAuthenticationModal({
           // Refresh server list so latest status is visible after auth failure
           await mutateMcpServers();
           toggle(false);
-          throw new Error("Failed to initiate OAuth: " + error.detail);
+          throw new Error(
+            tr(
+              "Failed to initiate OAuth: " + error.detail,
+              "OAuth の開始に失敗しました: " + error.detail
+            )
+          );
         }
 
         const { oauth_url } = await oauthResponse.json();
@@ -455,18 +478,21 @@ export default function MCPAuthenticationModal({
         }
         toggle(false);
       }
-    } catch (error) {
-      console.error("Error saving authentication:", error);
-      // Ensure UI reflects latest status after any auth/config failure
-      await mutateMcpServers();
-      toast.error(
-        error instanceof Error
-          ? error.message
-          : "Failed to save authentication configuration"
-      );
-    } finally {
-      setIsSubmitting(false);
-    }
+      } catch (error) {
+        console.error("Error saving authentication:", error);
+        // Ensure UI reflects latest status after any auth/config failure
+        await mutateMcpServers();
+        toast.error(
+          error instanceof Error
+            ? error.message
+            : tr(
+                "Failed to save authentication configuration",
+                "認証設定の保存に失敗しました"
+              )
+        );
+      } finally {
+        setIsSubmitting(false);
+      }
   };
 
   return (
@@ -476,10 +502,18 @@ export default function MCPAuthenticationModal({
           icon={SvgArrowExchange}
           title={
             mcpServer
-              ? markdown(`Authenticate *${mcpServer.name}*`)
-              : "Authenticate MCP Server"
+              ? markdown(
+                  tr(
+                    `Authenticate *${mcpServer.name}*`,
+                    `*${mcpServer.name}* を認証`
+                  )
+                )
+              : tr("Authenticate MCP Server", "MCP サーバーを認証")
           }
-          description="Authenticate your connection to start using the MCP server."
+          description={tr(
+            "Authenticate your connection to start using the MCP server.",
+            "MCP サーバーを使用するために接続を認証してください。"
+          )}
         />
 
         <Formik<MCPAuthFormValues>
@@ -520,7 +554,9 @@ export default function MCPAuthenticationModal({
                             : "idle"
                       }
                     >
-                      <FormField.Label>Authentication Method</FormField.Label>
+                      <FormField.Label>
+                        {tr("Authentication Method", "認証方式")}
+                      </FormField.Label>
                       <FormField.Control asChild>
                         <InputSelect
                           value={values.auth_type}
@@ -555,35 +591,44 @@ export default function MCPAuthenticationModal({
                           }}
                         >
                           <InputSelect.Trigger
-                            placeholder="Select method"
+                            placeholder={tr("Select method", "方式を選択")}
                             data-testid="mcp-auth-method-select"
                           />
                           <InputSelect.Content>
                             <InputSelect.Item
                               value={MCPAuthenticationType.OAUTH}
-                              description="Each user need to authenticate via OAuth with their own credentials."
+                              description={tr(
+                                "Each user need to authenticate via OAuth with their own credentials.",
+                                "各ユーザーが自分の認証情報で OAuth 認証します。"
+                              )}
                             >
                               OAuth
                             </InputSelect.Item>
                             {isOAuthEnabled && (
                               <InputSelect.Item
                                 value={MCPAuthenticationType.PT_OAUTH}
-                                description="Forward the user's OAuth access token used to authenticate Onyx."
+                                description={tr(
+                                  "Forward the user's OAuth access token used to authenticate Onyx.",
+                                  "Onyx の認証に使うユーザーの OAuth アクセストークンをそのまま転送します。"
+                                )}
                               >
-                                OAuth Pass-through
+                                {tr("OAuth Pass-through", "OAuth パススルー")}
                               </InputSelect.Item>
                             )}
                             <InputSelect.Item
                               value={MCPAuthenticationType.API_TOKEN}
-                              description="Use per-user individual API key or organization-wide shared API key."
+                              description={tr(
+                                "Use per-user individual API key or organization-wide shared API key.",
+                                "ユーザー個別の API キーまたは組織共有の API キーを使用します。"
+                              )}
                             >
-                              API Key
+                              {tr("API Key", "API キー")}
                             </InputSelect.Item>
                             <InputSelect.Item
                               value={MCPAuthenticationType.NONE}
-                              description="Not Recommended"
+                              description={tr("Not Recommended", "非推奨")}
                             >
-                              None
+                              {tr("None", "なし")}
                             </InputSelect.Item>
                           </InputSelect.Content>
                         </InputSelect>
@@ -597,7 +642,7 @@ export default function MCPAuthenticationModal({
                     <Divider paddingPerpendicular="fit" />
                   </div>
 
-                  {/* OAuth Section */}
+                      {/* OAuth Section */}
                   {values.auth_type === MCPAuthenticationType.OAUTH && (
                     <div className="flex flex-col gap-4 px-2 py-2 bg-background-tint-00 rounded-12">
                       {/* OAuth Client ID */}
@@ -611,7 +656,12 @@ export default function MCPAuthenticationModal({
                               : "idle"
                         }
                       >
-                        <FormField.Label optional>Client ID</FormField.Label>
+                        <FormField.Label>
+                          {tr("Client ID", "クライアント ID")}
+                          <Text as="p" text03 mainUiMuted className="mx-0.5">
+                            {tr("(Optional)", "（任意）")}
+                          </Text>
+                        </FormField.Label>
                         <FormField.Control asChild>
                           <InputTypeIn
                             name="oauth_client_id"
@@ -638,8 +688,11 @@ export default function MCPAuthenticationModal({
                               : "idle"
                         }
                       >
-                        <FormField.Label optional>
-                          Client Secret
+                        <FormField.Label>
+                          {tr("Client Secret", "クライアントシークレット")}
+                          <Text as="p" text03 mainUiMuted className="mx-0.5">
+                            {tr("(Optional)", "（任意）")}
+                          </Text>
                         </FormField.Label>
                         <FormField.Control asChild>
                           <PasswordInputTypeIn
@@ -659,14 +712,16 @@ export default function MCPAuthenticationModal({
                       {/* Info Text */}
                       <div className="flex flex-col gap-2">
                         <Text as="p" text03 secondaryBody>
-                          Client ID and secret are optional if the server
-                          connection supports Dynamic Client Registration (DCR).
+                          {tr(
+                            "Client ID and secret are optional if the server connection supports Dynamic Client Registration (DCR).",
+                            "サーバー接続が Dynamic Client Registration (DCR) をサポートしている場合、クライアント ID とシークレットは任意です。"
+                          )}
                         </Text>
                         <Text as="p" text03 secondaryBody>
-                          If your server does not support DCR, you need register
-                          your Onyx instance with the server provider to obtain
-                          these credentials first. Make sure to grant Onyx
-                          necessary scopes/permissions for your actions.
+                          {tr(
+                            "If your server does not support DCR, you need register your Onyx instance with the server provider to obtain these credentials first. Make sure to grant Onyx necessary scopes/permissions for your actions.",
+                            "DCR に対応していない場合は、まずサーバープロバイダー側で Onyx インスタンスを登録し、これらの認証情報を取得してください。必要なスコープ/権限も付与してください。"
+                          )}
                         </Text>
                         {/* Redirect URI */}
                         <div className="flex items-center gap-1 w-full">
@@ -676,9 +731,9 @@ export default function MCPAuthenticationModal({
                             secondaryBody
                             className="whitespace-nowrap"
                           >
-                            Use{" "}
+                            {tr("Use", "使用する")}{" "}
                             <span className="font-secondary-action">
-                              redirect URI
+                              {tr("redirect URI", "リダイレクト URI")}
                             </span>
                             :
                           </Text>
@@ -691,7 +746,7 @@ export default function MCPAuthenticationModal({
                           </Text>
                           <CopyButton
                             getCopyText={() => redirectUri}
-                            tooltip="Copy redirect URI"
+                            tooltip={tr("Copy redirect URI", "リダイレクト URI をコピー")}
                             prominence="tertiary"
                             size="sm"
                           />
@@ -703,8 +758,11 @@ export default function MCPAuthenticationModal({
                         onOpenChange={setAdvancedOpen}
                       >
                         <SimpleCollapsible.Header
-                          title="Advanced"
-                          description="Configure a known OAuth provider with explicit authorization and token endpoints instead of automatic discovery."
+                          title={tr("Advanced", "詳細設定")}
+                          description={tr(
+                            "Configure a known OAuth provider with explicit authorization and token endpoints instead of automatic discovery.",
+                            "自動検出ではなく、認可エンドポイントとトークンエンドポイントを明示して既知の OAuth プロバイダーを設定します。"
+                          )}
                         />
                         <SimpleCollapsible.Content>
                           <Section alignItems="stretch" height="auto">
@@ -719,7 +777,9 @@ export default function MCPAuthenticationModal({
                                     : "idle"
                               }
                             >
-                              <FormField.Label>Provider Mode</FormField.Label>
+                              <FormField.Label>
+                                {tr("Provider Mode", "プロバイダーモード")}
+                              </FormField.Label>
                               <FormField.Control asChild>
                                 <InputSelect
                                   value={values.oauth_provider_mode}
@@ -727,23 +787,29 @@ export default function MCPAuthenticationModal({
                                     setFieldValue("oauth_provider_mode", value);
                                   }}
                                 >
-                                  <InputSelect.Trigger placeholder="Select mode" />
+                                  <InputSelect.Trigger placeholder={tr("Select mode", "モードを選択")} />
                                   <InputSelect.Content>
                                     <InputSelect.Item
                                       value={
                                         MCPOAuthProviderMode.AUTO_DISCOVERY
                                       }
-                                      description="Use MCP SDK challenge/discovery flow (default)."
+                                      description={tr(
+                                        "Use MCP SDK challenge/discovery flow (default).",
+                                        "MCP SDK のチャレンジ/ディスカバリーフローを使用します（既定）。"
+                                      )}
                                     >
-                                      Auto Discovery
+                                      {tr("Auto Discovery", "自動検出")}
                                     </InputSelect.Item>
                                     <InputSelect.Item
                                       value={
                                         MCPOAuthProviderMode.KNOWN_PROVIDER
                                       }
-                                      description="Use configured authorization/token endpoints."
+                                      description={tr(
+                                        "Use configured authorization/token endpoints.",
+                                        "設定済みの認可/トークンエンドポイントを使用します。"
+                                      )}
                                     >
-                                      Known Provider
+                                      {tr("Known Provider", "既知のプロバイダー")}
                                     </InputSelect.Item>
                                   </InputSelect.Content>
                                 </InputSelect>
@@ -765,7 +831,10 @@ export default function MCPAuthenticationModal({
                                   }
                                 >
                                   <FormField.Label>
-                                    Authorization Endpoint
+                                    {tr(
+                                      "Authorization Endpoint",
+                                      "認可エンドポイント"
+                                    )}
                                   </FormField.Label>
                                   <FormField.Control asChild>
                                     <InputTypeIn
@@ -799,7 +868,7 @@ export default function MCPAuthenticationModal({
                                   }
                                 >
                                   <FormField.Label>
-                                    Token Endpoint
+                                    {tr("Token Endpoint", "トークンエンドポイント")}
                                   </FormField.Label>
                                   <FormField.Control asChild>
                                     <InputTypeIn
@@ -817,9 +886,20 @@ export default function MCPAuthenticationModal({
                                 </FormField>
 
                                 <FormField name="oauth_scopes_override">
-                                  <FormField.Label optional>
-                                    Scopes Override (comma-separated)
-                                  </FormField.Label>
+                                <FormField.Label>
+                                  {tr(
+                                    "Scopes Override (comma-separated)",
+                                    "スコープの上書き（カンマ区切り）"
+                                  )}
+                                  <Text
+                                    as="p"
+                                    text03
+                                    mainUiMuted
+                                    className="mx-0.5"
+                                  >
+                                    {tr("(Optional)", "（任意）")}
+                                  </Text>
+                                </FormField.Label>
                                   <FormField.Control asChild>
                                     <InputTypeIn
                                       name="oauth_scopes_override"
@@ -831,8 +911,19 @@ export default function MCPAuthenticationModal({
                                 </FormField>
 
                                 <FormField name="oauth_additional_auth_params">
-                                  <FormField.Label optional>
-                                    Additional Auth Params (JSON)
+                                  <FormField.Label>
+                                    {tr(
+                                      "Additional Auth Params (JSON)",
+                                      "追加認証パラメータ (JSON)"
+                                    )}
+                                    <Text
+                                      as="p"
+                                      text03
+                                      mainUiMuted
+                                      className="mx-0.5"
+                                    >
+                                      {tr("(Optional)", "（任意）")}
+                                    </Text>
                                   </FormField.Label>
                                   <FormField.Control asChild>
                                     <InputTypeIn
@@ -847,10 +938,13 @@ export default function MCPAuthenticationModal({
                                 </FormField>
 
                                 <Text as="p" text03 secondaryBody>
-                                  Known-provider mode requires endpoint
-                                  configuration. Google reference endpoints:
-                                  authorization{" "}
-                                  {GOOGLE_AUTHORIZATION_ENDPOINT_HINT} and token{" "}
+                                  {tr(
+                                    "Known-provider mode requires endpoint configuration. Google reference endpoints:",
+                                    "既知プロバイダーモードではエンドポイント設定が必要です。Google 参考エンドポイント:"
+                                  )}{" "}
+                                  {tr("authorization", "認可")}{" "}
+                                  {GOOGLE_AUTHORIZATION_ENDPOINT_HINT}{" "}
+                                  {tr("and token", "、トークン")}{" "}
                                   {GOOGLE_TOKEN_ENDPOINT_HINT}.
                                 </Text>
                               </>
@@ -879,10 +973,10 @@ export default function MCPAuthenticationModal({
                       >
                         <Tabs.List>
                           <Tabs.Trigger value="per-user">
-                            Individual Key (Per User)
+                            {tr("Individual Key (Per User)", "個別キー（ユーザー別）")}
                           </Tabs.Trigger>
                           <Tabs.Trigger value="admin">
-                            Shared Key (Admin)
+                            {tr("Shared Key (Admin)", "共有キー（管理者）")}
                           </Tabs.Trigger>
                         </Tabs.List>
 
@@ -907,19 +1001,25 @@ export default function MCPAuthenticationModal({
                                     : "idle"
                               }
                             >
-                              <FormField.Label>API Key</FormField.Label>
+                              <FormField.Label>
+                                {tr("API Key", "API キー")}
+                              </FormField.Label>
                               <FormField.Control asChild>
                                 <PasswordInputTypeIn
                                   name="api_token"
                                   value={values.api_token}
                                   onChange={handleChange}
-                                  placeholder="Shared API key for your organization"
+                                  placeholder={tr(
+                                    "Shared API key for your organization",
+                                    "組織で共有する API キー"
+                                  )}
                                 />
                               </FormField.Control>
                               <FormField.Description>
-                                Do not use your personal API key. Make sure this
-                                key is appropriate to share with everyone in
-                                your organization.
+                                {tr(
+                                  "Do not use your personal API key. Make sure this key is appropriate to share with everyone in your organization.",
+                                  "個人用の API キーは使わないでください。このキーが組織の全員と共有して問題ないことを確認してください。"
+                                )}
                               </FormField.Description>
                               <FormField.Message
                                 messages={{
@@ -934,14 +1034,26 @@ export default function MCPAuthenticationModal({
                   )}
                   {values.auth_type === MCPAuthenticationType.NONE && (
                     <MessageCard
-                      title="No authentication for this MCP server"
-                      description="No authentication will be used for this connection. Make sure you trust this server. You are responsible for actions taken with this connection."
+                      title={tr(
+                        "No authentication for this MCP server",
+                        "この MCP サーバーには認証を使いません"
+                      )}
+                      description={tr(
+                        "No authentication will be used for this connection. Make sure you trust this server. You are responsible for actions taken with this connection.",
+                        "この接続では認証を使用しません。信頼できるサーバーか確認してください。この接続で実行される操作は利用者の責任です。"
+                      )}
                     />
                   )}
                   {values.auth_type === MCPAuthenticationType.PT_OAUTH && (
                     <MessageCard
-                      title="Use pass-through for services with shared identity provider."
-                      description="Onyx will forward the user's OAuth access token directly to the server as an Authorization header. Make sure the server supports authentication with the same provider."
+                      title={tr(
+                        "Use pass-through for services with shared identity provider.",
+                        "共有 ID プロバイダーを使うサービスではパススルーを使用します。"
+                      )}
+                      description={tr(
+                        "Onyx will forward the user's OAuth access token directly to the server as an Authorization header. Make sure the server supports authentication with the same provider.",
+                        "Onyx はユーザーの OAuth アクセストークンを Authorization ヘッダーとしてサーバーへ直接転送します。サーバーが同じプロバイダーでの認証に対応していることを確認してください。"
+                      )}
                     />
                   )}
                 </Modal.Body>
@@ -952,14 +1064,14 @@ export default function MCPAuthenticationModal({
                     type="button"
                     onClick={() => toggle(false)}
                   >
-                    Cancel
+                    {tr("Cancel", "キャンセル")}
                   </Button>
                   <Button
                     disabled={!isValid || isSubmitting}
                     type="submit"
                     data-testid="mcp-auth-connect-button"
                   >
-                    {isSubmitting ? "Connecting..." : "Connect"}
+                    {isSubmitting ? tr("Connecting...", "接続中...") : tr("Connect", "接続")}
                   </Button>
                 </Modal.Footer>
               </Form>
diff --git a/web/src/sections/actions/modals/OpenAPIAuthenticationModal.tsx b/web/src/sections/actions/modals/OpenAPIAuthenticationModal.tsx
index 631f6fcfd7..0ef2c87833 100644
--- a/web/src/sections/actions/modals/OpenAPIAuthenticationModal.tsx
+++ b/web/src/sections/actions/modals/OpenAPIAuthenticationModal.tsx
@@ -19,6 +19,7 @@ import { getOAuthConfig } from "@/lib/oauth/api";
 import { SvgArrowExchange } from "@opal/icons";
 import { useAuthType } from "@/lib/hooks";
 import { AuthType } from "@/lib/constants";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export type AuthMethod = "oauth" | "custom-header" | "pt-oauth";
 
@@ -68,7 +69,7 @@ export default function OpenAPIAuthenticationModal({
   isOpen,
   onClose,
   title,
-  description = "Authenticate your connection to start using the OpenAPI actions.",
+  description,
   skipOverlay = false,
   defaultMethod = "oauth",
   oauthConfigId = null,
@@ -78,6 +79,9 @@ export default function OpenAPIAuthenticationModal({
   onSkip,
   entityName = null,
 }: OpenAPIAuthenticationModalProps) {
+  const { locale } = useLocale();
+  const isJa = locale === "ja";
+  const tr = (en: string, ja: string) => (isJa ? ja : en);
   const authType = useAuthType();
   const isOAuthEnabled =
     authType === AuthType.OIDC || authType === AuthType.GOOGLE_OAUTH;
@@ -102,6 +106,12 @@ export default function OpenAPIAuthenticationModal({
     }
     return `${window.location.origin}/oauth-config/callback`;
   }, []);
+  const modalDescription =
+    description ??
+    tr(
+      "Authenticate your connection to start using the OpenAPI actions.",
+      "OpenAPI アクションを使うために接続を認証してください。"
+    );
 
   useEffect(() => {
     let isActive = true;
@@ -129,7 +139,10 @@ export default function OpenAPIAuthenticationModal({
         if (isActive) {
           setExistingOAuthConfig(null);
           setOAuthConfigError(
-            "Failed to load existing OAuth configuration. Re-enter the details to update it."
+            tr(
+              "Failed to load existing OAuth configuration. Re-enter the details to update it.",
+              "既存の OAuth 設定の読み込みに失敗しました。更新する場合は内容を再入力してください。"
+            )
           );
         }
       } finally {
@@ -203,7 +216,7 @@ export default function OpenAPIAuthenticationModal({
             ),
         }),
       }),
-    [isEditingOAuthConfig]
+    [isEditingOAuthConfig, tr]
   );
 
   const computedInitialValues = useMemo<OpenAPIAuthFormValues>(() => {
@@ -316,7 +329,7 @@ export default function OpenAPIAuthenticationModal({
         <Modal.Header
           icon={SvgArrowExchange}
           title={title}
-          description={description}
+          description={modalDescription}
           onClose={onClose}
         />
 
@@ -355,7 +368,10 @@ export default function OpenAPIAuthenticationModal({
                 {shouldDisableForm ? (
                   <div className="flex min-h-[220px] items-center justify-center rounded-12 border border-border-01 bg-background-tint-00">
                     <Text as="p" secondaryBody text03>
-                      Loading existing configuration...
+                      {tr(
+                        "Loading existing configuration...",
+                        "既存の設定を読み込んでいます..."
+                      )}
                     </Text>
                   </div>
                 ) : (
@@ -371,7 +387,9 @@ export default function OpenAPIAuthenticationModal({
                               : "idle"
                         }
                       >
-                        <FormField.Label>Authentication Method</FormField.Label>
+                        <FormField.Label>
+                          {tr("Authentication Method", "認証方式")}
+                        </FormField.Label>
                         <FormField.Control asChild>
                           <InputSelect
                             value={values.authMethod}
@@ -379,27 +397,41 @@ export default function OpenAPIAuthenticationModal({
                               setFieldValue("authMethod", value)
                             }
                           >
-                            <InputSelect.Trigger placeholder="Select method" />
+                            <InputSelect.Trigger
+                              placeholder={tr("Select method", "方式を選択")}
+                            />
                             <InputSelect.Content>
                               <InputSelect.Item
                                 value="oauth"
-                                description="Each user authenticates via OAuth with their own credentials."
+                                description={tr(
+                                  "Each user authenticates via OAuth with their own credentials.",
+                                  "各ユーザーが自分の認証情報で OAuth 認証します。"
+                                )}
                               >
                                 OAuth
                               </InputSelect.Item>
                               {isOAuthEnabled && (
                                 <InputSelect.Item
                                   value="pt-oauth"
-                                  description="Forward the user's OAuth access token used to authenticate Onyx."
+                                  description={tr(
+                                    "Forward the user's OAuth access token used to authenticate Onyx.",
+                                    "Onyx の認証に使うユーザーの OAuth アクセストークンをそのまま転送します。"
+                                  )}
                                 >
-                                  OAuth Pass-through
+                                  {tr("OAuth Pass-through", "OAuth パススルー")}
                                 </InputSelect.Item>
                               )}
                               <InputSelect.Item
                                 value="custom-header"
-                                description="Send custom headers with every request."
+                                description={tr(
+                                  "Send custom headers with every request.",
+                                  "すべてのリクエストにカスタムヘッダーを付与します。"
+                                )}
                               >
-                                Custom Authorization Header
+                                {tr(
+                                  "Custom Authorization Header",
+                                  "カスタム認証ヘッダー"
+                                )}
                               </InputSelect.Item>
                             </InputSelect.Content>
                           </InputSelect>
@@ -426,7 +458,9 @@ export default function OpenAPIAuthenticationModal({
                                 : "idle"
                           }
                         >
-                          <FormField.Label>Authorization URL</FormField.Label>
+                          <FormField.Label>
+                            {tr("Authorization URL", "認可 URL")}
+                          </FormField.Label>
                           <FormField.Control asChild>
                             <InputTypeIn
                               name="authorizationUrl"
@@ -452,7 +486,9 @@ export default function OpenAPIAuthenticationModal({
                                 : "idle"
                           }
                         >
-                          <FormField.Label>Token URL</FormField.Label>
+                          <FormField.Label>
+                            {tr("Token URL", "トークン URL")}
+                          </FormField.Label>
                           <FormField.Control asChild>
                             <InputTypeIn
                               name="tokenUrl"
@@ -478,7 +514,9 @@ export default function OpenAPIAuthenticationModal({
                                 : "idle"
                           }
                         >
-                          <FormField.Label>OAuth Client ID</FormField.Label>
+                          <FormField.Label>
+                            {tr("OAuth Client ID", "OAuth クライアント ID")}
+                          </FormField.Label>
                           <FormField.Control asChild>
                             <InputTypeIn
                               name="clientId"
@@ -489,7 +527,10 @@ export default function OpenAPIAuthenticationModal({
                           </FormField.Control>
                           {isEditingOAuthConfig && (
                             <FormField.Description>
-                              Leave blank to keep the current client ID.
+                              {tr(
+                                "Leave blank to keep the current client ID.",
+                                "現在のクライアント ID を保持するには空欄のままにしてください。"
+                              )}
                             </FormField.Description>
                           )}
                           <FormField.Message
@@ -509,7 +550,12 @@ export default function OpenAPIAuthenticationModal({
                                 : "idle"
                           }
                         >
-                          <FormField.Label>OAuth Client Secret</FormField.Label>
+                          <FormField.Label>
+                            {tr(
+                              "OAuth Client Secret",
+                              "OAuth クライアントシークレット"
+                            )}
+                          </FormField.Label>
                           <FormField.Control asChild>
                             <PasswordInputTypeIn
                               name="clientSecret"
@@ -520,7 +566,10 @@ export default function OpenAPIAuthenticationModal({
                           </FormField.Control>
                           {isEditingOAuthConfig && (
                             <FormField.Description>
-                              Leave blank to keep the current client secret.
+                              {tr(
+                                "Leave blank to keep the current client secret.",
+                                "現在のクライアントシークレットを保持するには空欄のままにしてください。"
+                              )}
                             </FormField.Description>
                           )}
                           <FormField.Message
@@ -541,19 +590,24 @@ export default function OpenAPIAuthenticationModal({
                           }
                         >
                           <FormField.Label>
-                            Scopes{" "}
-                            <span className="text-text-03">(Optional)</span>
+                            {tr("Scopes", "スコープ")}{" "}
+                            <span className="text-text-03">
+                              {tr("(Optional)", "（任意）")}
+                            </span>
                           </FormField.Label>
                           <FormField.Control asChild>
                             <InputTypeIn
                               name="scopes"
                               value={values.scopes}
                               onChange={handleChange}
-                              placeholder="e.g. repo, user"
+                              placeholder={tr("e.g. repo, user", "例: repo, user")}
                             />
                           </FormField.Control>
                           <FormField.Description>
-                            Comma-separated list of OAuth scopes to request.
+                            {tr(
+                              "Comma-separated list of OAuth scopes to request.",
+                              "要求する OAuth スコープをカンマ区切りで入力します。"
+                            )}
                           </FormField.Description>
                           <FormField.Message
                             messages={{
@@ -564,8 +618,10 @@ export default function OpenAPIAuthenticationModal({
 
                         <div className="flex flex-col gap-3 rounded-12 bg-background-tint-01 p-3">
                           <Text as="p" text03 secondaryBody>
-                            OAuth passthrough is only available if you enable
-                            OIDC or OAuth authentication.
+                            {tr(
+                              "OAuth passthrough is only available if you enable OIDC or OAuth authentication.",
+                              "OAuth パススルーは OIDC または OAuth 認証を有効にした場合のみ利用できます。"
+                            )}
                           </Text>
                           <div className="flex flex-col gap-2 w-full">
                             <Text
@@ -574,9 +630,9 @@ export default function OpenAPIAuthenticationModal({
                               secondaryBody
                               className="flex flex-wrap gap-1"
                             >
-                              Use{" "}
+                              {tr("Use", "使用する")}{" "}
                               <span className="font-secondary-action">
-                                redirect URI
+                                {tr("redirect URI", "リダイレクト URI")}
                               </span>
                               :
                             </Text>
@@ -590,7 +646,10 @@ export default function OpenAPIAuthenticationModal({
                               </Text>
                               <CopyButton
                                 getCopyText={() => redirectUri}
-                                tooltip="Copy redirect URI"
+                                tooltip={tr(
+                                  "Copy redirect URI",
+                                  "リダイレクト URI をコピー"
+                                )}
                                 prominence="tertiary"
                                 size="sm"
                               />
@@ -603,11 +662,13 @@ export default function OpenAPIAuthenticationModal({
                       <section className="flex flex-col gap-4 rounded-12 bg-background-tint-00 border border-border-01 p-4">
                         <div className="flex flex-col gap-2">
                           <Text as="p" mainUiAction text04>
-                            Authentication Headers
+                            {tr("Authentication Headers", "認証ヘッダー")}
                           </Text>
                           <Text as="p" secondaryBody text03>
-                            Specify custom headers for all requests sent to this
-                            action&apos;s API endpoint.
+                            {tr(
+                              "Specify custom headers for all requests sent to this action&apos;s API endpoint.",
+                              "このアクションの API エンドポイントに送るすべてのリクエストにカスタムヘッダーを指定します。"
+                            )}
                           </Text>
                         </div>
                         <FormField
@@ -616,13 +677,16 @@ export default function OpenAPIAuthenticationModal({
                         >
                           <FormField.Control asChild>
                             <KeyValueInput
-                              keyTitle="Header"
-                              valueTitle="Value"
+                              keyTitle={tr("Header", "ヘッダー")}
+                              valueTitle={tr("Value", "値")}
                               items={values.headers}
                               onChange={(items) =>
                                 setFieldValue("headers", items)
                               }
-                              addButtonLabel="Add Header"
+                              addButtonLabel={tr(
+                                "Add Header",
+                                "ヘッダーを追加"
+                              )}
                               onValidationError={(message) =>
                                 setFieldError("headers", message || undefined)
                               }
@@ -642,8 +706,14 @@ export default function OpenAPIAuthenticationModal({
                     )}
                     {values.authMethod === "pt-oauth" && (
                       <MessageCard
-                        title="Use pass-through for services with shared identity provider."
-                        description="Onyx will forward the user's OAuth access token directly to the server as an Authorization header. Make sure the server supports authentication with the same provider."
+                        title={tr(
+                          "Use pass-through for services with shared identity provider.",
+                          "共有 ID プロバイダーを使うサービスではパススルーを使用します。"
+                        )}
+                        description={tr(
+                          "Onyx will forward the user's OAuth access token directly to the server as an Authorization header. Make sure the server supports authentication with the same provider.",
+                          "Onyx はユーザーの OAuth アクセストークンを Authorization ヘッダーとしてサーバーへ直接転送します。サーバーが同じプロバイダーでの認証に対応していることを確認してください。"
+                        )}
                       />
                     )}
                   </>
@@ -656,7 +726,7 @@ export default function OpenAPIAuthenticationModal({
                   type="button"
                   onClick={handleSkip}
                 >
-                  Cancel
+                  {tr("Cancel", "キャンセル")}
                 </Button>
                 <Button
                   disabled={
@@ -664,7 +734,9 @@ export default function OpenAPIAuthenticationModal({
                   }
                   type="submit"
                 >
-                  {isSubmitting ? "Connecting..." : "Connect"}
+                  {isSubmitting
+                    ? tr("Connecting...", "接続中...")
+                    : tr("Connect", "接続")}
                 </Button>
               </Modal.Footer>
             </Form>
diff --git a/web/src/sections/sidebar/AccountPopover.tsx b/web/src/sections/sidebar/AccountPopover.tsx
index e34df25b59..72aa82f3f6 100644
--- a/web/src/sections/sidebar/AccountPopover.tsx
+++ b/web/src/sections/sidebar/AccountPopover.tsx
@@ -36,6 +36,8 @@ import UserAvatar from "@/refresh-components/avatars/UserAvatar";
 import { useNotificationSummary } from "@/hooks/useNotifications";
 import { SvgOnyxLogo } from "@opal/logos";
 import { markdown } from "@opal/utils";
+import { useLocale } from "@/providers/LocaleProvider";
+import LanguageSwitcher from "@/components/LanguageSwitcher";
 
 interface SettingsPopoverProps {
   onUserSettingsClick: () => void;
@@ -53,6 +55,7 @@ function SettingsPopover({
   const router = useRouter();
   const pathname = usePathname();
   const searchParams = useSearchParams();
+  const { locale, t } = useLocale();
   const isAnonymousUser =
     user?.is_anonymous_user || checkUserIsNoAuthUser(user?.id ?? "");
   const showLogout = user && !isAnonymousUser && !LOGOUT_DISABLED;
@@ -70,7 +73,7 @@ function SettingsPopover({
     logout()
       .then((response) => {
         if (!response?.ok) {
-          alert("Failed to logout");
+          alert(t(locale, "sidebar.failedLogout"));
           return;
         }
 
@@ -86,7 +89,7 @@ function SettingsPopover({
       })
 
       .catch(() => {
-        toast.error("Failed to logout");
+        toast.error(t(locale, "sidebar.failedLogout"));
       });
   };
 
@@ -103,7 +106,7 @@ function SettingsPopover({
             variant="section"
             rounding="sm"
             icon={SvgSliders}
-            title="Settings"
+            title={t(locale, "sidebar.settings")}
             href="/app/settings"
             onClick={onUserSettingsClick}
           />
@@ -114,7 +117,7 @@ function SettingsPopover({
           variant="section"
           rounding="sm"
           icon={SvgBell}
-          title="Notifications"
+          title={t(locale, "sidebar.notifications")}
           onClick={onOpenNotifications}
           rightChildren={
             undismissedCount ? (
@@ -128,7 +131,7 @@ function SettingsPopover({
           variant="section"
           rounding="sm"
           icon={SvgHelpCircle}
-          title="Help & FAQ"
+          title={t(locale, "sidebar.helpFaq")}
           href="https://docs.onyx.app"
           target="_blank"
         />,
@@ -154,7 +157,7 @@ function SettingsPopover({
             variant="section"
             rounding="sm"
             icon={SvgUser}
-            title="Log in"
+            title={t(locale, "auth.logIn")}
             onClick={handleLogin}
           />
         ),
@@ -166,11 +169,15 @@ function SettingsPopover({
             color="danger"
             rounding="sm"
             icon={SvgLogOut}
-            title="Log Out"
+            title={t(locale, "auth.logOut")}
             onClick={handleLogout}
           />
         ),
         null,
+        <div key="language" className="p-2">
+          <LanguageSwitcher />
+        </div>,
+        null,
         <div key="version" className="p-2">
           <Content
             sizePreset="secondary"
diff --git a/web/src/sections/sidebar/AdminSidebar.tsx b/web/src/sections/sidebar/AdminSidebar.tsx
index 96c916f7ea..354e9e3116 100644
--- a/web/src/sections/sidebar/AdminSidebar.tsx
+++ b/web/src/sections/sidebar/AdminSidebar.tsx
@@ -31,12 +31,12 @@ import { markdown } from "@opal/utils";
 
 const SECTIONS = {
   UNLABELED: "",
-  AGENTS_AND_ACTIONS: "Agents & Actions",
-  DOCUMENTS_AND_KNOWLEDGE: "Documents & Knowledge",
-  INTEGRATIONS: "Integrations",
-  PERMISSIONS: "Permissions",
-  ORGANIZATION: "Organization",
-  USAGE: "Usage",
+  AGENTS_AND_ACTIONS: "エージェントとアクション",
+  DOCUMENTS_AND_KNOWLEDGE: "ドキュメントとナレッジ",
+  INTEGRATIONS: "連携",
+  PERMISSIONS: "権限",
+  ORGANIZATION: "組織",
+  USAGE: "利用状況",
 } as const;
 
 interface SidebarItemEntry {
@@ -160,7 +160,7 @@ function buildItems(
   if (!isCurator && !hasSubscription) {
     items.push({
       section: SECTIONS.UNLABELED,
-      name: "Upgrade Plan",
+      name: "プランをアップグレード",
       icon: SvgArrowUpCircle,
       link: ADMIN_ROUTES.BILLING.path,
     });
@@ -249,14 +249,14 @@ function AdminSidebarInner() {
               setFocusSearch(true);
             }}
           >
-            Search
+            検索
           </SidebarTab>
         ) : (
           <InputTypeIn
             ref={searchRef}
             variant="internal"
             searchIcon
-            placeholder="Search..."
+            placeholder="検索..."
             value={query}
             onChange={(e) => setQuery(e.target.value)}
             clearButton
@@ -303,8 +303,8 @@ function AdminSidebarInner() {
                 icon={icon}
                 tooltip={markdown(
                   requiredTier === Tier.ENTERPRISE
-                    ? "This feature is available on the [Enterprise version of Onyx](/admin/billing) only."
-                    : "This feature is available on the [Business or Enterprise version of Onyx](/admin/billing) only."
+                    ? "この機能は [Onyx Enterprise 版](/admin/billing) でのみ利用できます。"
+                    : "この機能は [Onyx Business または Enterprise 版](/admin/billing) でのみ利用できます。"
                 )}
               >
                 {name}
@@ -327,7 +327,7 @@ function AdminSidebarInner() {
           variant="sidebar-light"
           folded={folded}
         >
-          Exit Admin Panel
+          管理パネルを終了
         </SidebarTab>
         <AccountPopover folded={folded} />
       </SidebarLayouts.Footer>
diff --git a/web/src/sections/sidebar/AppSidebar.tsx b/web/src/sections/sidebar/AppSidebar.tsx
index 9ff8905fb7..fca76696fc 100644
--- a/web/src/sections/sidebar/AppSidebar.tsx
+++ b/web/src/sections/sidebar/AppSidebar.tsx
@@ -86,6 +86,7 @@ import { dismissNotification } from "@/lib/notifications/api";
 import AccountPopover from "@/sections/sidebar/AccountPopover";
 import ChatSearchCommandMenu from "@/sections/sidebar/ChatSearchCommandMenu";
 import { useQueryController } from "@/providers/QueryControllerProvider";
+import { useLocale } from "@/providers/LocaleProvider";
 
 // Visible-agents = pinned-agents + current-agent (if current-agent not in pinned-agents)
 // OR Visible-agents = pinned-agents (if current-agent in pinned-agents)
@@ -126,6 +127,7 @@ function RecentsSection({
   isLoadingMore,
   onLoadMore,
 }: RecentsSectionProps) {
+  const { locale, t } = useLocale();
   const { setNodeRef, isOver } = useDroppable({
     id: DRAG_TYPES.RECENTS,
     data: {
@@ -168,10 +170,10 @@ function RecentsSection({
         isOver && "bg-background-tint-03"
       )}
     >
-      <SidebarSection title="Recents">
+      <SidebarSection title={t(locale, "sidebar.recents")}>
         {chatSessions.length === 0 ? (
           <Text as="p" text01 className="px-3">
-            Try sending a message! Your chat history will appear here.
+            {t(locale, "sidebar.trySendingMessage")}
           </Text>
         ) : (
           <>
@@ -209,6 +211,7 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
   const posthog = usePostHog();
   const { newTenantInfo, invitationInfo } = useModalContext();
   const { setAppMode, reset } = useQueryController();
+  const { locale, t } = useLocale();
 
   // Use SWR hooks for data fetching
   const {
@@ -393,6 +396,7 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
         refreshCurrentProjectDetails,
         fetchProjects: refreshProjects,
         currentProjectId,
+        errorMessage: t(locale, "sidebar.failedMoveChat"),
       });
       const projectRefreshPromise = currentProjectId
         ? refreshCurrentProjectDetails()
@@ -450,7 +454,7 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
         try {
           await performChatMove(targetProject.id, chatSession);
         } catch (error) {
-          showErrorNotification("Failed to move chat. Please try again.");
+          showErrorNotification(t(locale, "sidebar.failedMoveChat"));
         }
       }
 
@@ -481,6 +485,8 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
       refreshChatSessions,
       refreshCurrentProjectDetails,
       refreshProjects,
+      locale,
+      t,
     ]
   );
 
@@ -508,7 +514,7 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
             reset();
           }}
         >
-          New Session
+          {t(locale, "sidebar.newSession")}
         </SidebarTab>
       </div>
     );
@@ -518,6 +524,8 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
     combinedSettings,
     currentAgent,
     defaultAppMode,
+    locale,
+    t,
   ]);
 
   const buildButton = useMemo(
@@ -541,12 +549,12 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
       <ChatSearchCommandMenu
         trigger={
           <SidebarTab icon={SvgSearchMenu} folded={folded}>
-            Search Chats
+            {t(locale, "sidebar.searchChats")}
           </SidebarTab>
         }
       />
     ),
-    [folded]
+    [folded, locale, t]
   );
   const moreAgentsButton = useMemo(
     () => (
@@ -562,11 +570,13 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
           selected={activeSidebarTab.isMoreAgents()}
           variant={folded ? "sidebar-heavy" : "sidebar-light"}
         >
-          {visibleAgents.length === 0 ? "Explore Agents" : "More Agents"}
+          {visibleAgents.length === 0
+            ? t(locale, "sidebar.exploreAgents")
+            : t(locale, "sidebar.moreAgents")}
         </SidebarTab>
       </div>
     ),
-    [folded, activeSidebarTab, visibleAgents]
+    [folded, activeSidebarTab, visibleAgents, locale, t]
   );
   const newProjectButton = useMemo(
     () => (
@@ -577,10 +587,10 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
         folded={folded}
         variant={folded ? "sidebar-heavy" : "sidebar-light"}
       >
-        New Project
+        {t(locale, "sidebar.newProject")}
       </SidebarTab>
     ),
-    [folded, createProjectModal.toggle, createProjectModal.isOpen]
+    [folded, createProjectModal.toggle, createProjectModal.isOpen, locale, t]
   );
   const handleShowBuildIntro = useCallback(() => {
     setShowIntroAnimation(true);
@@ -599,7 +609,9 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
             icon={SvgSettings}
             folded={folded}
           >
-            {isAdmin ? "Admin Panel" : "Curator Panel"}
+            {isAdmin
+              ? t(locale, "sidebar.adminPanel")
+              : t(locale, "sidebar.curatorPanel")}
           </SidebarTab>
         )}
         <AccountPopover
@@ -610,7 +622,15 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
         />
       </div>
     ),
-    [folded, isAdmin, isCurator, handleShowBuildIntro, isOnyxCraftEnabled]
+    [
+      folded,
+      isAdmin,
+      isCurator,
+      handleShowBuildIntro,
+      isOnyxCraftEnabled,
+      locale,
+      t,
+    ]
   );
 
   return (
@@ -642,7 +662,7 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
               try {
                 await performChatMove(target, chat);
               } catch (error) {
-                showErrorNotification("Failed to move chat. Please try again.");
+                showErrorNotification(t(locale, "sidebar.failedMoveChat"));
               }
             }
           }}
@@ -694,7 +714,7 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
               collisionDetection={closestCenter}
               onDragEnd={handleAgentDragEnd}
             >
-              <SidebarSection title="Agents">
+              <SidebarSection title={t(locale, "sidebar.agents")}>
                 <SortableContext
                   items={visibleAgentIds}
                   strategy={verticalListSortingStrategy}
@@ -719,13 +739,13 @@ const MemoizedAppSidebarInner = memo(function AppSidebarInner() {
             >
               {/* Projects */}
               <SidebarSection
-                title="Projects"
+                title={t(locale, "sidebar.projects")}
                 action={
                   <OpalButton
                     icon={SvgFolderPlus}
                     prominence="tertiary"
                     size="sm"
-                    tooltip="New Project"
+                    tooltip={t(locale, "sidebar.newProject")}
                     onClick={() => createProjectModal.toggle(true)}
                   />
                 }
diff --git a/web/src/sections/sidebar/ChatButton.tsx b/web/src/sections/sidebar/ChatButton.tsx
index bfe288a43c..5b7ce126ed 100644
--- a/web/src/sections/sidebar/ChatButton.tsx
+++ b/web/src/sections/sidebar/ChatButton.tsx
@@ -42,6 +42,7 @@ import {
 } from "@opal/icons";
 import useOnMount from "@/hooks/useOnMount";
 import { useAgents, usePinnedAgents } from "@/lib/agents/hooks";
+import { useLocale } from "@/providers/LocaleProvider";
 
 export interface PopoverSearchInputProps {
   setShowMoveOptions: (show: boolean) => void;
@@ -52,6 +53,7 @@ export function PopoverSearchInput({
   setShowMoveOptions,
   onSearch,
 }: PopoverSearchInputProps) {
+  const { locale, t } = useLocale();
   const [searchTerm, setSearchTerm] = useState("");
 
   const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
@@ -84,7 +86,7 @@ export function PopoverSearchInput({
         value={searchTerm}
         onChange={handleChange}
         onKeyDown={handleKeyDown}
-        placeholder="Search Projects"
+        placeholder={t(locale, "chat.searchProjects")}
         onClick={noProp()}
         variant="internal"
         autoFocus
@@ -101,6 +103,7 @@ export interface ChatButtonProps {
 
 const ChatButton = memo(
   ({ chatSession, project, draggable = false }: ChatButtonProps) => {
+    const { locale, t } = useLocale();
     const route = useAppRouter();
     const activeSidebarTab = useAppFocus();
     const active = useMemo(
@@ -126,7 +129,6 @@ const ChatButton = memo(
       projects,
       fetchProjects,
       currentProjectId,
-      createProject,
     } = useProjectsContext();
     const { agents } = useAgents();
     const { pinnedAgents, togglePinnedAgent } = usePinnedAgents();
@@ -193,7 +195,7 @@ const ChatButton = memo(
             sizePreset="main-ui"
             rounding="sm"
             icon={SvgShare}
-            title="Share"
+            title={t(locale, "chat.share")}
             onClick={noProp(() => setShowShareModal(true))}
           />,
           <LineItemButton
@@ -201,7 +203,7 @@ const ChatButton = memo(
             sizePreset="main-ui"
             rounding="sm"
             icon={SvgEdit}
-            title="Rename"
+            title={t(locale, "chat.rename")}
             onClick={noProp(() => setRenaming(true))}
           />,
           <LineItemButton
@@ -209,7 +211,7 @@ const ChatButton = memo(
             sizePreset="main-ui"
             rounding="sm"
             icon={SvgFolderIn}
-            title="Move to Project"
+            title={t(locale, "chat.moveToProject")}
             onClick={noProp(() => setShowMoveOptions(true))}
           />,
           project && (
@@ -218,7 +220,9 @@ const ChatButton = memo(
               sizePreset="main-ui"
               rounding="sm"
               icon={SvgFolder}
-              title={`Remove from ${project.name}`}
+              title={t(locale, "chat.removeFromProjectNamed", {
+                name: project.name,
+              })}
               onClick={noProp(() => handleRemoveFromProject())}
             />
           ),
@@ -229,7 +233,7 @@ const ChatButton = memo(
             rounding="sm"
             color="danger"
             icon={SvgTrash}
-            title="Delete"
+            title={t(locale, "chat.delete")}
             onClick={noProp(() => setDeleteConfirmationModalOpen(true))}
           />,
         ];
@@ -264,7 +268,9 @@ const ChatButton = memo(
                   sizePreset="main-ui"
                   rounding="sm"
                   icon={SvgFolderPlus}
-                  title={`Create ${searchTerm.trim()}`}
+                  title={t(locale, "chat.createProjectNamed", {
+                    name: searchTerm.trim(),
+                  })}
                   onClick={noProp(() =>
                     handleCreateProjectAndMove(searchTerm.trim())
                   )}
@@ -284,7 +290,8 @@ const ChatButton = memo(
       project,
       chatSession.id,
       searchTerm,
-      createProject,
+      locale,
+      t,
     ]);
 
     // Pin the chat's agent when clicking on the conversation
@@ -321,7 +328,7 @@ const ChatButton = memo(
         await refreshChatSessions();
       } catch (error) {
         console.error("Failed to delete chat:", error);
-        showErrorNotification("Failed to delete chat. Please try again.");
+        showErrorNotification(t(locale, "chat.failedDeleteChat"));
       }
     }
 
@@ -334,6 +341,7 @@ const ChatButton = memo(
           refreshCurrentProjectDetails,
           fetchProjects,
           currentProjectId,
+          errorMessage: t(locale, "sidebar.failedMoveChat"),
         });
         setShowMoveOptions(false);
         setSearchTerm("");
@@ -394,7 +402,7 @@ const ChatButton = memo(
         setNavigateAfterMoveProjectId(null);
       } catch (error) {
         console.error("Failed to create project and move chat:", error);
-        showErrorNotification("Failed to create project. Please try again.");
+        showErrorNotification(t(locale, "chat.failedCreateProject"));
         setNavigateAfterMoveProjectId(null);
       }
     }
@@ -457,7 +465,7 @@ const ChatButton = memo(
       <>
         {deleteConfirmationModalOpen && (
           <ConfirmationModalLayout
-            title="Delete Chat"
+            title={t(locale, "chat.deleteChatTitle")}
             icon={SvgTrash}
             onClose={() => setDeleteConfirmationModalOpen(false)}
             submit={
@@ -468,12 +476,11 @@ const ChatButton = memo(
                   handleChatDelete();
                 }}
               >
-                Delete
+                {t(locale, "chat.delete")}
               </Button>
             }
           >
-            Are you sure you want to delete this chat? This action cannot be
-            undone.
+            {t(locale, "chat.deleteChatBody")}
           </ConfirmationModalLayout>
         )}
 
diff --git a/web/src/sections/sidebar/ChatSearchCommandMenu.tsx b/web/src/sections/sidebar/ChatSearchCommandMenu.tsx
index 6522edcb2b..5f6d72586b 100644
--- a/web/src/sections/sidebar/ChatSearchCommandMenu.tsx
+++ b/web/src/sections/sidebar/ChatSearchCommandMenu.tsx
@@ -18,7 +18,6 @@ import { useCurrentAgent } from "@/lib/agents/hooks";
 import Text from "@/refresh-components/texts/Text";
 import {
   useChatSearchOptimistic,
-  FilterableChat,
 } from "./useChatSearchOptimistic";
 import {
   SvgEditBig,
@@ -29,21 +28,25 @@ import {
   SvgKeystroke,
 } from "@opal/icons";
 import TextSeparator from "@/refresh-components/TextSeparator";
+import { useLocale } from "@/providers/LocaleProvider";
 
-/**
- * Dynamic footer that shows contextual action labels based on highlighted item type
- */
 function DynamicFooter() {
+  const { locale, t } = useLocale();
   const { highlightedItemType } = useCommandMenuContext();
 
-  // "Show all" for filters, "Open" for everything else (items, actions, or no highlight)
-  const actionLabel = highlightedItemType === "filter" ? "Show all" : "Open";
+  const actionLabel =
+    highlightedItemType === "filter"
+      ? t(locale, "chat.showAll")
+      : t(locale, "chat.open");
 
   return (
     <CommandMenu.Footer
       leftActions={
         <>
-          <CommandMenu.FooterAction icon={SvgArrowUpDown} label="Select" />
+          <CommandMenu.FooterAction
+            icon={SvgArrowUpDown}
+            label={t(locale, "chat.select")}
+          />
           <CommandMenu.FooterAction icon={SvgKeystroke} label={actionLabel} />
         </>
       }
@@ -65,6 +68,7 @@ interface FilterableProject {
 export default function ChatSearchCommandMenu({
   trigger,
 }: ChatSearchCommandMenuProps) {
+  const { locale, t } = useLocale();
   const [open, setOpen] = useState(false);
   const [searchValue, setSearchValue] = useState("");
   const [activeFilter, setActiveFilter] = useState<
@@ -75,21 +79,17 @@ export default function ChatSearchCommandMenu({
   >();
   const router = useRouter();
 
-  // Data hooks
   const { projects } = useProjects();
   const combinedSettings = useSettingsContext();
   const currentAgent = useCurrentAgent();
   const createProjectModal = useCreateModal();
 
-  // Constants for preview limits
   const PREVIEW_CHATS_LIMIT = 4;
   const PREVIEW_PROJECTS_LIMIT = 3;
 
-  // Determine if we should enable optimistic search (when searching or viewing chats filter)
   const shouldUseOptimisticSearch =
     searchValue.trim().length > 0 || activeFilter === "chats";
 
-  // Use optimistic search hook for chat sessions (includes fallback from useChatSessions + useProjects)
   const {
     results: filteredChats,
     isSearching,
@@ -101,7 +101,6 @@ export default function ChatSearchCommandMenu({
     enabled: shouldUseOptimisticSearch,
   });
 
-  // Transform and filter projects (sorted by latest first)
   const filteredProjects = useMemo<FilterableProject[]>(() => {
     const projectList = projects
       .map((project) => ({
@@ -122,7 +121,6 @@ export default function ChatSearchCommandMenu({
     );
   }, [projects, searchValue]);
 
-  // Compute displayed items based on filter state
   const displayedChats = useMemo(() => {
     if (activeFilter === "all" && !searchValue.trim()) {
       return filteredChats.slice(0, PREVIEW_CHATS_LIMIT);
@@ -137,22 +135,20 @@ export default function ChatSearchCommandMenu({
     return filteredProjects;
   }, [filteredProjects, activeFilter, searchValue]);
 
-  // Header filters for showing active filter as a chip
   const headerFilters = useMemo(() => {
     if (activeFilter === "chats") {
-      return [{ id: "chats", label: "Sessions" }];
+      return [{ id: "chats", label: t(locale, "chat.sessions") }];
     }
     if (activeFilter === "projects") {
-      return [{ id: "projects", label: "Projects" }];
+      return [{ id: "projects", label: t(locale, "chat.projects") }];
     }
     return [];
-  }, [activeFilter]);
+  }, [activeFilter, locale, t]);
 
   const handleFilterRemove = useCallback(() => {
     setActiveFilter("all");
   }, []);
 
-  // Navigation handlers
   const handleNewSession = useCallback(() => {
     const href =
       combinedSettings?.settings?.disable_default_assistant && currentAgent
@@ -197,10 +193,8 @@ export default function ChatSearchCommandMenu({
 
   const handleEmptyBackspace = useCallback(() => {
     if (activeFilter !== "all") {
-      // Remove active filter, return to root menu
       setActiveFilter("all");
     } else {
-      // No filter active, close the menu
       setOpen(false);
     }
   }, [activeFilter]);
@@ -216,7 +210,7 @@ export default function ChatSearchCommandMenu({
       <CommandMenu open={open} onOpenChange={handleOpenChange}>
         <CommandMenu.Content>
           <CommandMenu.Header
-            placeholder="Search chat sessions, projects..."
+            placeholder={t(locale, "chat.searchChatsAndProjects")}
             value={searchValue}
             onValueChange={setSearchValue}
             filters={headerFilters}
@@ -227,10 +221,11 @@ export default function ChatSearchCommandMenu({
 
           <CommandMenu.List
             emptyMessage={
-              hasSearchValue ? "No results found" : "No chats or projects yet"
+              hasSearchValue
+                ? t(locale, "chat.noResults")
+                : t(locale, "chat.noChatsOrProjectsYet")
             }
           >
-            {/* New Session action - always visible in "all" filter, even during search */}
             {activeFilter === "all" && (
               <CommandMenu.Action
                 value="new-session"
@@ -238,11 +233,10 @@ export default function ChatSearchCommandMenu({
                 onSelect={handleNewSession}
                 defaultHighlight={!hasSearchValue}
               >
-                New Session
+                {t(locale, "sidebar.newSession")}
               </CommandMenu.Action>
             )}
 
-            {/* Recent Sessions section - show if filter is 'all' or 'chats' */}
             {(activeFilter === "all" || activeFilter === "chats") &&
               displayedChats.length > 0 && (
                 <>
@@ -255,7 +249,9 @@ export default function ChatSearchCommandMenu({
                         filteredChats.length <= PREVIEW_CHATS_LIMIT
                       }
                     >
-                      {activeFilter === "chats" ? "Recent" : "Recent Sessions"}
+                      {activeFilter === "chats"
+                        ? t(locale, "chat.recent")
+                        : t(locale, "chat.recentSessions")}
                     </CommandMenu.Filter>
                   )}
                   {displayedChats.map((chat) => (
@@ -266,7 +262,7 @@ export default function ChatSearchCommandMenu({
                       rightContent={({ isHighlighted }) =>
                         isHighlighted ? (
                           <Text figureKeystroke text02>
-                            ↵
+                            Enter
                           </Text>
                         ) : (
                           <Text secondaryBody text03>
@@ -279,7 +275,6 @@ export default function ChatSearchCommandMenu({
                       {highlightMatch(chat.label, searchValue)}
                     </CommandMenu.Item>
                   ))}
-                  {/* Infinite scroll sentinel and loading indicator for chats */}
                   {activeFilter === "chats" && hasMore && (
                     <div ref={sentinelRef} className="h-1" aria-hidden="true" />
                   )}
@@ -292,7 +287,6 @@ export default function ChatSearchCommandMenu({
                 </>
               )}
 
-            {/* Projects section - show if filter is 'all' or 'projects' */}
             {(activeFilter === "all" || activeFilter === "projects") && (
               <>
                 <CommandMenu.Filter
@@ -303,16 +297,15 @@ export default function ChatSearchCommandMenu({
                     filteredProjects.length <= PREVIEW_PROJECTS_LIMIT
                   }
                 >
-                  Projects
+                  {t(locale, "chat.projects")}
                 </CommandMenu.Filter>
-                {/* New Project action - shown after Projects filter when no search term */}
                 {!hasSearchValue && activeFilter === "all" && (
                   <CommandMenu.Action
                     value="new-project"
                     icon={SvgFolderPlus}
                     onSelect={() => handleNewProject()}
                   >
-                    New Project
+                    {t(locale, "sidebar.newProject")}
                   </CommandMenu.Action>
                 )}
                 {displayedProjects.map((project) => (
@@ -323,7 +316,7 @@ export default function ChatSearchCommandMenu({
                     rightContent={({ isHighlighted }) =>
                       isHighlighted ? (
                         <Text figureKeystroke text02>
-                          ↵
+                          Enter
                         </Text>
                       ) : (
                         <Text secondaryBody text03>
@@ -339,7 +332,6 @@ export default function ChatSearchCommandMenu({
               </>
             )}
 
-            {/* Create New Project with search term - shown at bottom when searching */}
             {hasSearchValue &&
               (activeFilter === "all" || activeFilter === "projects") && (
                 <CommandMenu.Action
@@ -347,20 +339,23 @@ export default function ChatSearchCommandMenu({
                   icon={SvgFolderPlus}
                   onSelect={() => handleNewProject(searchValue.trim())}
                 >
-                  <>
-                    Create New Project "
-                    <span className="text-text-05">{searchValue.trim()}</span>"
-                  </>
+                  <span className="text-text-05">
+                    {t(locale, "chat.createProjectNamed", {
+                      name: searchValue.trim(),
+                    })}
+                  </span>
                 </CommandMenu.Action>
               )}
 
-            {/* No more results divider - shown when no results for the active filter */}
             {((activeFilter === "chats" && displayedChats.length === 0) ||
               (activeFilter === "projects" && displayedProjects.length === 0) ||
               (activeFilter === "all" &&
                 displayedChats.length === 0 &&
                 displayedProjects.length === 0)) && (
-              <TextSeparator text="No more results" className="mt-auto mb-2" />
+              <TextSeparator
+                text={t(locale, "chat.noMoreResults")}
+                className="mt-auto mb-2"
+              />
             )}
           </CommandMenu.List>
 
@@ -368,7 +363,6 @@ export default function ChatSearchCommandMenu({
         </CommandMenu.Content>
       </CommandMenu>
 
-      {/* Project creation modal */}
       <createProjectModal.Provider>
         <CreateProjectModal initialProjectName={initialProjectName} />
       </createProjectModal.Provider>
diff --git a/web/src/sections/sidebar/sidebarUtils.ts b/web/src/sections/sidebar/sidebarUtils.ts
index a4b9e54327..61976c40dd 100644
--- a/web/src/sections/sidebar/sidebarUtils.ts
+++ b/web/src/sections/sidebar/sidebarUtils.ts
@@ -24,6 +24,7 @@ export interface MoveOperationParams {
   refreshCurrentProjectDetails: () => Promise<any>;
   fetchProjects: () => Promise<any>;
   currentProjectId: number | null;
+  errorMessage?: string;
 }
 
 export const handleMoveOperation = async ({
@@ -33,6 +34,7 @@ export const handleMoveOperation = async ({
   refreshCurrentProjectDetails,
   fetchProjects,
   currentProjectId,
+  errorMessage,
 }: MoveOperationParams) => {
   try {
     await moveChatSession(targetProjectId, chatSession.id);
@@ -42,7 +44,7 @@ export const handleMoveOperation = async ({
     await Promise.all([refreshChatSessions(), projectRefreshPromise]);
   } catch (error) {
     console.error("Failed to perform move operation:", error);
-    toast.error("Failed to move chat. Please try again.");
+    toast.error(errorMessage ?? "Failed to move chat. Please try again.");
     throw error;
   }
 };
-- 
2.54.0.windows.1

